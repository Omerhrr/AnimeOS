# Worklog

---
Task ID: 1
Agent: Super Z (main agent)
Task: Build the AI-Native Animation Production Platform ("Animation OS") MVP per the user's 2542-line vision doc (upload/Pasted Content_1790058255757.txt)

Work Log:
- Loaded fullstack-dev + LLM skills; initialized Next.js 16 scaffold via init script
- Designed Prisma schema (SQLite): Project/Season/Episode/Scene/Shot, Character/CharacterState/Relationship, Asset/AssetVersion, RenderJob/Evaluation, ContinuityEvent, Terminology, ProductionEvent, DshMessage
- Built core libs: continuity checker (conflict + missing-capability detection), render engine simulator (timed stage pipeline, param application), seed ("Immortal Path" donghua demo universe with Lin Yue development states, Scene 12 six-shot breakdown, Jade Sword destroyed-Ep29 continuity conflict, terminology)
- Built DSH brain: 15-tool production API (create_project/character/state/relationship/environment/asset/episode/scene/shot/terminology, add_continuity_event, check_continuity, check_capabilities, render_shot, get_production_context), JSON tool protocol, 4-round INTENT→PLAN→EXECUTE→OBSERVE orchestrator with mid-turn project switching, LLM render evaluator proposing bounded parameter modifications
- Built 13 API route handlers (all force-dynamic, malformed-body guards)
- Built studio UI: dark cinematic shell (zustand + TanStack Query), Dashboard, DSH Director console with execution-trace rendering, Productions (config form per §8), Characters (states/relationships/derivatives), Story & Scenes (capability ✓/✗, continuity conflicts with resolutions, shot list with render triggers), Timeline (duration-proportional status blocks), Render Queue (live progress, evaluation cards, apply-fixes/retry/approve), Continuity, Terminology, History
- Built Three.js cinematic preview (§37): procedural night-mountain MVP scene (§53) - peak, cloud sea, rain, lightning flashes + bolts, cultivator figure with glowing sword energy particles; shot camera presets derived from real shot records (movement-aware), driven by live scene params DSH tunes
- Fixed bugs found during verification: useRef/useState missing imports, continuity payload wrapper, static caching of GET /api/projects, DSH mid-turn project switch writing to old project, stale SQLite file handle after db delete (restarted via .zscripts/dev.sh detached)
- Verified end-to-end: seeded render completes → DSH LLM inspection returns NEEDS_REVISION + 4 bounded modifications → apply writes params to scene + auto-queues attempt 2; DSH live turns from UI (created "Azure Sky" production with 12 tool calls across 3 rounds; added terminology + continuity event) all traceable in console UI
- Agent Browser verification: dashboard, story, 3D preview (WebGL renders + shot auto-advance), render queue evaluation card, DSH console live turn, terminology, characters, timeline, mobile 390px - all rendered and interactive; fixed header overlap on mobile; lint clean; dev.log clean

Stage Summary:
- Deliverable: runnable Next.js 16 studio app at / (single route SPA), db/custom.db auto-seeded on first request
- Core thesis proven: DSH decides → tools execute → engine builds → render → DSH evaluates → revise → approve, with persistent universe state
- Key files: prisma/schema.prisma; src/lib/{types,seed,continuity,store,api-client}.ts; src/lib/engine/render.ts; src/lib/dsh/{tools,prompts,orchestrator,evaluator}.ts; src/app/api/**; src/components/studio/studio-shell.tsx; src/components/views/*.tsx; src/components/preview/cinematic-preview.tsx; scripts/{wipe.ts,test-render-loop.sh}
- Stack adaptations (per doc's replaceability principle): Next.js instead of Nuxt, Prisma/SQLite instead of Postgres, simulated engine driver instead of live Blender (bridge interface preserved), in-app job simulation instead of Redis/Celery

---
Task ID: 2
Agent: Super Z (main agent)
Task: Push MVP to GitHub (Omerhrr/AnimeOS) + next iteration (Comic Mode)

Work Log:
- Audited repo before push: no secrets in tree (token scan clean), untracked runtime artifacts (.env, db/custom.db, .zscripts/dev.pid), fixed mangled .gitignore append, added .env.example + db/.gitkeep with ignore negations
- Set repo-local identity to Omerhrr, added origin remote, pushed MVP commit e52394e to main, verified via ls-remote
- Iteration: Comic Mode - native manhua/manhwa/manga storyboard support
  - src/lib/comic/layout.ts: deterministic panel layout engine (shot-type weights → greedy page packing → rotating 6-col grid templates, splash pages for establishing shots, webtoon strip heights)
  - src/components/views/comic-panel-art.tsx: procedural SVG panel sketches per shot type (ridges/ECU eye/silhouettes/speed lines/rain hatching/pagoda), format-adaptive palettes, manga screentone pattern
  - src/components/views/comic-view.tsx: format selector (Manhua LTR / Manhwa webtoon vertical / Manga RTL), episode picker, stats (panels/pages/splash/read time), Print-PDF button, scene chapter cards
  - Wired StudioView "comic" + nav + print stylesheet in globals.css
  - README.md with architecture, feature map, screenshots (docs/screenshots/), run instructions
- Verified: tsc/eslint clean on new files; agent-browser check of all three formats rendering with correct reading direction + art; no console errors
- Pushed 8c11c45 (Comic Mode) and ccaa18a (README screenshots) to GitHub main

Stage Summary:
- GitHub: https://github.com/Omerhrr/AnimeOS main @ ccaa18a (3 commits)
- Platform now natively covers animation (donghua/anime pipeline) AND comics (manhua/manhwa/manga Comic Mode)
- Next horizons: image-generation-backed panel art, speech-bubble/dialogue authoring, live Blender bridge, batch rendering

---
Task ID: 3
Agent: Super Z (main agent)
Task: Iteration 3 - AI panel art generation + speech-bubble dialogue authoring (user: "proceed with both")

Work Log:
- Loaded fullstack-dev + image-generation skills; extended prisma Shot model (artworkUrl, dialogue JSON), db push + client regen, dev server restart
- Built POST /api/panel-art: z-ai SDK image generation, prompts from shot-type framing + scene environment/weather/lighting + style preset + characters' episode-resolved states (appearance/clothing/weapon/cultivation); saves public/panels/{shotId}.png, artworkUrl with cache-busting version
- PATCH /api/shots: dialogue validation (array, ≤8 lines, text required), artworkUrl set/clear
- Comic Mode UI: per-panel toolbar (generate art, edit dialogue, AI badge), batch "Generate art" with concurrency-2 pool + live counts, AI art layer with procedural sketch fallback, stats "AI art ready n/n"
- Speech bubbles (comic-bubbles.tsx + dialogue.ts): SPEECH (tailed) / THOUGHT (dashed + dots) / SFX (stroked italic), deterministic spot pool mirrored for manga RTL; DialogueEditor dialog with speaker autocomplete from cast + kind select + add/remove lines
- Genericized layout engine (PanelPlacement<T>/ComicPage<T>) so panels carry full ShotRow
- Verified E2E via agent-browser: single + batch generation → 6/6 panels AI-illustrated; dialogue saved via editor and bubble renders over art in manhua/manga(RTL)/webtoon; fixed a React border/borderStyle dev warning; console clean; tsc+eslint clean on changed files

Stage Summary:
- GitHub main @ 6aa1a62 (7 commits total); Comic Mode now produces AI-illustrated, dialogue-authored comic pages in all 3 formats
- Remaining horizons: Blender bridge, per-character model sheets for casting consistency, DSH tool access to art/dialogue, batch rendering

---
Task ID: 4
Agent: Super Z (main agent)
Task: Iteration 4 - per-character model sheets + DSH dialogue/art authoring tools + live Blender render bridge (user: "proceed with this")

Work Log:
- Schema: Character.modelSheetUrl/modelSheetPrompt (canonical visual anchor), RenderJob.driver; db push + client regen + dev server restart
- src/lib/ai/art.ts: shared art service - extracted panel-art from the route, added generateCharacterModelSheet (turnaround sheet prompt from appearance JSON + latest dev state + per-style tokens) storing the anchor prompt; generateShotPanelArt now re-injects each detected character's anchor ("match established character design") for cross-panel consistency; retry wrapper on SDK calls
- API: /api/panel-art thin wrapper; new POST /api/character-sheet; new GET /api/bridge (live driver status + env hint)
- Live Blender bridge: src/lib/bridge/blender.ts (status probe of ANIMEOS_BLENDER_HOST or auto-spawned local `blender -b -P animeos_bridge.py`, submit + progress polling, graceful offline); bridges/blender/animeos_bridge.py - self-contained bpy add-on (ThreadingHTTPServer on 127.0.0.1:8100, /status /progress /render /ping; maps AnimeOS scene params to bpy fog/world/sun/area lights, camera framing per shot type/lens, timer-chunked render so HTTP stays live, returns base64 frame); py_compile clean
- Render pipeline: createRenderJob submits to live Blender when reachable (driver=BLENDER) else simulator; tickRenderJob polls bridge progress/stage, pulls finished PNG into public/renders/, mid-job bridge loss degrades to simulator cleanly; verified fallback (SIM badge, job progressed, DSH inspection unchanged)
- DSH: 18 tools now - set_shot_dialogue (validated lines, replace/clear), generate_panel_art (format-aware), generate_model_sheet (anchor store); context enrichment (dialogueLines/art per shot, modelSheet per character); doctrine rewritten (director directs art/dialogue via tools, model-sheet-before-panel-art rule); maxDuration 300 on /api/dsh
- UI: Characters view (model-sheet thumbnails, ANCHOR badge, per-card ✦ generate, detail sheet image + canonical anchor display); Render view (Engine driver card LIVE BLENDER/SIMULATOR with attach instructions, per-job BLENDER/SIM badge); api-client typed additions
- Fixed two real bubble bugs found in browser E2E: (1) caption overpainting bubbles in small panels → top-heavy spot pool + z-20 bubbles + line-clamped captions; (2) manga RTL double-mirroring pushed bubbles off-panel → removed mirrorSpot (the left→right style switch already mirrors)
- Verified E2E: real Lin Yue model sheet generated (30s, donghua turnaround, anchor stored + served), live DSH turn autonomously called set_shot_dialogue + generate_panel_art (75s, thought bubble "This ends today." renders over anchor-consistent art in manhua + manga RTL), bridge card + fallback verified, console clean, tsc/eslint clean on changed files (tools.ts errors pre-exist on HEAD, confirmed via stash)

Stage Summary:
- Casting consistency mechanism shipped: model sheet → stored anchor → injected into every panel prompt
- DSH is now a full art/dialogue director (18 tools); Blender bridge is live-attachable with transparent simulator fallback
- GitHub main pushed with this iteration; next horizons: batch rendering, style LoRA per production, webtoon slice export

---
Task ID: 5
Agent: Super Z (main agent)
Task: Iteration 5 - batch rendering across episodes + webtoon slice export + per-production art style tuning (user: "proceed with batch rendering across episodes, webtoon slice export, and per-production style tuning for the art prompts")

Work Log:
- Schema: Project.artStylePrompt/artPalettePrompt/artNegativePrompt (nullable free-text style directives); db push + client regen + clean dev-server restart (found stale orphan `next dev` child holding :3000 after parent kill - killed tree, restarted, EADDRINUSE resolved)
- src/lib/ai/art.ts: productionStyleTokens()/productionNegativeTokens() compile the DB fields (custom directive overrides visualStyle preset tokens, palette appended, negatives appended to base tail); injected into BOTH panel-art and model-sheet prompts so the whole cast stays in one visual language
- API: PATCH /api/projects/[id] accepts the three fields (trim, 600-char cap, empty→null) + production event; POST /api/episodes hardened (400/400/404 guards - my own malformed test curl exposed a raw FK-violation 500)
- Style Direction dialog (style-direction-dialog.tsx) in Comic Mode toolbar: 3 fields + live "compiled into every prompt" preview (client mirror of the server compile), CUSTOM badge on the button, reset-fields, save → PATCH + query invalidate
- DSH: tool #19 set_art_style (styleDirective/paletteTokens/negativePrompt, empty string resets) + artStyleTuning block in get_production_context + doctrine rule 9 (style direction via tool, not restated per message); fixed the pre-existing `let x = null` inference errors in tools.ts (create_scene/create_shot/render_shot/check_capabilities) and stageFor in types.ts, plus dsh-console setProject destructure + studio-shell NAV icon type - src/ now fully tsc-clean
- Batch rendering: POST /api/render-jobs action "batch" { episodeIds[], mode } → one render job per non-FINAL shot across episodes, DRAFT episodes bumped IN_PRODUCTION, production event with counts; GET inspections capped at 2/tick (a 9-job batch completing at once must not turn one poll into 9 sequential LLM calls - remainder picked up on subsequent 2s polls)
- Render Queue UI: BatchRenderCard (episode chips with shot/FINAL counts, All-episodes toggle, PREVIEW/FINAL segmented mode, live result line) + aggregate stats row (active/in review/approved) + status filter (All/Rendering/Needs review/Approved)
- Webtoon slice export: src/lib/comic/export-slices.ts - client-side canvas compositor at 800px (panel art cover-cropped from AI artwork or live-DOM-serialized procedural SVG, border, shot chip, speech bubbles w/ tails mirrored from the DOM spot pool, SFX outline text, narration caption), boundary-aware packing into ≤1280px slices, jszip packaging with manifest.json (slice→panel mapping), auto-download; data-shot-id anchors added to panel figures; Export slices button (enabled only in MANHWA, progress states in-button)
- Verified E2E: style dialog save persisted (detail API returns all 3 fields); slice ZIP = EP07_slice_01.png 800×1280 + EP07_slice_02.png 800×606 + manifest, slice visually confirmed (AI art, bubbles w/ speaker labels + tails, captions, chips); batch "9 renders queued across 2 episodes" (E07+E08) → 21 jobs spanning both episodes, evaluations flowing under the cap; live DSH turn autonomously called set_art_style (moonlit silver-blue palette + "no warm gold" negatives written correctly); console clean, eslint clean, src tsc-clean
- README: feature map (19 tools, batch, style direction, slice export), new screenshot rows (batch-render.png, webtoon-slice-1.png), Try-the-loop steps 5-6, status paragraph

Stage Summary:
- GitHub main pushed with this iteration (5 commits + this one)
- Studio now: batch-renders whole episodes with throttled DSH inspection; exports webtoon platform slices (800px, boundary-aware, manifest) from the live strip; art prompts are per-production tunable from UI and by DSH itself
- Next horizons: per-shot style LoRA fine-tuning, multi-artist shot assignment, sound/SFX timing for motion panels

---
Task ID: 6
Agent: Super Z (main agent)
Task: Push Iteration 6 to GitHub + verify the three new features live (user: "you havent push the last work to github, push and proceed")

Work Log:
- Found the full Iteration 6 implementation sitting unpushed as a local commit with a UUID message (24 files, +2189 lines: loras/audio-cues/artists API routes, lora-studio/sound-timeline/artists/panel-inspector dialogs, comic/audio.ts Web Audio engine, schema StyleLora/Artist/AudioCue + Shot.loraId/loraStrength/artistId)
- Pre-push verification: bunx tsc --noEmit clean on src/ (only pre-existing examples/ + skills/ scaffold errors remain); eslint clean on all new/changed files
- Amended commit message to "Iteration 6: per-shot style LoRA fine-tuning, sound/SFX timing for motion panels, multi-artist shot assignment" and pushed cb10f83..5ee5e82 to origin/main
- Browser E2E verification of all three features:
  * LoRA studio: "Style LoRA registry" dialog lists 4 seeded adapters; registered new adapter "celestial-gold-v2" (name/base model/trigger tokens/strength slider) → badge LoRA 4→5, appears in registry + panel inspector
  * Sound timeline: shot 001 dialog with 3 seeded cues; "Auto-score from shot" derived 2 more (Storm rain bed AMBIENCE, Camera crane air swish SFX) + VOICE cue auto-created from dialogue; edited cue duration 5200 → server clamped to 4.2s timeline, persisted (verified in DB); preview button present (Web Audio synth)
  * Multi-artist: roster dialog lists 4 seeded artists; added "Han Zhao - Action sequences & FX"; panel inspector assigns artist + LoRA + weight per shot; saved shot 001 → Han Zhao + celestial-gold-v2 @ 0.85 (verified in DB via shot.artist/lora relations); toolbar artist filter chip "Han Zhao 1" updated live; bulk-assign chips present
- Console/error check clean throughout; removed temp verification screenshots

Stage Summary:
- GitHub main @ 5ee5e82 (6 commits total); Iteration 6 fully pushed and verified end-to-end
- Per-shot style LoRA binding (trigger tokens injected into panel-art prompts), motion-panel sound design with auto-scoring + Web Audio preview, multi-artist roster with per-shot ownership are all live
- Suggested next horizons: audio stems in webtoon slice export (manifest), DSH tools for artist/LoRA assignment, artist workload balance view

---
Task ID: 7
Agent: Super Z (main agent)
Task: Iteration 7 - DSH autonomous artist/LoRA staffing + audio stems in webtoon slice exports + artist workload-balance view (user: "proceed with DSH tools to autonomously assign artists/LoRas, audio stems embedded in webtoon slice exports, and an artist workload-balance view")

Work Log:
- DSH: new tool #21 auto_assign_scene_team - staffs an entire scene in one call: artist routing scores specialism (background/environment roles → ESTABLISHING/WIDE, character roles → CLOSEUP/ECU, FX roles → movement/energy-keyword shots) against a load penalty (0.5 × production-wide count) so work spreads; LoRA routing tokenizes name+trigger+notes (≥4 chars) and attaches the best content match (shot description + movement + lighting + scene environment) at the adapter's default weight; scope artists|lora|both, overwrite flag, guards for empty roster/registry, single production event per call; doctrine rule 11 rewritten (one-call scene staffing first, surgical set_shot_artist/set_shot_lora only for overrides) + intro updated
- Audio stems: src/lib/comic/stems.ts - OfflineAudioContext renderer (44.1kHz mono) with the live engine's synthesis DNA (bandpass-noise SFX, detuned-triangle BGM, wobbling-filter ambience; VOICE → syllabic-wobble formant blip since speechSynthesis can't render offline), 16-bit PCM WAV encoder; audio.ts exports labelHash/noiseBuffer (BaseAudioContext) so both engines share primitives; export-slices.ts re-times each slice's cues onto one stem timeline (panels back-to-back in reading order, per-cue offset by preceding panel durations, volume carried through), renders one WAV per scored slice (EPxx_slice_NN.wav) into the ZIP, manifest.audio gains stemFormat + per-stem {file, slice, durationMs, cueCount, cues[]} with panel mapping; SliceShot now carries duration, SliceAudioCue carries volume
- Workload view: src/components/views/artist-workload-dialog.tsx - toolbar button next to Artists; per-artist rows (episode panels vs production-wide total, colored bar, art/lora/in-flight/done mini-stats), production-wide balance badge (BALANCED ≤0.12 / UNEVEN ≤0.3 / SKEWED spread vs ideal per-artist share), unassigned pool row, "Distribute pool" one-click round-robin (least-production-load-first) using the existing bulk PATCH; ArtistsDialog blurb now points at the workload view + auto tool
- Fixed a self-inflicted file corruption in artist-workload-dialog.tsx (Write tool left a truncated/mangled line that surfaced as a client-side exception in Comic Mode) - rewrote the file cleanly and re-verified
- Verified E2E: live DSH turn "re-staff Scene 12..." → called auto_assign_scene_team {sceneNumber:12, scope:both, overwrite:true} → 6 shots re-routed (Jiang Wu 0→3, all roster members loaded), 4 LoRAs content-matched (azure-flame-fx, spirit-beast-form), DB verified; workload dialog on E08 showed 1/3 assigned UNEVEN → Distribute pool → "2 pool panels distributed across 2 artists ✓" → 3/3; webtoon export with capture-patched createObjectURL → ZIP holds EP07_slice_01/02.png + EP07_slice_01/02.wav (RIFF/WAVE magic), manifest audio: 17 cues (4 AMB/9 SFX/2 BGM/2 VOICE), stemFormat "WAV 16-bit PCM mono 44.1kHz", slice_01 stem 16000ms/12 cues with panel-relative re-timing; tsc src-clean, eslint clean
- README: 23-tool count, Multi-artist studio row, slice-export stem wording, status paragraph

Stage Summary:
- GitHub main pushed with this iteration (7 commits + this one)
- DSH now staffs scenes autonomously (specialism + load-balanced artists, content-matched LoRAs); exported webtoon slices carry real playable audio stems + authoring-tool manifests; creators get a live workload-balance board with one-click pool distribution
- Next horizons: real TTS/voice renders for VOICE stems, LoRA training-run simulation (dataset from approved panels), per-artist style preset affinity

---
Task ID: 8
Agent: Super Z (main agent)
Task: Iteration 8 - real TTS voice renders for stems, simulated LoRA training runs from approved panels, per-artist style affinity, and a repo-wide em dash purge (user: "proceed with: real TTS voice renders for stems, simulated LoRA training runs from approved panels, and per-artist style affinity, also, remove all em dashes, from everywhere, em dashes should not be generated at all")

Work Log:
- Schema: AudioCue gains voiceUrl/voiceActor/voiceSpeed/voiceDurationMs; StyleLora gains status/trainProgress/trainedPanels/trainedAt; new LoraTrainRun model (progress, step, totalSteps, panelCount, lossCurve, runLog); db push + client regen + dev server restart (bun run dev.sh failed via `bun`, must invoke with `bash`)
- Real TTS voice renders: POST /api/voice-renders calls z-ai SDK tts (7 voices, speed 0.5..2, 1024-char guard, "Speaker: line" prefix stripped), writes public/voices/{cueId}.wav, parses the WAV header for the true duration (first attempt read byteRate at the wrong chunk offset and returned 0; fixed to off+8+8), widens the cue slot up to the shot timeline limit; deterministic auto-cast maps a speaker hash to a default voice
- Stem integration: stems.ts gains decodeVoiceClips + a realVoice mixer (24kHz take resamples into the 44.1kHz render, gain-faded so a take never bleeds into the next panel), blip fallback retained; export-slices.ts re-times cues with voiceUrl, mixes decoded takes into each slice stem, manifest audio gains voiceTakes {rendered, takes[]}; comic-view mapping now carries the voice fields; live CuePlayer prefers the rendered take (HTMLAudioElement) over speechSynthesis
- Sound timeline UI: "Real voice render" block on VOICE cues (voice actor select, speed slider, render/re-render, take status line), "Render voices (n/m)" batch button with a 2-worker pool + auto-cast, cue blocks show a cyan dot when a take exists
- Simulated LoRA training: src/lib/ai/lora-train.ts - dataset = production's approved panels with generated art (bound-to-adapter count reported), totalSteps = clamp(600 + panels*50, 600, 2400), tick-on-poll (GET /api/lora-train advances RUNNING runs like the render queue), deterministic decaying loss curve + seeded jitter sampled at 48 points, six milestone log lines, completion sets the adapter READY with trainedPanels/trainedAt and emits production events; 409 guard against concurrent runs on one adapter
- LoRA studio UI: Train button per adapter (dataset-size tooltip, disabled while running), TRAINING badge, live step/progress bar, final-loss readout, loss-curve sparkline (SVG polyline), run-completed readout, TRAINED · N PANELS badge after completion
- Per-artist style affinity: artist-workload-dialog computes production-wide per-artist LoRA pairings (usage share boosted by the pairing's approval rate, normalized to the artist's strongest = 100), rendered as a "Style affinity" section under the balance bars with per-pairing bars, shot counts and average strengths
- Em dash purge: scripts/de-emdash.mjs replaced 287 dash chars (em/en/multichar) across 44 tracked files (src, prisma, scripts, bridges, README, worklog); scripts/de-emdash-db.ts cleaned 84 persisted DB values via raw REPLACE updates over a table/column whitelist; DSH system prompt rule 15 + evaluator rules now forbid em/en dashes in all generated text; verified zero dash chars remain (the only post-sweep match was the sweep script's own comment, fixed)
- Verified E2E: TTS take rendered via UI on shot 001 (jam 2.7s + douji 8.9s takes, DB voiceUrl/voiceDurationMs verified, slot clamped to timeline), batch "Render voices" on an E08 panel auto-cast douji for Lin Yue (1/1), LoRA train run 650 steps completed in UI (TRAINING badge + step ticker + milestones -> TRAINED · 1 PANELS, loss 0.042, events in DB), webtoon slice export re-run -> manifest voiceTakes rendered: 2 with actors/durations, stem RMS at the voice cue window 4x the ambience bed confirming real speech is mixed in, workload dialog shows affinity rows (Jiang Wu azure-flame-fx top 55, Su Qing 100, Dao Zhang spirit-beast-form 55); console clean; tsc src-clean; eslint clean
- README: Comic Mode + Multi-artist studio feature rows, Try-the-loop steps 6-7, status paragraph

Stage Summary:
- GitHub main pushed with this iteration (8 commits + this one)
- VOICE stems now carry real speech (rendered takes mixed into slice exports and the live preview), style adapters are trainable from approved panels with a full simulated run readout, and the roster board ranks which artist clicks with which style
- The repo (source, docs, worklog, database, DSH prompts) is em-dash-free and the prompts now enforce it for all future generated text

---
Task ID: 9
Agent: Super Z (main agent)
Task: Iteration 9 - voice takes per character state, batch-train all adapters, affinity-driven pool distribution (user: "proceed with voice takes per character state (excited/injured delivery), batch-train all adapters, and affinity-driven pool distribution where 'Distribute pool' prefers the highest-affinity artist per panel")

Work Log:
- Schema: AudioCue gains voiceState (delivery profile: NEUTRAL | EXCITED | INJURED) + voiceStateLabel (the character state the delivery resolved from); db push + client regen + dev restart
- Shared delivery module src/lib/comic/delivery.ts (client + server import the same catalog): three delivery profiles (Excited 1.18x quickened/bright, Injured 0.82x strained/trailing, Neutral), keyword classifier over state labels (battle-damaged/injured/blooded -> INJURED; breakthrough/triumphant/furious -> EXCITED), and light punctuation shaping (excited lands on "!", injured trails on "...", lines with terminal punctuation untouched)
- Voice takes per character state: POST /api/voice-renders accepts delivery AUTO | explicit profile; AUTO resolution loads the speaker's CharacterState rows effective at the shot's episode (same-episode TEMPORARY beats win over the latest PERMANENT progression), classifies the label, falls back to Character.canonicalState, else NEUTRAL; effective TTS speed = base speed x delivery multiplier, cue keeps the BASE speed (first implementation stored the multiplied speed, which compounded on every re-render, 1.18 -> 0.97 -> 0.79; fixed by storing base + deriving effective speed from voiceState, verified stable across two identical renders)
- Sound timeline UI: "Delivery (character state)" select (auto from character state / Neutral / Excited / Injured), resolved-delivery readout "delivery: injured (from "Battle-damaged (temple fight)") . speed x0.82 . last take", take line shows effective speed + state chip, batch "Render voices" now AUTO-renders per speaker state and reports non-neutral counts; offline voice blips in stems.ts follow the delivery pace too
- Batch-train all adapters: POST /api/lora-train {projectId, batch: true} starts simulated runs for every idle adapter (skips ones already TRAINING/RUNNING with reasons, 409 when nothing can start, take raised to 60 runs); LoRA studio gains a "Train all" button with dataset tooltip + batch result line ("Batch training: 5 runs started")
- Affinity-driven pool distribution: workload dialog builds the raw (non-normalized) artist x LoRA affinity matrix; "Distribute pool (affinity-first)" routes each LoRA-bound panel to the artist with the highest raw delivery score on that adapter (ties broken by lower production-wide load), unbound panels rotate across the least-loaded roster, result message splits the routing ("3 pool panels distributed across 3 artists (2 affinity-first, 1 load-balanced)")
- Manifest/stems plumbing: SliceAudioCue carries voiceState/voiceStateLabel, exported manifest voiceTakes gains delivery + stateLabel per take, stem cues carry the delivery for the offline blip
- Verified E2E: AUTO take on shot 001 resolved INJURED from Lin Yue's "Battle-damaged (temple fight)" (TEMPORARY @ ep7) at speed x0.82 (DB voiceState/voiceStateLabel verified), manual EXCITED override renders at x1.18; batch-train started 5 runs, all completed with TRAINED badges in the UI; pool test (unassigned ep7 shots 1/4/6) routed azure-flame-fx -> Su Qing and spirit-beast-form -> Dao Zhang affinity-first and the unbound shot load-balanced, DB confirmed; webtoon slice export ZIP re-verified: manifest voiceTakes carry delivery INJURED + stateLabel and NEUTRAL for the speakerless seeded cue, both stems RIFF-valid; console clean; tsc src-clean; eslint clean; zero em dashes repo-wide

Stage Summary:
- GitHub main pushed with this iteration (9 commits + this one)
- Voice takes are now performed, not just spoken: the speaker's episode-resolved character state drives delivery, speed and punctuation shaping through the live preview, the exported stems and the authoring manifest
- The whole adapter registry trains in one click and the pool distributor routes by demonstrated style affinity first, load second

---
Task ID: 10
Agent: Super Z (main agent)
Task: Iteration 10 - DSH state-aware voice direction tool (direct_voice_takes) + per-artist voice casting (cast_voice_actor + casting board) (user: "proceed with DSH tool for state-aware voice direction, and per-artist voice casting")

Work Log:
- Schema: Artist gains voiceId (the TTS voice the artist performs with); Character gains voiceArtistId (+ SetNull relation, deleting an artist clears their voice castings); AudioCue gains voiceDelivery (standing direction: null = state-aware auto per render, else a pinned NEUTRAL/EXCITED/INJURED), voiceNote (directorial note) and voiceCast (cast artist that performed the last take); db push + client regen + dev restart (first restart died because the detached server lost its parent; re-spawned via setsid bun run dev)
- Shared modules: src/lib/comic/voice-catalog.ts (client + server import the same 7-voice catalog, isVoiceId, voiceById, defaultVoiceFor so UI and API never drift); src/lib/ai/voice-casting.ts (resolveAutoDelivery moved here from the render route so the DSH tool resolves delivery identically, plus resolveVoiceCast: explicit voice > the character's cast artist voiceId > deterministic hash)
- Render API (/api/voice-renders): voice resolution is now cast-first (request override > Character.voiceArtist.voiceId > hash, artist name returned as cast source "cast"); delivery priority is request override > the cue's standing direction (source "direction") > episode-resolved character state; a pinned direction no longer attributes the old state label; takes persist voiceCast alongside voiceActor; response adds cast + direction blocks
- API plumbing: GET /api/audio-cues enriches VOICE cues with the resolved standing cast (artistName + voiceId) so the editor shows who performs the line before any take exists; PATCH /api/audio-cues/[id] accepts voiceDelivery (AUTO/null clears the pin) and voiceNote; PATCH /api/characters accepts voiceArtistId (validated against the roster, production event on cast/clear); PATCH /api/artists/[id] accepts voiceId (validated against the catalog); GET /api/projects/[id] includes characters.voiceArtist
- DSH: tool #24 direct_voice_takes - sets standing delivery + optional directorial note on every VOICE cue of a scene (or one shot); AUTO re-resolves each speaker's episode-effective state at call time for the summary (shot 001 Lin Yue -> injured from "Battle-damaged (temple fight)") but leaves the cue unpinned so future re-renders keep re-reading the state, explicit profiles pin every directed cue; tool #25 cast_voice_actor - binds a character to a roster artist (empty name clears), optionally (re)assigns the artist's TTS voice, auto-assigns a deterministic voice when the artist has none, roster/cast guards with helpful lists; doctrine rule 16 (direct a scene after scoring VOICE cues, cast principals once, re-render after changes) + intro updated; buildCompactContext now exposes artists.voice and characters.voiceActor so DSH sees the casting board
- Sound timeline UI: voice select defaults to last take > cast artist voice > hash; header line shows the cast ("cast: Su Qing · douji") before a take and "take: douji · cast Su Qing · 6.2s · x1.18 · excited" after; the delivery select is now a standing direction (changing it pins the cue, AUTO unpins) plus a Direction note field persisted on the cue; batch "Render voices" passes no explicit voice so the server casts from the roster and reports "N from cast artists"; batch render merges server rows so the resolved cast is preserved
- Artists dialog: roster rows gain a per-artist voice select (voice rides takes, stems, manifest); new "Voice casting" board binds each character to the roster artist who speaks for them (cast counter, "uncast · default voice" escape, DSH cross-reference note)
- Manifest/stems plumbing: SliceAudioCue carries voiceDelivery/voiceNote/voiceCast; exported manifest voiceTakes gains cast/direction/note per take and cuesByShot entries carry all three
- README: 25-tool count, DSH Director row gains voice direction + casting, Comic Mode row reworded for per-artist casting + standing direction, Multi-artist row gains voice casting, try-the-loop step 7 (Artists voice casting + DSH cross-reference)
- Verified E2E: Artists board set Su Qing -> douji and cast her as Lin Yue's voice (DB: Lin Yue -> Su Qing (douji), Su Qing.voiceId douji); sound timeline showed the cast line, pinned EXCITED standing direction + note "clenched teeth, wind-blasted" (DB verified), then POST /api/voice-renders with no voice/delivery returned delivery source "direction" EXCITED @x1.18 and cast Su Qing/douji, take persisted voiceActor douji + voiceCast Su Qing; live DSH turn ran both tools in one request (direct_voice_takes on Scene 12: 1 neutral + 1 injured from character state with note "wind-torn, strained"; cast_voice_actor: Demon Lord Wei -> Han Zhao with voice luodo, DB + production events verified); webtoon slice exports re-run on E07 + E08, manifest voiceTakes carry voiceCast "Su Qing" + delivery EXCITED + direction/note, uncast take stays voiceCast null, stems RIFF-valid; console clean; tsc src-clean; eslint clean; zero em/en dashes repo-wide

Stage Summary:
- GitHub main pushed with this iteration (10 commits + this one)
- Voice takes are now directed and cast: DSH sets standing deliveries per character state scene-wide, and every character can be bound to a roster artist whose TTS voice performs their lines across the live preview, batch renders and exported stems/manifest
- Next horizons: per-episode voice direction diffs (state changes over time re-render only affected takes), dialogue-line-level delivery (multi-state lines inside one shot), voice audition preview inside the casting board

---
Task ID: 11
Agent: Super Z (main agent)
Task: Iteration 11 - per-episode direction diffs with selective re-render, line-level delivery inside a single shot, audition preview in the casting board (user: "proceed with per-episode direction diffs that re-render only affected takes, line-level delivery inside a single shot, and an audition preview in the casting board")

Work Log:
- Schema: AudioCue gains voiceSig (JSON take-input snapshot stamped at render time: hashed speakable text + TTS voice id + delivery profile + base speed); db push + client regen (first re-render attempt failed with "Unknown argument voiceSig" because the running dev server still held the pre-schema Prisma client; restarted the server and the same POST then succeeded - all later verification ran on the fresh client)
- src/lib/comic/dialogue.ts: DialogueLine gains delivery (NEUTRAL/EXCITED/INJURED or null = state-aware auto); parse/serialize round-trip it; new dialogueDeliveryForCue matches a VOICE cue's "Speaker: text" label back to its dialogue line and returns that line's explicit delivery
- src/lib/ai/voice-plan.ts (new, shared by render + diff): splitCueLabel, hashText (FNV-1a base36), buildTakeSig/parseTakeSig/sigChanges, resolveTakePlan - one resolver for WHO speaks (cast: request > Character.voiceArtist > hash) and HOW the line is played (delivery chain: request override > dialogue-line delivery > cue standing direction > episode-resolved character state, "line" is a new source), plus effective speed and the signature; ResolvedDelivery.source widened to include "line"
- src/lib/ai/voice-render.ts (new): renderVoiceTake extracted from the route (cue load, plan, ZAI TTS, public/voices/{cueId}.wav, cue update incl. voiceSig stamp), VoiceRenderError with status codes, wavDurationMs exported for reuse; /api/voice-renders POST is now a thin wrapper (GET voices list unchanged); response direction block adds lineDelivery
- /api/voice-diffs (new): GET ?episodeId|projectId re-resolves every VOICE cue's would-be plan and diffs against the stored snapshot -> fresh / stale (changed: line text, voice / casting, delivery direction, base speed, or "predates direction snapshots") / unrendered / blocked, with per-cue now-vs-taken readouts; POST {episodeId} re-renders ONLY the stale takes (concurrency 2, unrendered cues left to the sound timeline batch), tally + ProductionEvent recorded
- /api/voice-auditions (new): throwaway casting-board audition - validates the voice id, picks the text (explicit board line > the character's own first dialogue line anywhere in the production, via parseDialogue over the project's shots > classic sample by voice hash), renders TTS in the chosen register and returns base64 WAV + metadata; zero DB writes, zero files
- api-client: AudioCueRow.voiceSig, VoiceDiffRow/VoiceDiffEpisode/AuditionResult types, voiceDiff/reRenderStaleVoices/auditionVoice methods
- Dialogue editor (comic-bubbles.tsx): every dialogue line gains a Delivery select (Auto · state-aware / the three profiles); parseSafe preserves it; description mentions per-line direction
- VoiceDiffDialog (new component) wired into the Comic Mode toolbar next to Workload: episode-scoped board with count chips (total/fresh/stale/no take), per-cue rows (shot number, status chip, speaker, text, "moved:" reasons + was-snapshot, "now:" chain readout incl. source and cast artist), "Re-render affected (N)" disabled at 0 stale, result line; button shows an amber stale badge for the selected episode
- Casting board auditions (artists-dialog.tsx): board-level register select (default Excited) + optional audition line input; play button on every roster row (artist's voice or their hash default) and on every character casting row (cast artist's voice, empty board line auditions the character's OWN first line via the speaker lookup); one shared HTMLAudioElement with stop button + playing/busy states; readout like "audition: douji · excited · custom · 2.8s · not saved as a take"; clip stopped on dialog close
- Sound timeline: VOICE cue editor shows a "line direction: X · authored on this dialogue line, it outranks the standing direction" readout when the matching line carries a delivery
- README: Comic Mode row (line-level delivery precedence + direction diff board), Multi-artist row (auditions before casting), try-the-loop steps 6-8 rewritten (line delivery, audition flow, diff re-render), status paragraph
- Verified E2E (API): first diff marked all 3 existing takes stale ("predates direction snapshots"); PATCH shot 001's dialogue with delivery EXCITED flipped only that cue's resolution to EXCITED source "line"; POST re-rendered exactly the 2 stale E7 takes (DB: voiceSig stamped, shot 001 voiceState EXCITED); flipping the line to INJURED re-flagged only shot 001 ("delivery direction"), re-render touched 1 of 2 takes (shot 003 stayed fresh), DB voiceState INJURED, effective x0.82, production event "Direction diff re-rendered 1 of 1 stale take(s)... (1 delivery direction)"; project-wide diff shows E7 2 fresh / 0 stale while untouched E8 remains 1 stale (selectivity proven); audition API returned a 430KB RIFF WAV for douji reading Lin Yue's own line INJURED (source "character line") with take count unchanged before/after, custom-line and 400-on-bad-voice paths checked
- Verified E2E (agent-browser): Direction diff board on E07 renders chips (2 fresh / 0 stale / 0 no take) with "now: injured (line delivery) · x0.82 · douji · cast Su Qing" and disabled re-render; E08 board shows the stale row "moved: predates direction snapshots"; Artists board auditioned douji (sample, 3.0s) then a custom line on Lin Yue's casting row ("custom · 2.8s"), readouts rendered; dialogue editor shows the line's persisted "Injured" delivery; sound timeline VOICE cue shows the line-direction readout; console + page errors clean; screenshots saved (direction diff board, casting auditions, line delivery)
- Ops note: the dev server had to be restarted to pick up the regenerated Prisma client; a plain setsid spawn died between tool calls, the reliable pattern was `(setsid bash -c 'exec bun run dev' >> /tmp/devserver.log 2>&1 &)`

Stage Summary:
- GitHub main pushed with this iteration (11 commits + this one)
- Voice direction is now diffable and line-scoped: every take carries a snapshot of the inputs it was made with, each dialogue line can be directed in its own register inside one shot, and a per-episode board re-renders only the takes whose direction actually moved
- Casting decisions can be heard before they are made: the board auditions any roster voice on a custom line or the character's own dialogue without persisting anything

---
Task ID: 12
Agent: Super Z (main agent)
Task: Iteration 12 - DSH tools for the direction diff (diff_episode_direction) and batch diffing across all episodes (diff_all_episodes), plus a season-wide batch mode in the diff board (user: "proceed with DSH tools for the diff (diff_episode_direction), and batch diffing across all episodes")

Work Log:
- src/lib/ai/voice-diff.ts (new): the direction-diff core extracted from the API route so the route and the DSH brain share one implementation - diffEpisode/diffEpisodeById/diffProjectEpisodes (fresh / stale / unrendered / blocked classification with changed-field labels), resolveStale, renderStaleBatch (2-worker pool), reRenderStaleTakes (single episode, actor-parameterized ProductionEvent), reRenderStaleAcrossProject (every episode of the production, budget-capped, default 16 / max 32 per call, returns per-episode reRendered/failed/staleCount + remaining for follow-up calls)
- /api/voice-diffs refactored onto the shared core: GET unchanged behavior (episodeId = one episode, projectId = batch across all episodes); POST now accepts {projectId, limit?} for the season-wide capped batch re-render alongside the original {episodeId}; single-episode response gains staleCount/remaining
- DSH tools (src/lib/dsh/tools.ts): diff_episode_direction (episodeNumber defaults to latest; reports per-cue stale lines like "shot 001 \"Lin Yue\": moved delivery direction (was neutral x1.00 / douji; now excited / douji)"; reRender:true re-renders only the stale takes; wrong episode number lists available episodes) and diff_all_episodes (per-episode tallies + overall counts; reRender:true batch re-renders across the season capped by limit, reports remaining takes; failures listed per episode); TOOL_DEFS docs written so the LLM knows when to reach for each
- DSH doctrine (prompts.ts): intro line now mentions the diff tools; new rule 17 "Direction hygiene" - after dialogue, standing direction, casting or character-state changes, diff the touched episode (or all episodes for a sweep) and re-render only what moved; never assume takes already reflect a direction change
- api-client: voiceDiffAll(projectId), reRenderStaleVoicesAll(projectId), reRenderStaleVoices gains staleCount/remaining
- VoiceDiffDialog: "This episode" / "All episodes" mode toggle; batch mode shows aggregate chips (episodes, cues, fresh, stale, no take), per-episode cards with EP badge, counts and a "moved: Speaker (field + field)" line for stale episodes, and one "Re-render stale, all episodes (N)" button that batch re-renders (capped server-side) and refreshes to the post-render state; comic-view passes projectId
- Verified E2E (tool layer, Immortal Path): diff_episode_direction Ep7 = 2 fresh; diff_all_episodes = Ep07 2 fresh / Ep08 1 stale ("predates direction snapshots"); USER batch POST via API re-rendered exactly the 1 stale Ep8 take (real TTS, Ep7 untouched), summary "Batch re-rendered 1 of 1 stale take(s) (Ep08 1/1)"; direct_voice_takes pinned EXCITED -> diff_episode_direction Ep8 reRender:true flagged "moved delivery direction (was neutral / douji; now excited / douji)" and re-rendered only that take (1.6s), follow-up diff all fresh; error paths (episodeNumber 99 lists "Episodes: 7, 8"; DSH batch re-render with nothing stale is a clean no-op)
- Verified E2E (agent-browser): dialog opens in episode mode, "All episodes" renders the 2 episode cards with aggregate chips; after pinning NEUTRAL standing the board shows "1 stale" + "moved: Lin Yue (delivery direction)"; clicking the batch re-render ran real TTS, result line "Batch re-rendered 1 of 1 stale take(s) (Ep08 1/1)", cards back to all fresh, button disabled at 0; screenshots saved (diff-batch-ui.png, dsh-diff-trace.png)
- Verified E2E (LLM orchestration): fresh-context probe confirmed the model selects diff_all_episodes with reRender:true unprompted; live console turn "Pin an excited delivery with note ... on every voice cue in scene 1, then diff all episodes and re-render only the takes the diff marks stale" executed direct_voice_takes then diff_all_episodes in one turn (trace 2 tools OK) and re-rendered the 1 stale take via real TTS; two earlier turns had the model break the JSON protocol and answer in prose (empty trace) - pre-existing orchestrator reliability characteristic under long histories, not tool-specific; note: conversation history with prior prose claims can bias the model away from tool calls
- Ops: dev server restart pattern from iteration 11 reused; worklog + code scanned for em dashes (none)

Stage Summary:
- GitHub main pushed with this iteration (12 commits + this one)
- The DSH brain can now reason about voice-take currency: it asks for a diff per episode or across the whole season, sees exactly which takes are stale and why, and re-renders only those takes (single episode or budget-capped season batch), with the same diff available to creators in the board's new "All episodes" mode
- ProductionEvents record both DSH and USER diff re-renders, so the History view shows direction maintenance alongside direction changes
- Next horizons: voice takes per character state variants beyond delivery (whisper/shout registers), diff-aware DSH auto-prompting after casting changes, stem/manifest tagging of stale vs fresh takes in webtoon exports

---
Task ID: 13
Agent: Super Z (main agent)
Task: Iteration 13 - per-state voice variants beyond delivery registers, and direction-currency (fresh vs stale) tagging of voice stems/manifests in webtoon slice exports (user: "proceed with per-state voice variants beyond delivery registers, and tagging stems/manifests as stale vs fresh in webtoon exports")

Work Log:
- Schema: CharacterState.voiceVariant (nullable TTS voice id); db push + client regen
- src/lib/ai/voice-casting.ts: new resolveStatePerformance - ONE pass over the speaker's states resolves both the delivery (first classifiable state, TEMPORARY beats first, then canonical fallback) and the voice variant (first candidate state carrying a voiceVariant); resolveAutoDelivery now delegates to it so every consumer shares one lookup; candidateStates helper extracted
- src/lib/ai/voice-plan.ts: TakePlan gains variant + effective voiceId (explicit request override wins over the variant; otherwise the variant outranks the cast voice while its state is episode-effective); the take signature hashes the EFFECTIVE voice, so binding/clearing a variant automatically flags rendered takes stale
- src/lib/ai/voice-render.ts: TTS renders with plan.voiceId (variant when active); cue stamps voiceActor = effective voice, voiceCast keeps the cast artist, voiceStateLabel gains a "(voice variant)" suffix when a variant performed the take; RenderTakeResult.cast carries the variant block
- src/lib/ai/voice-diff.ts: diff rows expose the effective voice + variant (voiceId/stateLabel), so the diff reports variant-driven staleness with the variant's source state
- DSH tool set_state_voice_variant (23rd tool): bind/clear a variant voice on a character state by (fuzzy) state label, defaulting to the latest episode-resolved state; validates the voice id against the catalog, logs a ProductionEvent, and the result text tells DSH to follow up with diff_episode_direction/diff_all_episodes reRender:true; buildCompactContext now lists voiceVariant per state so the LLM sees current variants
- /api/character-states PATCH (new route): creator-side variant editing (validate voice id, clear with empty string, ProductionEvent); api-client patchCharacterState; the character sheet's development-state cards gained a "Variant voice" select (Auto (cast voice) + the 7 catalog voices)
- DSH doctrine: intro tool line mentions set_state_voice_variant; rule 17 covers variants as a staleness trigger
- Webtoon export currency tagging: export-slices.ts gains SliceExportOptions.voiceStatus (cueId -> {status, changed} from the per-episode diff); manifest building extracted into a pure exported buildSliceManifest; manifest now carries audio.direction (fresh/stale/unrendered/blocked/unknown counts + current true/false/null + an actionable note), per-take directionStatus + changed, per-stem voiceCurrency tallies, and cueId/directionStatus/changed on every cue in cuesByShot; StemCue carries cueId; comic-view's runSliceExport fetches the diff first ("Checking direction currency…"), maps cueId -> status, and the completion line reports "all takes fresh" or "manifest flags N stale take(s)"
- Data incident: the dev DB was found reseeded this session (old cue/render state gone; universe shape intact via ensureSeed) - root cause not reproducible; a canary row survived both a prisma db push and a dev-server restart, so the current pipeline is stable; stale process hygiene identified as the likely source: pkill -f "bun run dev" does NOT kill the next-server child it spawned, so a pre-iteration server can keep serving port 3000 with old code (this also explained a render returning NEUTRAL/no-variant mid-session); fix: kill the next-server PID (or pkill -f next-server) before restarting
- Verified E2E (tools, fresh Immortal Path seed): set_shot_dialogue + add_audio_cue + cast_voice_actor (Su Qing/xiaochen) + render -> baseline take xiaochen/NEUTRAL; resolveStatePerformance('Lin Yue', 7) = INJURED + variant; set_state_voice_variant kazi -> diff flags "moved voice / casting (was injured x1.00 / kazi; now injured / xiaochen)" after clearing and "...(variant from \"Battle-damaged (temple fight)\")" after re-binding; re-render touched exactly the 1 speaker-carrying take (the speaker-less narration cue untouched), DB take = jam/INJURED with voiceCast Su Qing and voiceStateLabel "Battle-damaged (temple fight) (voice variant)"; error paths: bad voice id rejected, unknown state label lists states, wrong character lists cast
- Verified E2E (/api/character-states PATCH): clear/set/reject-bogus-voice
- Verified (manifest builder, scripts/check-manifest.ts): tallies with mixed statuses + unknown cues, current false with a stale take, stem-level currency excludes non-VOICE cues (fixed a total-count bug the check caught), untagged export -> current null, all-fresh -> current true
- Verified E2E (agent-browser, webtoon Comic Mode): Export slices with fresh takes shows "2 slices downloaded ✓ - all takes fresh"; after direct_voice_takes pinned EXCITED (making the take stale) the export fetched /api/voice-diffs (network log 200) and the button read "2 slices downloaded ✓ - manifest flags 1 stale take"; console clean; screenshot saved (export-currency.png); post-export diff reRender:true restored all-fresh (1 fresh + 1 unrendered)
- Ops: the reliable dev-server restart is now: pkill -f next-server; pkill -f "next dev"; then (setsid bash -c 'exec bun run dev' >> /tmp/devserver.log 2>&1 &)

Stage Summary:
- GitHub main pushed with this iteration (13 commits + this one)
- Character states are now a first-class VOICE axis: a state can swap the actual voice (possession/transformation/clone registers are castable), takes stamped with the effective voice flow into the direction diff, and DSH can bind/clear variants autonomously and then re-render only the affected takes
- Webtoon slice exports are direction-aware: every stem and take in manifest.json is tagged fresh/stale/unrendered/blocked vs the current direction, so downstream motion-comic tooling can tell which speech needs a re-render before a final master
- Next horizons: audition a state variant from the casting board (hear the possessed voice before binding), per-state variant speed/pitch hints, DSH auto-diff after set_state_voice_variant in the same turn
---
Task ID: 14
Agent: Super Z (main agent)
Task: Iteration 14 - fix the stale dev-server process hygiene issue, audition a state variant from the casting board, and per-state variant speed/pitch hints (user: "fix the stale issue, and proceed with auditioning a state variant from the casting board, and variant speed/pitch hints")

Work Log:
- Stale-process fix (the "stale issue"): scripts/dev-restart.sh is now the canonical restart path - kills the whole previous tree (next-server, next dev, next-worker), waits for port 3000 to actually free, refuses to start a second server if the port never frees, starts a fresh `bun run dev` in its own session and waits until the server answers before returning; package.json gains a predev guard that kills orphaned next-server/next dev children before every `bun run dev`, so a stale server can never keep serving port 3000 with old code (the Iteration 13 data-incident vector) and a new server can never silently bump to 3001 while E2E hits the stale one; both use the `[n]ext` bracket regex trick - the first cut without it self-matched its own pkill command line and killed its parent shell (caught on first run, fixed, verified)
- Schema: CharacterState.speedHint Float? and pitchHint Float? (speed = multiplier on the take's base speed, 0.5-2.0; pitch = playback pitch factor, <1 deeper, >1 higher); db push + client regen
- src/lib/ai/wav-dsp.ts (new): PCM16 WAV playback-rate shift (y[n] = x[n*factor], linear interpolation, frame-aware, canonical header rewrite) plus ttsSpeedAndPitchFactor (render at target/pitch, shift playback by pitch, so the take lands on the requested pace with the bend); unsupported formats pass through untouched; scripts/check-pitch-dsp.ts unit-checks duration ratios (x1.25 at 0.8, x0.8 at 1.25, x0.5 octave decimation sample-exact), header integrity and compensation math - all pass
- src/lib/ai/voice-casting.ts: ResolvedHints (stateLabel + speed + pitch); resolveStatePerformance now resolves a fourth axis - the first candidate state carrying any hint supplies the performance bend, independent of delivery and variant like the other axes; hint values clamped on read
- src/lib/ai/voice-plan.ts: TakeSig gains sh (state speed hint) and p (pitch factor); parseTakeSig defaults missing sh/p to 1 so takes stamped before this iteration only diff once a hint is actually active; sigChanges flags "speedHint"/"pitch"; TakePlan gains hints + pitch; effective speed = base x delivery multiplier x state hint (base stays the user's, so re-renders never compound)
- src/lib/ai/voice-render.ts: renders TTS at the compensated speed then shifts playback; the stored sig carries sh/p; RenderTakeResult reports the hints block + effective pitch
- src/lib/ai/voice-diff.ts: CHANGED_LABELS gains "state speed hint" and "pitch hint"; diff rows expose hints + pitch, so the diff board, DSH tools and webtoon manifests all label hint-driven staleness (manifest currency tagging inherits the new labels with zero changes)
- /api/voice-auditions: accepts stateId - loads the state, auditions its variant voice (or, when none is bound, the character's current cast/default voice), applies delivery register x speed hint and the pitch DSP; response carries variant {stateId, stateLabel, episodeNumber, variantVoiceId, speedHint, pitchHint} + effective pitch; fixed a TDZ crash (projectId referenced before declaration) found by the E2E
- /api/character-states PATCH: accepts speedHint/pitchHint (null clears, numbers clamp 0.5-2.0, non-numerics rejected), ProductionEvent summary lists what changed; api-client patchCharacterState now takes a patch object
- Casting board (artists-dialog.tsx): every character with states gains a "state try" row - pick a development state (option labels show variant voice, speed hint, pitch hint, episode) and audition it on the board's line or the character's own first line; the readout names the state, voice (variant or cast), effective speed, pitch factor, episode, duration and "not saved as a take"; clip playback extracted into playClip shared by voice and state auditions
- Character sheet (characters-view.tsx): state cards gain Speed hint (Auto/x0.8-x1.2) and Pitch hint (Natural/deep x0.75-0.9/high x1.1-1.3) selects next to the variant voice picker
- DSH: set_state_voice_variant gains optional speedHint/pitchHint args (null clears, omit leaves unchanged, numbers clamp, non-numerics rejected, no-op with no fields rejected); result text states the new performance and tells DSH to diff + reRender; description rewritten to "state voice performance" (variant voice AND how it performs); buildCompactContext lists speedHint/pitchHint per state; doctrine rule 17 now counts variant voices and speed/pitch hints as staleness triggers
- README: 25-tool -> 28-tool (count had drifted), state speed/pitch hints in the Comic Mode delivery chain, state auditions + hints in the Multi-artist row and try-the-loop step 7, status paragraph
- Verified E2E (DSP unit, scripts/check-pitch-dsp.ts): 9/9 checks (duration ratios, header integrity, octave decimation, compensation math, clamps)
- Verified E2E (API + tools, scripts/e2e-state-hints.ts, real TTS): DSH sets hints x0.85/x0.8 -> diff flags exactly ["state speed hint","pitch hint"] -> batch re-render re-renders exactly 1 take, sig stamped {"s":1,"sh":0.85,"p":0.8} (base stays user's) -> diff fresh; state audition returns a 215KB WAV with variant block (jam, 0.85, 0.8, pitch 0.8, EXCITED x hint = x1.00) and stores nothing (take count unchanged); hint-free state audition falls back to the cast voice (xiaochen) at pitch 1; custom-speaker audition works; clear step: hints cleared -> stale again -> re-render -> sig back to sh=1/p=1; error paths: non-numeric speedHint rejected (API + DSH), wild pitchHint clamps to 2, unknown state 404, explicit bad voice 400, DSH no-op rejected
- Verified E2E (agent-browser): casting board state-try row lists "Battle-damaged (temple fight) · jam · x0.85 · pitch 0.8 @Ep7", play disabled until a state is picked, audition readout "state audition: \"Battle-damaged (temple fight)\" · jam (variant voice) · x1.00 · pitch x0.80 · @Ep7 · Lin Yue's line · 3.4s · not saved as a take"; character sheet shows Variant voice / Speed hint / Pitch hint on every state card with persisted values (jam, x0.85, deep x0.8); changing the sheet's speed hint to x0.9 flips the diff to stale with only ["state speed hint"] (pitch untouched - precise labels), restoring x0.85 returns fresh; DSH live turn "Set Lin Yue's battle-damaged state to speed hint 0.9 and pitch hint 0.8, then diff episode 7 and re-render whatever moved" executed set_state_voice_variant then diff_episode_direction reRender:true in one turn (2 tools, trace clean) re-rendering the 1 stale take; console + page errors clean; screenshots saved (state-audition-board.png, state-hints-sheet.png, dsh-hints-trace.png)
- Ops: dev-restart.sh dogfooded for the server restart (single command, waits for readiness); DB incident from Iteration 13 not reproducible, but the orphaned-server window that most plausibly caused it is now closed structurally

Stage Summary:
- GitHub main pushed with this iteration (14 commits + this one)
- Character states now direct the whole performance: WHO speaks (variant voice), HOW the read lands (delivery register) and HOW the variant performs (speed + pitch hints), all resolved in one pass, all stamped into the take signature, all diffable and re-renderable per take; the casting board can hear any of it before anything is bound
- A state audition is a try-before-bind for the entire state performance, and the manifest's direction-currency labels carry the new hint axes for free
- Next horizons: DSH auto-audition proposal when binding a variant (render the audition inside the same turn for comparison), per-line state overrides in the dialogue editor, webtoon export preview that lists stale stems before download
---
Task ID: 15
Agent: Super Z (main agent)
Task: Iteration 15 - DSH proposes an audition inside the same turn when binding a variant, and per-line state overrides in the dialogue editor (user: "proceed with DSH proposing an audition inside the same turn when binding a variant, and per-line state overrides in the dialogue editor")

Work Log:
- src/lib/ai/audition.ts (new): audition core shared by the casting-board API and DSH - renderAudition picks the line (explicit > the character's own first dialogue line > a sample read), renders TTS at compensated speed and shifts playback for the pitch bend; renderVariantAudition renders a state's NEW performance right after a bind, saves the WAV under public/auditions/variant-{stateId}.wav and returns an AuditionPreview, returning null on failure so the bind stays successful; /api/voice-auditions refactored onto renderAudition with identical response shape
- types.ts: AuditionPreview (url, mimeType, durationMs, text, source, voiceId, deliveryId, speed, pitch, stateLabel, characterName) + TraceAction.audition (optional, persisted inside the trace JSON)
- DSH set_state_voice_variant: after saving the bind it renders the audition of the new performance (voice = the new variant or the character's cast/default when none; delivery = the state's classified register; hints = the new effective values) and attaches it to the ActionResult; the result text explicitly proposes playing the preview before re-rendering (or points at the casting board when the render failed); tool description tells DSH to point the creator at it in the reply the same turn
- orchestrator.ts: copies outcome.audition onto the trace action, so the preview rides the persisted DshMessage trace
- dsh-console.tsx: trace actions carrying an audition render an emerald "Audition - <character> \"<state>\"" card with an inline audio player plus the readout (quoted line, voiceId, pace/pitch factors, duration, source)
- prompts.ts doctrine: new rule 18 "Variant auditions" (point the creator to the attached audition in the same turn and let them veto before any re-render); rule 7 now documents per-line delivery + state direction
- Per-line state overrides: DialogueLine.state (string label fragment, null = auto); parseDialogue/serializeDialogue carry it; dialogueLineForCue extracted so dialogueDeliveryForCue and the new dialogueStateForCue share the cue-to-line matching
- voice-casting.ts: resolveStatePerformance gains stateOverride - matchStateOverride picks the best label match (exact > prefix > contains, case-insensitive) across ALL of the speaker's states, deliberately ignoring episode eligibility (a forced beat is a directorial decision); when nothing matches it falls back to auto resolution
- voice-plan.ts: resolveTakePlan reads the line's state override from the shot dialogue and passes it into the performance lookup; TakePlan.stateOverride records it; no sig schema change needed - a forced state moves the effective voice (v), delivery (d) or hints (sh/p), so stale detection inherits it, and forcing a state identical to the auto one correctly stays fresh
- voice-render.ts: RenderTakeResult.stateOverride; voiceStateLabel gains a "line override" attribution ("Battle-damaged (temple fight) (voice variant) - line override")
- voice-diff.ts: diff rows expose current.stateOverride; the diff board marks "(line override)" next to the source attribution; api-client VoiceDiffRow synced
- DSH set_shot_dialogue: lines pass through delivery and state (previously the tool silently dropped line delivery), so DSH can author per-line direction too
- Dialogue editor (comic-bubbles.tsx): every line gains a State select under Delivery ("Auto · episode-resolved" + the speaker's states, option labels showing episode and bound variant voice); a stored override stays selectable even after a speaker edit; changing the speaker resets the override; parseSafe preserves state; dialog copy updated
- comic-view.tsx: builds the speakerStates map (lowercase name -> states with label/episode/variantVoice) from the project characters and feeds the editor
- Verified E2E (scripts/e2e-line-overrides.ts, three steps, all PASS):
  - plan (no TTS): auto plan snapshot; forcing "Foundation Established" (@Ep15, not episode-effective for the Ep7 cue) clears the variant, falls back to the cast voice (xiaochen), drops the pitch hint and moves the sig; fuzzy override "battle" matches "Battle-damaged (temple fight)" (contains, case-insensitive) and restores the exact auto sig; restoring the dialogue restores the plan
  - bind (real TTS, 1.7s): set_state_voice_variant kazi/x0.9/x0.75 attaches an audition of Lin Yue's own line performed by kazi at pitch 0.75 (INJURED register classified from the state label, effective x0.74), WAV 6.0s saved to public/auditions/; result text carries the proposal; diff flags exactly "voice / casting, state speed hint, pitch hint"; restoring the prior performance (jam/x0.85/x0.8) returns the diff to all-fresh without a re-render
  - dialogue: set_shot_dialogue persists per-line delivery+state, the forced state drives the plan (variant jam) while the line delivery drives the register (line/EXCITED); original dialogue restored
- Verified E2E (agent-browser): DSH console renders the audition card with a playable player (media request 206, no page errors) - verified via an injected demo DshMessage (scripts/inject-audition-trace.ts, removed after); Comic Mode dialogue editor lists all four of Lin Yue's states in the State select with episode and variant markers ("Battle-damaged (temple fight) · Ep7 · jam"), selecting + saving persists {"state":"Battle-damaged (temple fight)"} in the shot dialogue, resetting to Auto restores the original line; console clean; screenshots saved (iter15-dsh-audition-trace.png, iter15-dialogue-state-override.png)
- Post-verification state: battle-damaged state back to jam/x0.85/x0.8, shot dialogue back to auto, injected message deleted, diff 1 fresh / 0 stale / 1 unrendered (the speaker-less narration cue, untouched by design)
- Note: DSH-side behavioral guard for the audition proposal is doctrine (rule 18) rather than an orchestrator enforcement; the tool result text carries the proposal either way, so the proposal survives even when the model ignores the rule

Stage Summary:
- GitHub main pushed with this iteration (15 commits + this one)
- Binding a state variant is now a try-before-commit loop inside one DSH turn: the bind result carries a playable audition of the NEW performance rendered on the character's own first line, the trace persists it, and the console plays it inline
- Dialogue lines are full-performance directives: a line can pin a register AND force one of the speaker's states (variant voice + hints + state-classified delivery) regardless of episode timing, from the dialogue editor or from DSH, with the direction diff tracking it through the existing sig axes
- Next horizons: webtoon export preview listing stale stems before download, DSH-side A/B audition (current take vs proposed variant in one card), multi-line state arcs (a state override that spans a shot range instead of a single line)

---
Task ID: 16
Agent: Super Z (main agent)
Task: Iteration 16 - A/B audition pairs (current stored take vs proposed performance) in DSH binds and the casting board, plus a webtoon export pre-flight that flags stale/blocked stems before anything downloads (user: "now whats the next logical step")

Work Log:
- src/lib/types.ts: AuditionSide (cueId, url, mimeType, durationMs, voiceId, deliveryId, stateLabel, origin "stored take"); AuditionPreview gains optional current (the A side of the pair)
- src/lib/ai/audition.ts: currentTakeForLine(projectId, speaker, line) finds the latest stored VOICE take whose label matches "speaker: text" (same cue-to-line convention as the dialogue parser, case-insensitive, latest 200 takes, best-effort); renderVariantAudition attaches it as audition.current whenever the auditioned line is a real production line (sample reads stay single-sided); zero extra TTS: the A side references the take already living under /voices/
- src/app/api/voice-auditions: response gains current, attached for state auditions AND plain voice auditions (hear a candidate voice against the incumbent take of the same line) whenever a speaker + line are known; custom board lines with no stored take stay single-sided (current null)
- src/lib/dsh/tools.ts: set_state_voice_variant result text now frames the audition as an A/B pair ("play both in this trace and compare before re-rendering") when a stored take exists; tool description mentions the pair
- src/lib/dsh/prompts.ts: doctrine rule 18 rewritten around the A/B pair (current take first, then the new read; veto before any re-render)
- src/components/views/dsh-console.tsx: the trace audition card renders two labeled players when audition.current exists: CURRENT TAKE (slate, take voice + duration + state label) above PROPOSED (emerald, voice + pace + pitch + duration), with an amber "A/B: current first, then proposed" hint; single-player card unchanged when no A side
- src/components/views/artists-dialog.tsx: state auditions store the returned pair per character (statePairs); a compare button (amber headphones) appears next to the state try play button once a pair exists; playCompare chains leg 1 (current stored take URL) then leg 2 (proposed base64 render) with an interruption guard (stopAudition mid-leg-1 does not roll into leg 2); readout gains "A/B ready: current take <voice> · <dur>"
- src/lib/api-client.ts: AuditionCurrentSide type; AuditionResult.current
- Webtoon export pre-flight (comic-view.tsx): runSliceExport is now a two-phase gate - fetch the diff, and when any take is stale or blocked the download stops and an amber pre-flight panel appears listing each offender (STALE/BLOCKED badge, shot number, speaker, cut line text, changed labels) plus fresh/unrendered tallies, with "Export anyway (N fresh)", a "re-render from the Direction diff board" hint and a dismiss X; all-fresh runs download straight through exactly as before; doSliceExport(voiceStatus) performs the actual export and keeps the post-download manifest flag line
- Verified E2E (scripts/e2e-ab-audition.ts, steps tool + api, real TTS on the tool step): bind attaches audition.current (origin "stored take", /voices/*.wav on disk, cueId resolves to the VOICE cue "Lin Yue: The Jade Sword still answers my call.", duration 3358ms, voice jam); result text carries "A/B pair"; bind makes the take stale, restore re-freshes; API: state audition returns current on the character's own line (A side /voices/ vs B side base64 render), custom board line with no take returns current null, plain voice audition of a candidate voice returns the incumbent take as current, unknown state 404
- Verified E2E (agent-browser): injected demo trace renders the two-player card (CURRENT TAKE jam 3.4s + PROPOSED kazi x0.74 pitch x0.75 6.1s), current-take media fetched 206 and probe-played ok, no page errors; injected message removed after; Comic Mode with a hint-pinned stale take: "Export slices" shows the pre-flight panel (STALE S1 Lin Yue "state speed hint", 0 fresh, 1 unrendered noted) with NO download, "Export anyway" completes with "manifest flags 1 stale take", restoring x0.85 + re-render then exports straight through with "all takes fresh" and no gate; casting board: state audition readout "A/B ready: current take jam · 3.4s", compare button appears and its first leg fetched the stored take (200/Media 206); screenshots saved (iter16-dsh-ab-card.png, iter16-export-preflight.png, iter16-export-anyway.png, iter16-casting-ab.png)
- Post-verification state: battle-damaged state back to jam/x0.85/x0.8, diff 1 fresh / 0 stale / 1 unrendered, injected trace removed

Stage Summary:
- GitHub main pushed with this iteration (16 commits + this one)
- Auditioning a performance change is now a true before/after decision: every DSH bind and every casting-board state try can be heard against the take that is actually in the stems, with zero extra TTS cost for the A side
- The webtoon export no longer discovers staleness after the fact: stale or blocked stems are named shot-by-shot before the ZIP exists, and downloading anyway is an explicit, informed click
- Next horizons: multi-line state arcs (a state override spanning a shot range instead of a single line), DSH proposing the export pre-flight in the same turn it detects staleness, casting-board A/B for plain voice casting (candidate vs incumbent already supported by the API)

---
Task ID: 17
Agent: Super Z (main agent)
Task: Iteration 17 - multi-line state arcs (a state override spanning a shot range, in the dialogue editor and as DSH's set_state_arc) and DSH proposing the export pre-flight in the same turn it detects staleness (user: "Proceed with multi-line state arcs (a state override spanning a shot range), and DSH proposing the pre-flight in the same turn it detects staleness")

Work Log:
- src/lib/comic/dialogue.ts: stampStateArc(lines, speaker, fromIndex, state) - stamps a state override onto ONE speaker's lines from fromIndex onward (case-insensitive speaker match, only real changes counted, null clears); pure helper shared by the DSH tool and the comic-view UI
- src/lib/dsh/tools.ts: set_state_arc (30th tool) - stamps/clears a state override across a shot range (shotFrom..shotTo inclusive, defaults to the whole scene, sceneNumber defaults to latest scene-with-shots); fuzzy state match stamps the FULL label so overrides stay exact; per-shot stamping via stampStateArc, ProductionEvent STATE_CHANGE, result reports stamped lines + touched shots; idempotent re-runs report "All N line(s) in the range already carry ..." instead of a misleading no-speaker message; zero-stamp cases distinguish no-speaker (lists speakers present) from no-dialogue; error paths: unknown character (lists cast), unknown state (lists states), inverted range (lists shots), out-of-range (lists shots), stateless character (create_character_state hint)
- Pre-flight proposal in the diff tools: diff_episode_direction (when stale > 0 and not re-rendering) appends "Export pre-flight: ... the webtoon slice export will stop at its gate and name each one before downloading - propose this re-render to the creator in the same turn"; diff_all_episodes appends the season-wide variant when totals.stale > 0
- src/lib/dsh/prompts.ts: intro tool list gains set_state_arc; rule 7 points at set_state_arc for states that last across a beat; rule 17 gains the same-turn pre-flight proposal duty when a diff reports stale takes without an immediate re-render
- Dialogue editor (comic-bubbles.tsx): every line's State row gains an arrow button (ArrowDownToLine, violet) - "State arc: apply <picked state or Auto (clear)> to this line and every following line of <speaker> in this scene"; onExtendArc prop (optional, hides the control when absent); arcBusy spinner per line, closes the editor on success
- comic-view.tsx: extendStateArc walks the scene's shots (sorted by number) from the clicked shot onward (fromIndex 0 for later shots), stamps via stampStateArc and persists through api.patchShot per changed shot, invalidates once; wired into DialogueEditor
- README: 29-tool count, set_state_arc + same-turn pre-flight proposal in the DSH row, state arc + export pre-flight in the Comic Mode row
- Verified E2E (scripts/e2e-state-arc.ts, steps plan + tool): plan layer - stamping is speaker-exact/index-aware/no-op-on-re-run, arc forces an out-of-episode state over the Ep7 auto plan (jam variant -> cast voice, hints gone, sig moved), and the reverse pull (battle-damaged forced at Ep15 brings jam + pitch 0.8 back in); tool layer - 2 lines stamped across shots 1+3 of Sc12 from one call, full label stamped, re-run no-op, diff flags exactly the S1 take (voice / casting, state speed hint, pitch hint) with the line override named in the current block, clear restores 1 fresh / 0 stale, pre-flight proposal present in diff_episode_direction AND diff_all_episodes results, all five error paths correct; dialogues restored exactly, season ends fresh
- Verified E2E (agent-browser): dialogue editor arc button renders with a dynamic title; Radix state pick + one arc click stamped "S02 - Foundation Established" onto shots 1 and 3 (DB verified); reopening shows the persisted state in the select; Auto + arc click cleared the arc across the scene (DB + diff API verified fresh); note: agent-browser's native select command does not drive the shadcn/Radix Select, so the first automated attempt silently applied Auto (caught by the DB check and redone via trigger+option clicks; harmless no-op by design); the Direction diff badge stays hidden until the dialog is opened once (pre-existing behavior, untouched); screenshots saved (iter17-dialogue-arc-button.png, iter17-diff-badge.png); no page errors
- Post-verification state: Sc12 dialogues back to auto everywhere, Ep7 diff 1 fresh / 0 stale / 1 unrendered

Stage Summary:
- GitHub main pushed with this iteration (17 commits + this one)
- A state is now a span of story, not a checkbox: creators paint a state across a scene with one click (or one DSH call), every affected line performs with the variant voice + hints + register, and the direction diff tracks the whole arc through the existing per-line sig axes with zero schema change
- Staleness now comes with a proposal attached: the moment a diff detects stale takes, the tool result hands DSH the pre-flight framing so the creator hears "your export will gate, want the re-render?" in the same turn
- Next horizons: arc awareness in the panel inspector (show the active state span on each shot card), cross-scene/episode-wide arcs, DSH auto-re-render offer after set_state_arc in the same turn

---
Task ID: 18
Agent: Super Z (main agent)
Task: Iteration 18 - arc spans shown on shot cards in the panel inspector, and cross-scene arcs (user: "Proceed with this: arc spans shown on shot cards in the panel inspector, and cross-scene arcs")

Work Log:
- src/lib/comic/arcs.ts (new): pure arc-span layer over dialogue overrides - computeArcSpans walks episode-ordered shots (scene number, then shot number) and returns maximal runs of ONE speaker's lines sharing the same state (other speakers and narration do not break a run; the span's speaker going auto ends it; resuming the state opens a new span); each ArcSpan carries start/end scene+shot, line/shot counts, crossesScene and ordered shotIds; arcSpansForShot maps a shot to its spans with startsHere/endsHere flags; formatArcRange ("Sc12 shots 3-6" in-scene, "Sc12 S3 → Sc13 S2" across) and describeArcPosition shared by every surface
- Shot cards (comic-view.tsx): ArcChips component renders up to two violet chips stacked under the shot-number chip on PanelFrame AND WebtoonPanel (filled chip = the span starts here, outlined = continues; "+N arcs" overflow); chip tooltip names state, speaker, full span range, the crosses-scenes marker and the shot's position; ComicView computes orderedShots/arcSpans/arcChipsByShot from the episode's scenes once per render
- Panel inspector (panel-inspector-dialog.tsx): new State arcs section (Route icon) at the top - one violet card per span touching the inspected shot with the state label, speaker, CROSS-SCENE badge when the span crosses scenes, the range + line/shot counts + position readout, and this shot's own covered lines quoted; empty state points at the dialogue editor's arrow buttons and set_state_arc; fed by a new optional episodeScenes prop from ComicView
- Cross-scene arcs in the dialogue editor (comic-bubbles.tsx): second violet button (ChevronsDown) next to the scene arc button - "Cross-scene arc: apply X to this line and every following line of <speaker> through the end of this episode (crosses scene boundaries)"; extendArc is mode-aware (scene | episode) with per-mode busy state; description copy updated
- comic-view extendStateArc gained a scope arg: episode scope walks ALL of the episode's scenes and shots in order from the clicked position, scene scope keeps the previous behavior
- DSH set_state_arc: new scope arg ("scene" default, fully back-compatible - scene-scope result text byte-identical) - scope:'episode' resolves the episode (episodeNumber arg or latest episode with shots), stamps from sceneNumber/shotFrom (start scene + shot) through toSceneNumber/shotTo (end scene + shot), covering every shot in between scenes; range labels are scene-prefixed (Sc12 S4) and the result names the episode and both endpoints ("across episode 7 (Sc12 S1 → Sc13 S2)"); error paths: unknown episode (lists episodes with shots), unknown start/end scene (lists scenes with their shot ranges), shot bounds out of range, inverted ranges ("the arc must run forward"); ProductionEvent summaries carry the episode range
- prompts.ts: intro and rule 7 now document scope:'episode' for beats that cross scene boundaries
- Verified E2E (scripts/e2e-cross-scene-arcs.ts, steps plan + fixture + tool + cleanup, all PASS): plan layer - span runs, breaks on the speaker's auto line, resumes as a new span, rival lines and narration do not break, positions and labels correct; tool layer - one episode-scope call stamped 4 Lin Yue lines across Sc12 S1/S3 + fixture Sc13 S1/S2 (narration line untouched), real-data spans computed cross-scene (12->13, starts/ends correct), diff flagged exactly the Sc12 S1 take (voice / casting, state speed hint, pitch hint), one clear call reset the whole episode arc and the diff went fresh, partial range Sc12 S3 → Sc13 S1 stamped exactly its two edges, scene scope regression intact, all six error paths correct; dialogues restored exactly, fixture removed, season ends 1 fresh / 0 stale
- Verified E2E (agent-browser): with a stamped cross-scene arc, manhua shot cards show the violet chips (DOM titles: "Sc12 S1 → Sc13 S2 (crosses scenes) - starts here / runs through / runs through / ends here" across the four stamped cards) and webtoon panels render the same chips; panel inspector on shot 001 renders the State arcs card (S02 - Foundation Established, Lin Yue, CROSS-SCENE badge, "Sc12 S1 → Sc13 S2 · 4 lines across 4 shots · starts here", quoted line); dialogue editor shows both arrow buttons with correct titles and the Radix State pick + cross-scene button click cleared the whole episode arc through the UI (DB verified: 0 stamped lines, diff 1 fresh / 0 stale); console clean; screenshots saved (iter18-arc-chips.png, iter18-arc-chips-sc12.png, iter18-inspector-arcs.png, iter18-dialogue-episode-arc.png, iter18-webtoon-chips.png)
- README: DSH row and Comic Mode row updated for scope:'episode', arc-span chips and the inspector's State arcs section

Stage Summary:
- GitHub main pushed with this iteration (18 commits + this one)
- A state arc is now visible where the work happens: every shot card in Comic Mode wears its arc chips, the panel inspector shows the full span (range, cross-scene badge, position, covered lines), and a beat that crosses scene boundaries stays ONE arc - stampable from the dialogue editor's double-arrow or DSH's set_state_arc scope:'episode', tracked end-to-end by the existing per-line sig axes with zero schema change
- Next horizons: DSH auto-re-render offer after set_state_arc in the same turn, arc timeline strip (a season-wide span ruler across episodes), arc templates (reusable beat presets like "possession spread" applied to a scene range)

---
Task ID: 19
Agent: Super Z (main agent)
Task: Iteration 19 - season-wide arc ruler (spans visualized across episodes), DSH offering the re-render in the same turn as set_state_arc, and reusable arc templates ("possession spread") (user: "Proceed with a season-wide arc ruler (spans visualized across episodes), DSH offering the re-render in the same turn as set_state_arc, and reusable arc templates ('possession spread')")

Work Log:
- src/lib/comic/arcs.ts season layer: SeasonArcShotInput/SeasonArcSpan, computeSeasonArcSpans reuses the run logic over the WHOLE season's episode-ordered shots so a span that survives an episode boundary (same speaker + state on both sides, rival lines and narration do not break) merges into ONE season arc with start/endEpisode and crossesEpisode; formatSeasonArcRange ("Ep7 Sc12 S3 -> Ep8 Sc20 S1"); packSpanLanes greedy lane packing (overlap stacks, adjacency reuses, input order preserved)
- src/lib/comic/arc-templates.ts (new): reusable beat SHAPES - possession spread (auto 25% -> state 50% -> auto 25%), full takeover (auto 15% -> state 70% -> auto 15%), recovery arc (state 60% -> auto 40%); planTemplateCounts largest-remainder allocation (exact sums, ties to the wider fraction then the earlier segment so short beats lean on the dominant segment); planTemplateAssignment per-line plan; applyArcTemplate paints the shape across the WHOLE range in one arc (speaker's lines collected in range order across shots, NOT repeated per shot), only real changes counted, per-segment report; formatTemplateReport; arcTemplateByName (id, exact, partial, miss)
- src/lib/dsh/tools.ts: shared resolveArcRange extracted from set_state_arc (both scopes, every tool-facing error string byte-identical, now reports the owning episode); directionImpactFor diffs the owning episode right after an arc move and hands DSH the same-turn framing; set_state_arc result now ends with "Direction impact: episode N now has S stale take(s) ... Offer the re-render in the same turn ..." (clean path: "is clean ... no rendered take moved"; no-op path: "No take moved, so no re-render is needed"); apply_arc_template is the 31st tool (template name/id + stateLabel + the same range args/scopes, shape report in the result, same direction impact + offer, fuzzy state match stamps the FULL label, error paths: unknown template lists the registry, missing stateLabel, unknown state lists states, unknown character, inverted ranges)
- src/lib/dsh/prompts.ts: intro lists apply_arc_template; rule 7 points at templates when a beat has a SHAPE; rule 17 now says the arc tools already report Direction impact and DSH must put the same-turn offer in its reply ("N takes are now stale, want me to re-render them?") and chain diff_episode_direction reRender:true in the same batch when the creator already asked
- src/components/views/arc-ruler.tsx (new): season-wide ruler strip - episode segments sized by shot count with E-labels + titles, violet lane-packed bars (min width, tooltip with speaker/state/season range/line+shot counts/crosses marker), cross-episode bars glow amber and carry a "-> season" marker, click a bar opens that episode, empty state points at the editor/tools; computed from the project query the view already holds (zero new API)
- src/components/views/arc-template-dialog.tsx (new): Arc templates dialog in the Comic Mode toolbar - template select with a stacked shape bar preview, speaker select (characters with states), state select (variant voice + ep shown), target range (whole episode or one scene), LIVE dry-run listing every affected line with its resulting state before anything is written, Apply persists changed shots via patchShot and invalidates
- comic-view.tsx: violet "Arc ruler" toggle button + collapsible ruler strip above the content, ArcTemplateDialog wired with mapped states (voiceVariant -> variantVoice)
- README: 31-tool counts, DSH row gains apply_arc_template + direction impact/same-turn offer, Comic Mode row gains the season-wide arc ruler and arc templates with dry-run preview
- Fixed en route: applyArcTemplate initially painted per shot (a mini possession arc every shot); redesigned to one arc across the range via across-shots line collection; removed a double-count bug in the tool's stamped total
- Verified E2E (scripts/e2e-arc-templates.ts, steps plan + tool + cleanup, ALL PASS): plan layer - allocation math exact across every template x sizes (8->2/4/2, 4->1/2/1, 3->1/1/1, 2->1/1/0, 1->dominant), assignment order, across-shots painter (rivals untouched, re-run no-op, changed flags per shot), season merging across an episode boundary + resume after the speaker's auto line, season range labels, lane packing, name resolution; tool layer - possession spread stamps per plan over 2 real lines (S1 auto, S3 Foundation, clean direction impact since S3's cue is unrendered), re-run no-op, full takeover reshapes both lines and the RENDERED S1 take goes stale with the same-turn offer riding the result (diff agrees: exactly 1 stale), set_state_arc clear reports "is clean" with no offer and the season returns fresh, set_state_arc stamp carries the offer too, recovery arc at episode scope names "across episode 7 (Sc12 S1 -> Sc12 S6)" and an in-episode state stamp keeps the take fresh, REAL cross-episode merge verified with a fixture Ep8 (one span "Ep7 Sc12 S3 -> Ep8 Sc20 S1", 2 lines), all six error paths correct; dialogues restored byte-identical, fixture removed, season ends 1 fresh / 0 stale
- Regression: e2e-state-arc (plan + tool), e2e-cross-scene-arcs (fixture + plan + tool + cleanup) and e2e-diff-tools all still pass on the refactored range resolver
- Verified E2E (agent-browser): Comic Mode toolbar shows the violet "Arc ruler" toggle and "Arc templates" button; empty ruler reads the guidance line; template dialog dry-run lists S001 -> auto and S003 -> S02 exactly, Apply persists (DB: S3 carries the state, S1 untouched), shot card wears the chip and the ruler shows the bar; with the Ep8 fixture the ruler draws ONE bar "Ep7 Sc12 S3 -> Ep8 Sc20 S1 - 2 lines in 2 shots - crosses episodes" crossing the E07|E08 boundary with the amber glow + "-> season" marker (screenshot iter19-season-bar.png), clicking the bar keeps/opens its start episode; cleanup restores ruler-empty + 0 chips (DB-verified); console clean, no page errors; screenshots saved (iter19-ruler-empty.png, iter19-ruler-bar.png, iter19-season-bar.png, iter19-template-dialog.png)
- Post-verification state: fixture Ep8 removed, Sc12 dialogues byte-identical originals, Ep7 diff 1 fresh / 0 stale / 1 unrendered

Stage Summary:
- GitHub main pushed with this iteration (19 commits + this one)
- The season is now measurable: the arc ruler draws every state arc over an episode axis, and a beat that survives an episode boundary is drawn as ONE bar across the boundary - the same pure span layer, zero schema change
- Arc direction changes now come with their bill attached: set_state_arc and apply_arc_template report the takes they made stale and instruct DSH to offer the re-render in the same turn
- Beats are paintable from a palette: possession spread / full takeover / recovery arc stretch over any range from the dialog (with a dry-run preview) or one DSH call
- Next horizons: user-defined arc templates saved per production, ruler lanes per speaker, DSH proposing a template when a creator describes a beat shape in prose

---
Task ID: 20
Agent: Super Z (main agent)
Task: Iteration 20 - user-defined arc templates saved per production, per-speaker ruler lanes, and DSH suggesting a template when a creator describes a beat in prose (user: "user-defined templates saved per production, per-speaker ruler lanes, and DSH suggesting a template when a creator describes a beat in prose")

Work Log:
- prisma/schema.prisma: ArcTemplate model (projectId, name, description, segments JSON [{frac, kind: auto|state}], unique per (projectId, name), Cascade) + Project.arcTemplates; db push + generate clean
- src/app/api/arc-templates/route.ts + [id]/route.ts: GET/POST/PATCH/DELETE with parseArcTemplateSegments validation (normalized fracs, adjacent same-kind merge), duplicate-name 409, malformed segments 400, delete logs a ProductionEvent
- src/lib/comic/arc-templates.ts: parseArcTemplateSegments (validate + normalize + merge), formatTemplateShape ("auto 25% -> state 50% -> auto 25%"), matchArcTemplates prose scorer - lexical overlap between the creator's words and each template's name + description PLUS shape signals (starts normal / ends normal / the state dominates / opens inside the state) matched against segment layout; sorted best-first
- src/lib/dsh/tools.ts: suggest_arc_template is the 32nd tool - ranks the registry (built-ins + production rows via arcTemplateRegistry) against the creator's prose, strong match (score >= 3) names the template with shape + why + the apply_arc_template framing to propose in the same turn, weak/no match falls back to the registry with set_state_arc/custom-shape guidance, characterName lists stateLabel candidates (unknown character non-fatal); apply_arc_template now resolves SAVED production templates (resolveTemplateForApply: built-ins first, then DB rows, exact then partial name match; malformed stored shapes get a re-save hint; the unknown-name error lists production templates too) and tags "(production template)" in results + events; built-in result strings byte-identical
- src/lib/dsh/prompts.ts: intro gains suggest_arc_template; rule 7 rewritten: prose beat description -> suggest first, then apply on confirmation; production-saved templates are matched too
- src/lib/api-client.ts: ArcTemplateRow + listArcTemplates/createArcTemplate/patchArcTemplate/deleteArcTemplate
- src/components/views/arc-template-dialog.tsx: Shape select gains groups (Built-ins / Production templates (saved) / Custom); custom segment editor (auto|state toggle + percent input per row, add/remove up to 6, normalized on use); Save-as-template section (name + description) persists the CURRENT shape; saved templates show a "saved on this production" strip with delete (window.confirm); dry run + Apply run off the effective template (custom | saved | built-in)
- src/lib/comic/arcs.ts: groupSpansBySpeaker (ONE lane per speaker, ordered by first span start, spans sorted inside the lane)
- src/components/views/arc-ruler.tsx: per-speaker lanes with a labeled left gutter (speaker names, title lists arc count), bars sit on their speaker's row, hint text "one lane per speaker"
- Verified E2E (scripts/e2e-template-registry.ts, steps plan + api + tool, ALL PASS): plan - segment validation (merge/normalize/5 rejection paths), shape readout, prose matching (possession/recovery/domination prose each rank their template first, empty prose, production template wins on its own words), per-speaker lane grouping; api - CRUD round trip with 409/400/404 paths and normalized segments echoed back; tool - suggest strong match proposes possession spread with state candidates, production template matched and tagged, no-match fallback, empty-arg error, apply of TWO saved templates (corruption creep stamps shot 3 with a clean impact, steady drain forces shot 1 and the RENDERED take goes stale with the same-turn offer riding the result, diff agrees exactly 1 stale), unknown-name error lists production templates, clear resets, fixture + templates + fixture state removed, Ep7 untouched (0 fresh / 0 stale / 1 unrendered)
- Regression: the season data had drifted since iter19 (Sc12 S1 lost its dialogue, no rendered takes, no variants on real states), so e2e-arc-templates, e2e-state-arc, e2e-line-overrides, e2e-state-hints and e2e-ab-audition were ported to self-contained Ep8 fixtures (own shots + dialogue + temporary fixture state with a variant, create-or-reuse where steps must share state, epoch createdAt for the audition line scan, explicit cleanup steps); all pass again: iter19 template shapes (full takeover over 3 lines keeps the S1 auto bookend via the largest-remainder tie to the earlier segment, recovery arc forces the head and stales the rendered take), iter17 arcs + pre-flight proposal, per-line overrides + bind auditions, state hints end-to-end (hints -> stale -> batch re-render -> fresh -> clear -> stale -> re-render -> cleanup), A/B pairs (current side rides the fixture take)
- Verified E2E (agent-browser): ruler draws TWO labeled lanes (Lin Yue, Su Yan) with the fixture's four arcs in their speaker's row (screenshot iter20-ruler-lanes.png); template dialog: custom shape editor set to 30/40/30, saved as "browser creep" -> auto-selected with the saved strip + delete button (iter20-template-saved.png); dry run listed exactly S003 -> S02; Apply persisted (DB verified) and the ruler merged Ep7 S3 + Ep8 S1 into ONE cross-episode "S02 - Foundation Established -> season" bar in Lin Yue's lane (iter20-ruler-applied.png); delete with confirm fell the select back to built-ins and the API returns []; cleanup restores Sc12 S3 byte-identical, fixture removed, ruler back to "0 state arcs"; console clean, no page errors
- Post-verification state: Ep8 removed, Sc12 S3 byte-identical, 0 saved templates, Ep7 diff 0 fresh / 0 stale / 1 unrendered (unchanged baseline)

Stage Summary:
- GitHub main pushed with this iteration (20 commits + this one)
- Arc templates are now a production vocabulary: creators build and save their own beat shapes next to the built-ins (dialog: live dry run; DSH: apply by name, and the suggest tool matches prose to the whole registry), and the season ruler reads character by character - one labeled lane per speaker so rival arcs never stack
- Data-drift lesson banked: E2E scripts now carry their own fixtures (episode + state + take) instead of assuming seed data, so regressions stay green regardless of demo drift
- Next horizons: DSH proposing a template inside the SAME turn it detects a beat description (chain suggest + apply in one batch), template sharing across productions (studio-level library), ruler zoom/focus mode per episode

---
Task ID: 21
Agent: Super Z (main agent)
Task: Iteration 21 - DSH chaining suggest + apply in one batch when it detects a beat description, a studio-level template library shared across productions, and a ruler focus mode per episode (user: "proceed with DSH chaining suggest + apply in one batch when it detects a beat description, a studio-level template library shared across productions, and a ruler focus mode per episode")

Work Log:
- prisma/schema.prisma: ArcTemplate gains scope ("PROJECT" | "STUDIO") + nullable projectId (null = studio-library row shared across productions); comment documents the two scopes and the SQLite NULL-in-unique-index caveat; db push + generate clean
- src/app/api/arc-templates/route.ts: GET returns the production's rows PLUS the studio library (OR scope STUDIO), rows carry scope + projectId; POST accepts scope - STUDIO rows dedupe across the library in app code (name clash = 409 "already exists in the studio library"), PROJECT rows keep the (projectId, name) unique check; studio rows are created with projectId null (they outlive any production) and the event stays attributed to the caller's production
- src/app/api/arc-templates/[id]/route.ts: PATCH accepts scope - promote to STUDIO (detach + studio-name dedupe excluding self) or demote to PROJECT (requires body.projectId); bad scope 400; DELETE works for studio rows and names the library in the event
- src/lib/api-client.ts: ArcTemplateRow gains scope + projectId; create/patch signatures accept scope (+ projectId for demote)
- src/lib/dsh/tools.ts: arcTemplateRegistry + resolveTemplateForApply include studio rows (source "built-in" | "production" | "studio", production wins over a same-named studio row, error registry lists "(studio template)" rows); new templateSourceTag + resolveCharacterState + applyResolvedTemplate helpers (apply core extracted, byte-identical strings) shared by apply_arc_template and the new ONE-BATCH CHAIN: suggest_arc_template accepts stateLabel + the range args, and when the prose match is strong (score >= 3) with characterName + stateLabel it applies the matched template IN THE SAME CALL - result = match header + "Applied in this batch: <apply core result>" + one-batch closing guidance; chained failures (unknown character, bad state, bad range) return ERROR with the match header intact; non-chained path proposes with "or pass stateLabel here to apply it in this same batch"
- src/lib/dsh/prompts.ts: intro + rule 7 rewritten: naming a shape -> apply_arc_template; describing one in prose -> suggest_arc_template that CHAINS the apply when character and state are known (omit stateLabel only when genuinely ambiguous); registry now spans built-ins + production saves + the studio library shared across every production
- src/components/views/arc-ruler.tsx: per-episode FOCUS MODE - episode labels in the label row are buttons ("click an episode label to focus the ruler on it"); focused view: header chip "Focus: EpNN - title" + "Exit focus (season view)", scene segments replace episode segments (SC labels + titles), shot ticks strip under the axis (S1..Sn labels when <= 32 shots, dots above), season spans CLIPPED to the episode window with amber edge glow and arrow markers ("←"/"→" + "continues from/to EpN" in the tooltip), same per-speaker lanes, focus-scoped empty message
- src/components/views/arc-template-dialog.tsx: Shape select gains the "Studio library (shared across productions)" group between built-ins and production saves; save section has a This production / Studio library toggle with scope-specific copy; saved strip shows the studio message + a "to studio library"/"make private" promote/demote button (patchArcTemplate) next to delete (studio-aware confirm text)
- scripts/e2e-studio-library.ts: api step (studio CRUD round trip, cross-production visibility from a foreign projectId, studio dup 409, promote/demote via PATCH, promote name-clash 409, bad scope 400, delete + 404, library empty again) + tool step on TWO self-contained fixtures: project B ("E2E Studio Library B", Ep9 Sc90, Ren Wu + rendered take) proves cross-production sharing (studio template matched from prose AND applied by name on another production, staleness + offer riding the result) and the built-in chain; production A's fixture (Ep8 Sc20, Lin Yue + rendered take) exercises the studio chain (match header + "Applied in this batch" + 1 stale + event tag), the chained-failure paths (bad state keeps the match header), the weak-prose no-chain path, production-over-studio name precedence, and the unknown-name error listing studio rows; cleanup restores Ep8 fixtures, state, project B and the studio row, Ep7 baseline re-asserted
- scripts/ep7-dialogue-backup.ts: byte-identical backup/restore helper for browser verification of arc painting on the real season

Verified E2E (scripts/e2e-studio-library.ts, steps api + tool, ALL PASS): api - 15 checks; tool - 24 checks including two real TTS renders, exactly 1 stale after the chained studio apply on A and after the cross-production apply on B, DB lines state-on-1+3/auto-on-4/rival-untouched, Ep7 untouched (0 fresh / 0 stale / 1 unrendered)
- Regression: e2e-template-registry (plan + api + tool), e2e-state-arc, e2e-arc-templates ALL PASS (non-chained suggest strings byte-identical, apply markers unchanged)
- Verified E2E (agent-browser): ruler season mode shows the painted "Battle-damaged (temple fight)" bar in Lin Yue's lane with the focus hint (iter21-ruler-season.png); clicking the E07 label focuses - "Focus: Ep07" chip + exit button, SC12 scene label, Lin Yue lane with the bar, S1..S6 shot ticks (iter21-ruler-focus.png); template dialog: save-scope toggle, studio save ("browser studio sweep") selects itself with the studio strip + make private + delete (iter21-template-studio.png), Shape select shows "STUDIO LIBRARY (SHARED ACROSS PRODUCTIONS)" group, delete-with-confirm falls back to built-ins and the API returns []; console clean, 0 page errors; Ep7 dialogues restored byte-identical (diff back to 0 fresh / 0 stale / 1 unrendered), 0 saved templates

Stage Summary:
- GitHub main pushed with this iteration
- The template registry is now three-layered (built-ins -> production saves -> studio library) and visible from every production: a house beat shape saved once in the studio library is matched from prose and applied by name on any show, while same-named production templates keep precedence locally
- DSH chains suggest + apply in ONE batch when a creator describes a beat and the character/state are known: the match explanation, the stamped outcome and the direction impact arrive in a single tool result, so the beat lands and the re-render offer goes out in the same turn
- The ruler gained a per-episode focus mode: scene-resolved axis, per-shot ticks, and season beats clipped into the window with amber arrows showing where they continue
- Next horizons: DSH applying across MULTIPLE speakers in one chain (ensemble beats), template versioning (v2 shapes with migration notes), ruler focus mode remembering the focused episode per session

---
Task ID: 22
Agent: Super Z (main agent)
Task: Iteration 22 - DSH applying a template across MULTIPLE speakers in one chain (ensemble beats) (user: "awesome, proceed with all" on the proposal: DSH ensemble chain + parallel arcs made visible + ruler focus mode remembering the focused episode per session)

Work Log:
- src/lib/comic/arcs.ts: ArcSpan gains arcBatches (ordered dedup of ensemble batch ids on the span's lines, collected in computeArcSpans); groupEnsembleSpans rewritten as union-find with TWO links - shared arcBatch (different speakers) and shared shot id - so the classic alternating-dialogue ensemble groups even when no two spans share a shot; ensembleGroupSizes helper
- src/lib/comic/dialogue.ts: DialogueLine gains arcBatch (ensemble batch tag, MAX 64); parseDialogue reads + normalizes it, serializeDialogue writes it ONLY when set (byte-compatible with all existing dialogue JSON); stampStateArc clears the tag on changed lines (a new direction replaces the old parallel beat); the tag never enters the voice sig, so it cannot make a fresh take stale
- src/lib/comic/arc-templates.ts: applyArcTemplate takes options {batchId}: every WRITTEN line carries the batch (or null for solo applies), and an already-matching line whose batch differs is refreshed too (state unchanged, not counted as stamped) so a re-applied ensemble keeps its whole shape under ONE batch
- src/lib/dsh/tools.ts: parseEnsembleCharacters (JSON array of names or {name, stateLabel}, string-or-array, 6-speaker cap, tool-facing usage errors); applyEnsembleTemplate - resolves the range ONCE, validates speakers one by one (unknown name / unmatched state / missing stateLabel SKIP that speaker with a reported reason instead of killing the batch), paints the shape sequentially onto the shared buffer with ONE ens-<uuid> batch id, persists changed shots once, logs ONE event "(ensemble: A, B)", and returns a per-speaker breakdown (- X with "state": n line(s) stamped of m. Shape: ...) with ONE combined Direction impact and a single re-render offer; total-skip -> ERROR "Ensemble apply resolved no speaker." with every reason; applyResolvedTemplate (solo) now reports only state-touched shots in "in shot(s)" (batch refreshes persist silently); apply_arc_template gains the characters branch (explicit "Ensemble applies need a state" error when no speaker carries a state); suggest_arc_template gains the ENSEMBLE CHAIN (characters + strong match -> applied in the same call, wrapped with the match header) and the prose ENSEMBLE HINT (when a strong match is not chained and the description names 2+ cast members, the result points DSH at characters:[{name:"A"},{name:"B",stateLabel:"..."}])
- src/lib/dsh/prompts.ts: intro (ensemble in the same batch), rule 7 (ensemble applies: characters arg, per-speaker stateLabel overrides, one combined impact, skip reporting), rule 17 (ensemble impacts count every speaker's takes in ONE impact)
- src/components/views/arc-ruler.tsx: season + focus mode bars carry a teal border and an xN badge when their ensemble group has 2+ members, tooltips name the parallel speakers; focus mode now remembers the focused episode per session via useSyncExternalStore over sessionStorage (storage IS the state: survives ruler close/open and episode switches, SSR-safe, no setState-in-effect); projectId prop
- src/components/views/comic-view.tsx: shot-card arc chips show the teal xN badge on ensemble members (tooltip names the beat); ArcRuler gets projectId
- src/components/views/panel-inspector-dialog.tsx: State arcs section clusters ensemble members into ONE teal card ("ENSEMBLE BEAT - N speakers in parallel - M arcs on this shot") with inset member cards, story order preserved
- scripts/e2e-ensemble-arcs.ts: plan (batch/shot/bridge grouping, same-speaker isolation, batch collection, painter batch stamp/refresh/clear, serialize round trip) + tool (2 real TTS renders; ensemble possession apply with shared + per-speaker states; no-op re-run; recovery ensemble making BOTH rendered takes stale under ONE offer; real-data span grouping into ONE beat of 4; partial skip; total-skip ERROR; unmatched-state ERROR; missing-state error; 4 malformed-args errors; the suggest ensemble chain; the prose ensemble hint; legacy solo format pinned; batch clearing via set_state_arc; ensemble vs solo event tagging) on a fixture Ep10/Sc30 + fixture character "E2E Rival Wei" + two fixture states, all removed in cleanup
- scripts/iter22-browser-fixture.ts: self-contained Sc12 backup/apply/restore for the browser pass

Verified E2E (scripts/e2e-ensemble-arcs.ts, steps plan + tool, ALL PASS): tool - 33 checks including exactly 2 stale after the recovery ensemble and ONE "Offer the re-render" occurrence, DB states + shared ens- batch on S3/S4, one beat of 4 grouped from real data, restore clean (Ep7 0 fresh / 0 stale / 1 unrendered, fixture episode + character + states removed)
- Regression: e2e-state-arc (plan + tool), e2e-arc-templates (plan + tool), e2e-template-registry (plan + api + tool), e2e-studio-library (api + tool) ALL PASS (solo header format byte-identical, non-chained suggest strings unchanged)
- Verified E2E (agent-browser): browser fixture stamps Lin Yue + Su Yan lines sharing one batch on Ep7 Sc12 S1/S2 (no cues there, diff stays clean); season ruler draws both lanes with teal-bordered bars and the x2 badge (iter22-ruler-season.png); clicking the E07 label focuses - chip + SC12 + S1..S6 ticks + both ensemble bars (iter22-ruler-focus.png); toggling the ruler off/on RESTORES the focus (sessionStorage); panel inspector shows the ENSEMBLE BEAT cluster card with 2 speakers in parallel (iter22-inspector-ensemble.png); console clean, 0 page errors; fixture restored byte-identical (S3 back to the original string), Ep7 diff 0 fresh / 0 stale / 1 unrendered

Stage Summary:
- GitHub main pushed with this iteration
- DSH can now land ONE beat on SEVERAL speakers in a single call: apply_arc_template takes characters (names or {name, stateLabel}), suggest_arc_template chains the ensemble apply from prose, unresolvable speakers are skipped and reported, and the whole batch carries ONE Direction impact and ONE re-render offer
- Ensemble applies tag their lines with a shared batch id, and the ruler lanes, shot-card chips and the panel inspector all read those as ONE parallel beat (teal + xN), even for alternating dialogue where no two spans share a shot
- The ruler focus mode persists per session, surviving ruler close/open and episode switches
- Next horizons: ensemble beats in the casting board (multi-speaker A/B audition rows), template versioning (v2 shapes with migration notes), arc playback (play an arc's takes in sequence to hear the progression)

---
Task ID: 23
Agent: Super Z (main agent)
Task: Iteration 23 - multi-speaker A/B audition rows on the casting board, template versioning, and arc playback (user: "awesome, proceed with multi-speaker A/B audition rows on the casting board, template versioning, and arc playback (hear an arc's takes in sequence)")

Work Log:
- prisma/schema.prisma: ArcTemplate gains version (Int, default 1) + versions (JSON array of {version, segments, note, at}, the REPLACED shapes newest-first); db push + generate clean
- src/app/api/arc-templates/route.ts: GET rows carry version + parsed versions (safeVersions tolerates array-or-string segment encodings); POST unchanged (defaults land at v1 with empty history)
- src/app/api/arc-templates/[id]/route.ts: PATCH archives the replaced shape on a REAL shape change - history entry {version, segments (parsed shape), note (creator note or auto "shape moved on update (vN -> vN+1)"), at} + version bump; same-shape PATCH and description-only PATCH are no-ops (no bump); response {id, version, bumped}
- src/lib/api-client.ts: ArcTemplateVersionEntry + ArcTemplateRow.version/versions; patchArcTemplate accepts note, returns {id, version, bumped}; new auditionEnsemble (2..6 speakers, rows + skipped)
- src/app/api/voice-auditions/route.ts: ENSEMBLE branch - ensemble (array of {speaker?, stateId?}) renders EVERY speaker in ONE call (per-speaker state drives variant voice + hints, cast/default voice otherwise, shared board line + register); each row keeps the full single-audition shape (proposed render + currentTakeForLine A side when the line has a stored take); unresolvable entries (unknown state, no speaker, duplicate, voice resolve failure) are SKIPPED and reported instead of sinking the batch; 400 guards: non-array, < 2 speakers, > 6; single-speaker path untouched
- src/lib/comic/arc-playback.ts (NEW): buildArcTakes(span, shots) - the stored VOICE takes of an arc's lines in story order, matched by the SAME "speaker: text" label convention the render chain writes (case-insensitive, span speaker + exact state only, url-less cues and cues naming another speaker skipped); mergeArcTakes(spans, shots) - ensemble beat queue, all members merged and re-sorted by scene/shot/line; pure layer, no DB/DOM
- src/lib/dsh/tools.ts: saved templates carry their version through arcTemplateRegistry + resolveTemplateForApply; templateSourceTag(source, version) appends " vN" INSIDE the parens at v2+ (" (production template v2)"); v1 strings stay byte-identical (pinned E2E); apply results, event summaries and suggest match headers all tag the version they used
- src/lib/dsh/prompts.ts: rule 7 gains the versioning doctrine (updating a shape bumps the version + archives the replaced one with a migration note; name the version when reporting what landed)
- src/components/views/arc-template-dialog.tsx: saved strip shows a v2+ version badge; "update to this shape" button (visible when the viewed shape differs from the saved one, 3-decimal shape identity) PATCHes the segments with an optional window.prompt migration note and shows a teal notice; "history (N)" toggle lists replaced shapes newest-first with shape readout + note; select items label saved templates with " · vN" at v2+
- src/components/views/artists-dialog.tsx: ENSEMBLE TRY block in the casting board - character chips (multi-select), "Audition ensemble (N)" runs ONE batch call; per-speaker A/B rows: A = the current stored take (amber, present only when one exists), B = the proposed read (violet), per-row A/B compare (headphones, current first then proposed); "play sequence" plays every row's proposed read in cast order; seqRef token cancels any running sequence (stop + dialog close + new plays all bump it); playUrlAwait/playClipAwait resolve on ended/error/PAUSE so stop never hangs the waiter; skips listed under the rows
- src/components/views/panel-inspector-dialog.tsx: arc playback - playbackShots memo carries episode shots + audio cues; takesByKey holds a take queue per arc card (solo queues for members, merged story-order queue for ensemble clusters via mergeArcTakes); every arc card shows "N takes" + a play button (disabled at 0 takes with an honest title); ensemble cluster header gains "play beat (N takes merged)"; one sequential player per dialog (playTokenRef token, pause-safe waiters), stops on dialog close
- scripts/e2e-ensembles-versions-playback.ts: plan (label matching, speaker/state filtering, story order, url-less + wrong-speaker cue skipping, missing shots, ensemble merge ordering/sum) + api (version round trip: create v1, same-shape no-bump, shape bump + history + note, description-only no-bump, delete; ensemble audition with real TTS: 2 rows, real renders, shape-valid A sides, duplicate/unknown-state/empty-entry skips, 2/6/array guards) + tool (v2 template apply tags "(production template v2)" in result AND event, v1 template keeps the legacy tag with no suffix, suggest header names the v2, Ep7 baseline re-asserted) on a fixture Ep11/Sc31 + fixture character/state, all removed in cleanup
- scripts/iter23-browser-fixture.ts: create/cleanup (v2 probe template with history) + arc/arc-restore (temporary Battle-damaged stamp on Ep8 Sc20 S1 with byte backup)

Verified E2E (scripts/e2e-ensembles-versions-playback.ts, steps plan + api + tool, ALL PASS): plan 9 checks, api 18 checks incl. two real TTS ensemble renders (212KB/165KB) and the version round trip, tool 8 checks incl. exact "(production template v2)" tagging and Ep7 untouched (0 fresh / 0 stale / 1 unrendered)
- Regression: e2e-template-registry (plan + api + tool), e2e-ensemble-arcs (plan + tool), e2e-studio-library (api + tool), e2e-arc-templates (plan + tool), e2e-ab-audition ALL PASS (legacy v1 tags byte-identical, single-audition path unchanged; one transient TTS 429 aborted a studio-library run mid-flight and left a fixture state behind - removed, then re-run green)
- Verified E2E (agent-browser): casting board - picked Lin Yue + Lin Yue Clone 001 chips, ONE ensemble audition rendered 2 A/B rows with a real A side (current take) on Lin Yue, B buttons and per-row compare, "play sequence" ran and stop cancelled it (iter23-board-ensemble.png); template dialog - "browser version probe · v2" in the select, v2 badge on the strip, history panel listing the replaced v1 shape with its migration note (iter23-template-versions.png); panel inspector on Ep8 Sc20 S1 - "Battle-damaged (temple fight)" arc card shows "1 take" + play button, playback started (title flips to stop) and stops cleanly, natural end resets the button (eval-verified transitions, iter23-inspector-playback.png); console clean, 0 page errors; fixtures restored (Ep8 dialogue byte-identical, diff back to 1 fresh / 0 stale / 0 unrendered, probe template removed, leftover fixture state from the aborted run deleted)

Stage Summary:
- GitHub main pushed with this iteration
- The casting board auditions ENSEMBLES now: one batch call renders every picked speaker (per-speaker state or cast voice), lays out one A/B row per speaker, and plays the whole ensemble in sequence - A/B is no longer limited to one character at a time
- Saved arc templates are versioned: updating a shape bumps the version and archives the replaced shape with a migration note (UI history + v2+ badges), and DSH reports the exact version it applied so creators can always explain a past stamp
- Arcs are auditionable from the inspector: every arc card counts its rendered takes and plays them in story order, and an ensemble beat plays as ONE merged story-order sequence
- Next horizons: DSH proposing an ensemble audition in the same turn it lands an ensemble arc, template version diffing (v1 vs v2 side-by-side in the dialog), arc playback from the season ruler bars

---
Task ID: 24
Agent: Super Z (main agent)
Task: Iteration 24 - DSH proposing an ensemble audition in the same turn it lands an ensemble arc, v1-vs-v2 side-by-side diffing in the template dialog, and arc playback from the ruler bars (user: "proceed with DSH proposing an ensemble audition in the same turn it lands an ensemble arc, v1-vs-v2 side-by-side diffing in the dialog, and playback from the ruler bars" + "Proceed with both")

Work Log:
- src/lib/types.ts: EnsembleAuditionPreview {speakers: AuditionPreview[], skipped: string[]} attached to TraceAction.ensembleAudition; AuditionPreview.source widens to "custom" | "character line" | "sample"
- src/lib/ai/audition.ts: renderVariantAudition gains text (pins the read to an EXPLICIT line: the ensemble apply passes each speaker's first line stamped into the state, so the audition is the exact line the re-render will perform) + filePrefix (arc- vs variant- WAV namespacing so ensemble applies and variant binds never clobber each other's files)
- src/lib/dsh/tools.ts: resolveCharacterState exposes the state's voice performance (stateId, voiceVariant, speedHint, pitchHint); applyEnsembleTemplate collects each applied speaker's FIRST line stamped into the state (range order, changed lines only) and renderEnsembleAudition renders ONE read per engaged speaker (variant voice or cast, state delivery register, hints, filePrefix arc) - a failed row is skipped and reported, never sinking the apply; result text tells DSH to point the creator at the rows/sequence in the SAME-TURN reply; ActionResult carries ensembleAudition; suggest_arc_template's ensemble chain rides the chained apply's audition onto its own result
- src/lib/dsh/orchestrator.ts: ensembleAudition rides the trace step next to the single audition
- src/lib/dsh/prompts.ts: rule 7 doctrine - every ensemble apply that moves lines ALSO renders the ensemble audition in the same call, point the creator at the rows and the sequence player in the same-turn reply before the re-render decision
- src/components/views/dsh-console.tsx: EnsembleAuditionBlock (teal) - per-speaker rows with A/B audio players (current take when one exists, then the proposed read), per-row pace/pitch/duration readout and the auditioned line, "play sequence" walks every row in speaker order (token-guarded, pause-safe waiters, stop/dialog close cancels), skipped reasons listed
- src/lib/comic/arc-templates.ts: diffTemplateShapes(oldSegs, newSegs) - position-aligned human diff (frac moves with signed one-decimal deltas, kind flips, added/dropped segments, 3-decimal identity), pure display helper
- src/app/api/arc-templates/[id]/route.ts: history entries typed segments: unknown (they store the PARSED shape since iteration 23)
- src/components/views/arc-template-dialog.tsx: history rows gain a per-version "diff" toggle - opens a side-by-side panel (v1 replaced vs vN current ShapeBars + mono shape readouts) with a "What moved" list from diffTemplateShapes ("Shapes match exactly" fallback); saved strip row now wraps (flex-wrap + wrapping scope text) fixing a pre-existing horizontal dialog overflow the diff panel made visible
- src/components/views/arc-ruler.tsx: PLAYBACK FROM THE BARS - the flat shot list carries its VOICE cues (audioCues pass-through was missing and silently zeroed every take queue, fixed); RulerBar component: the label button opens the span's episode, a ▶ chip (z-10, always clickable) plays the span's stored takes in story order; an ENSEMBLE member plays the WHOLE beat merged via mergeArcTakes; one token-guarded sequential player per ruler (pause-safe waiters, unmount/another bar/chip cancels); chips render in season AND focus mode; header hints name the affordance
- scripts/e2e-ensemble-audition.ts (NEW): plan (diffTemplateShapes: identical, frac moves with signed deltas, kind flips, added/dropped, thirds rounding) + tool on a self-contained fixture (Ep12/Sc32, Rival Wei + two TEMPORARY states with kazi variant + pitch hints): ensemble possession apply -> 2 real B-only rows reading the stamped lines, re-run no-op -> no audition, recovery ensemble -> both rows as A/B pairs with the REAL stored takes as A sides (old plain state named), solo apply stays audition-free, partial skip -> 1 row + the skip report, suggest ensemble chain carries the audition, Ep7 baseline re-asserted; cleanup removes fixture episode, character, states, take and audition WAVs
- scripts/inject-audition-trace.ts: ensemble mode injects a trace carrying a 2-row ensembleAudition (one A/B row, one B-only, one skipped) for the console UI pass; clean removes both demo tags

Verified E2E (scripts/e2e-ensemble-audition.ts, steps plan + tool, ALL PASS): plan 7 checks; tool 25 checks including four real TTS renders (2 fixture takes + 4 audition reads), B-only and A/B row shapes, pitch hints on the reads, solo path untouched, chain riding, Ep7 untouched (0 fresh / 0 stale / 1 unrendered), restore clean
- Regression: e2e-ensemble-arcs (plan + tool), e2e-ensembles-versions-playback (plan + api + tool), e2e-arc-templates (plan + tool), e2e-state-arc (plan + tool), e2e-template-registry (plan + api + tool), e2e-studio-library (api + tool), e2e-ab-audition (tool) ALL PASS (one transient TTS 429 window failed 3 api checks mid-run; green after cooldown)
- Verified E2E (agent-browser): ruler - the Battle-damaged bar shows the ▶ chip (title "Play this arc's 1 stored take in story order"), click flips it to stop, natural end resets it, stop mid-play resets it, the chip also renders in focus mode (iter24-ruler-chip.png, iter24-ruler-focus-chip.png); template dialog - overflow fixed (scrollWidth 510 = clientWidth), probe v2 selected, history panel's diff toggle opens the side-by-side (v1 auto 25/state 50/auto 25 vs v2 auto 20/state 60/auto 20) with "What moved": auto run 25% -> 20% (-5), state run 50% -> 60% (+10) x2 (iter24-template-diff.png); DSH console - injected ensemble trace renders the teal ENSEMBLE AUDITION block: Lin Yue row with current + proposed players and A/B hint, Chen Hao B-only row, skipped report, play sequence flips to stop and resets (iter24-console-ensemble.png); console clean, 0 page errors; fixtures restored (Ep8 S1 dialogue byte-identical, probe template + injected message removed)
- Note: an earlier session's work on this scope had been committed under two UUID-titled commits; this iteration's fixes (e2e null-safety, ruler audioCues pass-through, dialog strip wrap) were squashed with them into ONE proper Iteration 24 commit before pushing

Stage Summary:
- GitHub main pushed with this iteration
- Landing an ensemble arc is now a HEARABLE moment: the same call that stamps several speakers renders each engaged speaker's proposed read (state voice, register, hints, exact stamped line) as A/B rows in the trace, with a sequence player - DSH points the creator at them in the same turn, before any re-render
- Saved templates can be diffed across versions inside the dialog: any replaced shape opens side-by-side against the current one with a human "What moved" list, so a migration note never has to carry the whole story
- The season ruler auditions arcs directly: every bar with rendered carries a play chip (season and focus modes), takes play in story order, and an ensemble member plays the whole merged beat
- Next horizons: arc playback from the DSH reply itself (playable arc chips in the console), audition history per state (past proposed reads kept for comparison), template diffing across SCOPES (studio vs production fork)

---
Task ID: 25
Agent: Super Z (main agent)
Task: Iteration 25 - playable arc chips in the DSH reply, per-state audition history, and cross-scope template diffing (user: "awesome, now proceed with playable arc chips in the DSH reply, per-state audition history, and cross-scope template diffing")

Work Log:
- prisma/schema.prisma: NEW model StateAudition {stateId, characterId, projectId, url, text, source, voiceId, deliveryId, speed, pitch, durationMs} - every auditioned read of ONE development state, kept for comparison; CharacterState.auditions + Character.stateAuditions back-relations; both FKs cascade (deleting a state or character deletes its history); db push + client regen + clean dev-server restart
- src/lib/types.ts: NEW ArcPlaybackChip {episodeId, episodeNumber, label, ensemble, spans:[{speakerKey, state, shotIds}]}; TraceAction.arcPlayback rides next to audition/ensembleAudition
- src/lib/dsh/tools.ts: arcChip()/ensembleArcChip() helpers; applyResolvedTemplate attaches a single-span chip (speaker + full state label + the range-ordered stamped shot ids), applyEnsembleTemplate tracks PER-SPEAKER stamped shot ids (new EnsembleOutcome.shotIds) and attaches one merged chip for the whole beat, set_state_arc attaches a chip when a state was stamped (cleared arcs attach nothing); every chip path adds a result sentence telling DSH to point the creator at it; suggest_arc_template's chained apply (ensemble AND single chains) rides applyRes.arcPlayback onto its own result
- src/lib/dsh/orchestrator.ts: arcPlayback rides the trace step next to audition/ensembleAudition
- src/lib/ai/audition.ts: NEW saveAuditionFile (TIMESTAMPED file name per render - a history row must never mutate into a later read) + recordStateAudition (best-effort row write + prune to the latest 12 per state, unlinking pruned files); renderVariantAudition now writes unique files and records EVERY DSH audition (bind + ensemble applies) into the state's history
- src/app/api/voice-auditions/route.ts: STATE auditions are now recorded - single branch saves the wav (prefix "state") + writes a row + returns historyUrl; ensemble branch rows with a stateId record too (characterId captured per entry); plain voice auditions (no state) stay throwaway
- src/app/api/state-auditions/route.ts (NEW): GET ?stateId= returns the state block + newest-first rows (cap 12); DELETE ?id= removes a row and unlinks its wav
- src/app/api/episodes/[id]/arc-playback/route.ts (NEW): the playback feed one episode serves - shots in story order (scene then shot) with dialogue + VOICE cues only, exactly the ArcPlaybackShot shape the take collectors want
- src/lib/api-client.ts: StateAuditionRow/StateAuditionsResponse/ArcPlaybackFeed types + api.stateAuditions/deleteStateAudition/arcPlayback; AuditionResult.historyUrl
- src/components/views/dsh-console.tsx: ArcPlaybackBlock (violet) - fetches the feed on mount (useQuery, 60s staleTime), collects the arc's STORED takes with the same buildArcTakes/mergeArcTakes chain the ruler uses (ensemble chips merge every span into one story-order queue), renders a "play arc · N" chip + a take-count line + a Sc/S queue list; one token-guarded sequential player, stop/new play/unmount cancel
- src/components/views/artists-dialog.tsx: StateAuditionHistory (per selected state) - newest-first rows with play (real wav), voice/register/pace/pitch/duration readout, the line, date, per-row delete; history toggle button in the state try row (disabled until a state is picked); the board's own state try bumps a refresh counter so an open list refetches after a new read lands
- src/components/views/arc-template-dialog.tsx: CROSS-SCOPE DIFF section - "compare two shapes" opens two pickers listing EVERY shape (built-ins + production + studio, with scope tags and versions); defaults to the selected saved template vs its same-name twin in the other scope when one exists; side-by-side ShapeBars + mono readouts + the same "What moved" list from diffTemplateShapes ("Shapes match exactly - the difference is only where they live" fallback)
- src/lib/dsh/prompts.ts: rule 7 extended - every landed arc attaches a PLAYABLE ARC CHIP that always plays the OLD stored takes in story order, while the audition rows play the NEW proposed reads; DSH points the creator at the right one
- scripts/e2e-arc-chips-history.ts (NEW): plan (cross-scope fork diff, buildArcTakes span/label filtering + story order + url-less skips, mergeArcTakes interleaving) + tool on the Ep12/Sc32 fixture: ensemble apply attaches the merged chip (per-speaker spans over the stamped shots) AND records both audition rows into the states' history (timestamped arc- files verified on disk), set_state_arc attaches a single-span chip over all three Lin shots (cleared arc attaches nothing), the suggest single chain rides the chip but records nothing, the feed endpoint serves the ordered shots + cues (unknown episode 404), the board's state audition returns historyUrl and lands newest-first, GET/DELETE round trip (DELETE unlinks the file), Ep7 baseline re-asserted; cleanup unlinks every recorded audition file before cascading
- scripts/browser-fixtures-iter25.ts (NEW): seed|clean browser fixtures without TTS - fixture episode whose VOICE cue points at an EXISTING audition wav, injected DSH message carrying a playable chip, two history rows on the REAL Battle-damaged state, and a "browser fork" production+studio template pair
- scripts/e2e-ensemble-audition.ts: cleanup updated for the timestamped audition files (collects recorded urls before the cascade)

Verified E2E (scripts/e2e-arc-chips-history.ts, steps plan + tool, ALL PASS): plan 7 checks; tool 31 checks including the merged ensemble chip spans, the single chip, the chain riding, 4 TTS renders (2 fixture takes + ensemble audition rows + 1 board state audition), history recording/pruning round trip, feed shape, Ep7 untouched (0 fresh / 0 stale / 1 unrendered), restore clean
- Regression: e2e-ensemble-arcs (plan + tool), e2e-ensemble-audition (plan + tool), e2e-ensembles-versions-playback (plan + tool + api), e2e-arc-templates (plan + tool), e2e-state-arc (plan + tool), e2e-template-registry (plan + api + tool), e2e-studio-library (api + tool), e2e-ab-audition (tool + cleanup) ALL PASS (two transient TTS 429 windows mid-run; green after cooldowns)
- Verified E2E (agent-browser): DSH console - injected chip trace renders the violet PLAYABLE ARC block ("play arc · 1" found the take through the feed), the queue lists Sc32 · S001 Lin Yue "The storm bends...", click flips the chip to stop, stop resets it (iter25-console-chip.png); casting board - Battle-damaged state picked, history toggle opens "AUDITION HISTORY - 2 RECORDED READS (NEWEST FIRST)" with voice/pace/pitch/duration/line/date rows, playing a row shows the stop control (iter25-board-history.png); template dialog - "compare two shapes" opens the cross pickers, production fork vs studio original renders both bars (auto 20/state 60/auto 20 vs auto 25/state 50/auto 25) with "What moved": auto run 20% -> 25% (+5), state run 60% -> 50% (-10) x2, dialog overflow-free (scrollWidth 510 = clientWidth) (iter25-cross-scope-diff.png); 0 page errors, console clean; fixtures removed (chip message, template pair, history rows, fixture episode)

Stage Summary:
- GitHub main pushed with this iteration
- The DSH reply is now a player: every landed arc (set_state_arc, apply_arc_template single or ensemble, chained suggest applies) carries a play chip in the trace that streams the arc's STORED takes in story order - ensemble beats merge every speaker's span into one queue - so hearing the current cut never requires leaving the conversation
- A state's auditions have a memory: every proposed read (DSH binds, ensemble applies, board state tries) lands in a per-state history with its performance snapshot, replayable and removable from the casting board, pruned to the latest 12, and every render now owns a timestamped file so no row ever mutates into a later read
- Templates diff across scopes: any two shapes (production fork vs studio original, saved vs built-in, studio vs studio) open side by side with the same human "What moved" list as version diffs - a fork's drift is readable at a glance
- Next horizons: audition history A/B chaining (compare any two recorded reads back to back), the arc chip inside the panel inspector clusters, template usage counts per scope (which shapes each show actually applies)

---
Task ID: 26
Agent: Super Z (main agent)
Task: A/B chaining in the audition history (any two recorded reads compared back to back), the playable arc chip inside the panel-inspector ensemble clusters, per-scope template usage counts, README refresh with a roadmap of what the plan has not achieved yet and how to close it

Work Log:
- prisma/schema.prisma: ArcTemplate gained usageCount (default 0) + lastUsedAt; db push + dev server restart so the running process picked up the regenerated client
- src/lib/comic/audition-chain.ts (NEW): pure pick/swap/clear semantics for the A/B chain pair - picking the row that holds the other side SWAPS the slots, so A and B always stay two different reads; re-picking a side unpicks it
- src/lib/comic/arc-templates.ts: formatTemplateUsage helper ("used 3x . last 09-24 14:05" / "never applied")
- src/app/api/arc-templates/[id]/use/route.ts (NEW): POST records one line-stamping application (usageCount increment + lastUsedAt), 404 on unknown ids; GET /api/arc-templates now serves usageCount + lastUsedAt per row
- src/lib/dsh/tools.ts: recordTemplateUse helper (built-ins are code and never counted; ONE batch = ONE use for ensembles) wired into applyResolvedTemplate (single + chained suggest applies) and applyEnsembleTemplate, only when lines actually moved - a no-op re-apply never counts; arcTemplateRegistry entries now carry usage and the suggest Registry line shows "used N times" per saved shape
- src/lib/dsh/prompts.ts: rule 7 extended with the per-scope usage-count doctrine (point at the numbers instead of guessing which shape the show favors)
- src/components/views/artists-dialog.tsx: StateAuditionHistory grew the A/B chain - per-row A/B pick chips (amber when picked), an a/b chain bar (compare A . B play/stop, side readout with voice + durations, swap, clear), the playing side's row highlighted, picks auto-drop when their row is deleted; parent playHistoryChain streams the two wavs back to back token-guarded (keys chain:<stateId>:A/B drive the side indicator); FIXED a latent sequencer bug: a clip fires pause AND ended at natural end, and the stale second event used to clear the NEXT clip's playing key - playClipAwait + playUrlAwait now settle once and only clear when still the current audio
- src/components/views/panel-inspector-dialog.tsx: the ensemble cluster now carries the playable arc chip - play beat . N chip (stop while playing) plus the ArcTakeQueue: the merged story-order take list (Sc . S . speaker . line . duration) under the cluster header, the same readout the DSH reply's chip shows
- src/components/views/arc-template-dialog.tsx: per-scope usage counts everywhere - shape select options ("browser fork . used 3x"), an amber usage badge in the selected saved template's meta row (formatTemplateUsage), cross-scope diff picker options; the dialog's apply() pings POST /use when a SAVED template stamped lines (stampedLines > 0) and updates the local list so the badge is fresh without a refetch
- src/lib/api-client.ts: ArcTemplateRow.usageCount/lastUsedAt + api.useArcTemplate
- scripts/e2e-ab-chain-usage.ts (NEW): plan (pickChainSide pick/swap/unpick/clear semantics + formatTemplateUsage readouts) + tool (usage fields ride the GET at 0, single apply records "now at 1 apply on its scope", no-op re-apply records NOTHING and keeps the count, ensemble batch records ONE use, suggest registry line carries "used 2x", POST /use increments to 3 and 404s unknown ids, history GET serves the A/B pair newest first, Ep7 baseline) + idempotent cleanup step
- scripts/browser-fixtures-iter26.ts (NEW): seed|clean browser fixtures without TTS - Ep14 ensemble beat (Lin + Rival Wei sharing ONE arcBatch, REAL wav-backed cues), two history rows on the real Battle-damaged state, and a browser fork template pair with the production row at usageCount 3
- README.md: feature map refreshed for iterations 22-26 (ensemble applies, arc chips in the reply, audition history + A/B chain, cluster playback, versioning, cross-scope diff, usage counts), Status rewritten, and a Roadmap section added: what the plan has NOT achieved yet (real animated shots, art-aware continuity, cross-turn DSH plans, voice scale/cloning, anchor-consistency evaluation, multi-user studio, distribution integrations) and the concrete next step for each

Verified E2E (scripts/e2e-ab-chain-usage.ts, steps plan + tool, ALL PASS): plan 13 checks; tool 14 checks including the usage round trip (0 -> 1 -> no-op stays 1 -> ensemble 2 -> dialog /use 3 -> 404), the suggest registry "used 2x", the newest-first history pair, Ep7 untouched (0 fresh / 0 stale / 1 unrendered), fixture removed, audition files unlinked
- Regression: e2e-arc-chips-history (plan + tool), e2e-template-registry (plan + api + tool), e2e-arc-templates (plan + tool), e2e-state-arc (plan + tool), e2e-studio-library (api + tool), e2e-ensemble-arcs (plan + tool), e2e-ensemble-audition (plan + tool), e2e-ensembles-versions-playback (plan + tool + api), e2e-ab-audition (tool) ALL PASS (one TTS 429 window mid-run; green after a 4-minute cooldown)
- Verified E2E (agent-browser): casting board - Battle-damaged state picked, history open, A/B chips on every row, picking two rows raised the a/b chain bar, compare played "stop . A" then "stop . B" then reset (DOM probed at 100ms: 0.0s stop A / 3.9s stop B / 6.8s idle), the playing side's row highlighted, screenshots saved (iter26-board-ab-chain.png); panel inspector on the Ep14 ensemble shot - cluster shows "2 speakers in parallel . 2 arcs on this shot", the play beat . 2 chip flips to stop, the queue lists PLAYABLE BEAT - 2 TAKES MERGED IN STORY ORDER with Sc34 . S001 Lin Yue and Sc34 . S002 Rival Wei rows (iter26-cluster-chip.png); template dialog - shape select shows "browser fork . used 3x", the meta badge reads "used 3x . last <date>", cross-scope diff pickers carry the usage, What moved renders the fork deltas (iter26-usage-counts.png); 0 page errors, console clean; fixtures removed (episode, template pair, history rows)

Stage Summary:
- GitHub main pushed with this iteration
- The audition history is now a comparison instrument: any two recorded reads of a state chain back to back on demand, and the fix to the shared clip sequencer keeps every multi-clip player (chain, ensemble sequence) honest about which clip is on
- The playable arc chip now lives where beats are inspected: an ensemble cluster in the panel inspector shows its merged story-order queue before you press play, matching the DSH reply and the ruler bars
- Templates carry their own evidence: every saved shape counts the applies that actually stamped lines, per scope, visible in the shape list, the meta badge, the cross-scope pickers and DSH's registry line
- The README now states the plan's open gaps (animated shots, art-aware continuity, cross-turn plans, voice scale, consistency scoring, multi-user, distribution) and the next concrete step for each
- Next horizons: export-time A/B adoption (a chained B read can be promoted into the state's standing performance), per-template usage breakdown inside DSH context, and usage-weighted template ranking in suggest_arc_template
---
Task ID: 27
Agent: Super Z (main agent)
Task: Real animated shots, not stills - bridge renders sequenced clips per shot's camera grammar, muxed with stems at export, plus installing Blender headless (user: "lets work on this: Real animated shots, not stills → bridge renders sequenced clips per shot's camera grammar, muxed with stems at export, also instead blender (headless)" then "i mean, also install blender headless")

Work Log:
- Installed Blender headless: apt blocked (no passwordless sudo), so the official portable build (4.3.2-linux-x64) was downloaded from download.blender.org, extracted to /home/z/blender-4.3.2-linux-x64 and symlinked onto PATH; verified headless Cycles-CPU rendering works in the container (0.7s/frame at 320x180)
- Rebuilt bridges/blender/animeos_bridge.py as v3 (worker-pool architecture): the v2 in-process server starved its HTTP threads during bpy renders (the render operator holds the GIL for whole frames, /progress froze mid-job), so the server no longer renders - it spawns ONE FRESH headless Blender worker subprocess per job (`--worker --job <state-file>`); the worker builds a stand-in 3D scene (rocky terrace, ico-sphere rocks, plinth, emissive blade whose strength follows energyIntensity, sun/rim/energy lights, fog-colored world), drives a CAMERA RIG through the movement grammar frame by frame (orbit sweep, dolly radius, pan yaw, tracking lateral, crane descent, tilt, static breathing - shot-type framing distances + lens mm per the existing table), strobes the sun inside deterministic lightning windows (fnv1a+mulberry32, same DNA as the TS planner), renders the frame range with Cycles CPU (PREVIEW 512px/10 samples/0 bounces, FINAL 1280px/48 samples/denoise) and encodes h264 via system ffmpeg with Blender's own FFMPEG writer as fallback; per-frame progress streams through the state file; the server keeps /status /progress /render /ping for workstation attach and works under plain python3 (bpy NOT required on the server)
- src/lib/bridge/blender.ts rewritten: driver sources are now env-host HTTP attach (ANIMEOS_BLENDER_HOST) and LOCAL WORKERS - each job's worker is spawned detached by Node itself (same state-file protocol, no middleman server to babysit); localBlenderBin probes ANIMEOS_BLENDER_BIN + ~/.venv/bin/blender + known install paths; one 3D worker at a time (localWorkers counter); pollLocalJob reads the state file; localJobStale fails jobs whose worker went quiet > 15min; BridgeStatus mode union is now LIVE_BLENDER | MOTION | SIMULATOR with source env|local
- src/lib/bridge/motion.ts (NEW) - the built-in MOTION engine: planCameraProgram (PURE, deterministic per jobId) maps movement (DOLLY_IN/PUSH_IN/ZOOM_IN, DOLLY_OUT/PULL_OUT, PAN, TRACKING, ORBIT, CRANE, TILT_UP/DOWN, STATIC + unknown→STATIC) onto zoom/pan keyframes, shot-type base framing (ESTABLISHING 1.0 ... EXTREME_CLOSEUP 1.3), scene cameraDistance, lens character (>=80mm tele: stronger vignette + tighter frame; <=35mm wide: the reverse), lighting grade (night/fire/moon), fog veil alpha + desaturation, deterministic lightning flash windows (1-3, seeded) and teal energy pulses; renderShotClip runs ffmpeg: key art (validated /panels/ path) pre-scaled to a 1.35x overscan plate and pushed through zoompan with baked expressions (or a seeded procedural gradients plate + drawtext SHOT chip when no art), then eq grade, fog drawbox, flash/pulse drawboxes with between(t) enables, vignette, fades, yuv420p libx264 veryfast at project fps (PREVIEW capped 960, FINAL 1920); -progress pipe:1 drives real job progress; a 2-slot semaphore serializes ffmpeg; probeMedia/ffprobe helper exported
- src/lib/engine/render.ts: driver chain BLENDER (env host) > BLENDER_LOCAL (spawned worker) > MOTION (ffmpeg) > SIMULATOR (wall-clock, now only when even ffmpeg is missing); MOTION jobs update themselves from ffmpeg progress and land outputUrl + "Motion clip ready - <program note>"; BLENDER_LOCAL ticks read the state file, fail on stale/missing clip, set outputUrl on the mp4; env-host jobs persist mp4_base64 (animated mode) with the legacy png path kept; RenderJob.outputUrl added to the schema (db push + client regen + dev restart)
- src/lib/comic/cut.ts (NEW) - the episode cut builder: planCut (pure) stacks shots back-to-back on one timeline and retimes every AudioCue onto it (clamped inside its shot slot); buildEpisodeCut collects each shot's latest clip (FINAL preferred), renders up to 6 missing shots inline via the MOTION engine, normalizes + concats the clips (scale/pad/fps/setsar + concat filter) into a silent master, synthesizes the stems SERVER-SIDE with one ffmpeg filter_complex (VOICE cues mix their real TTS takes from /voices/, unrendered VOICE gets an aevalsrc formant blip, SFX bandpass-shaped anoisesrc bursts with impact/bright label timbres, BGM detuned sine pads through lowpass, AMBIENCE pink-noise beds - all adelayed to their slot, amixed, limited), muxes video + stems (aac, -shortest) into public/renders/cuts/{slug}.mp4 with a JSON sidecar manifest (per-shot timeline, movements, audio kinds, realTakes vs synthesized, warnings) and a ProductionEvent
- src/app/api/episodes/[id]/cut/route.ts (NEW): POST muxes the cut (maxDuration 300); api-client: RenderJobRow.outputUrl, BridgeStatusInfo new mode/source union, EpisodeCutResult, api.exportCut
- src/components/views/render-view.tsx: EngineDriverCard speaks the new world (LIVE BLENDER emerald for env+local, MOTION ENGINE teal, SIMULATOR neutral; local source shows "headless worker pool"); job cards carry a video player whenever outputUrl exists and BLENDER/MOTION/SIM driver chips with per-driver tooltips; NEW EpisodeCutCard - episode picker, PREVIEW/FINAL toggle, export button with busy state, result video player + download link + meta (canvas, duration, shots, stem cues, inline-rendered count) + manifest link + warnings
- DSH: render_shot's result names the engine (headless Blender sequence worker / built-in MOTION engine) and the clip contract ("will finish as a playable animated clip following the shot's camera grammar (MOVEMENT, SHOT_TYPE)"); prompts.ts doctrine now says renders are CLIPS not stills and points at camera moves; README rewritten (render pipeline feature row, Render engines section replacing Attach-a-live-Blender, try-the-loop steps 1-2, status paragraph, roadmap item 1 graduated to "Character motion inside the frame" with the img2vid/keletal next steps)
- scripts/e2e-motion-cut.ts (NEW): plan (24 pure checks - movement profiles and aliases, shot-type zoom ordering, lens vignette/zoom character, lightning windows bounded + deterministic, fog veil, frames=duration*fps, PREVIEW/FINAL caps, describeProgram, planCut slot math + cue retiming/clamp + voice path resolution) + tool (fixture production at 1280x720: shot 1 renders BLENDER_LOCAL with ffprobe-verified h264 512px 2.00s clip, remaining shots end with clips, cut export succeeds with video+audio streams, 5.40s ~= the 5.6s shot sum, 960x540 canvas, manifest sidecar, "Episode cut muxed" ProductionEvent, render_shot names the engine + DOLLY_IN, /api/bridge reports the new driver world, Ep7 baseline untouched)
- Fixed along the way: drawtext y=ih is invalid (ffmpeg -22 with no packets) - HUD chip now uses main_h; e2e expectations corrected (PUSH_IN is its own key sharing the dolly profile, larger vignette angle = stronger, cue retimes are slot-relative); one dead-code cleanup in motion.ts
- scripts/browser-fixtures-iter27.ts (NEW): seed|clean - fixture production whose 3 shots (PAN/DOLLY_IN/STATIC, SFX + real-TTS VOICE + AMBIENCE cues) are RENDERED before the script returns; clean deletes the project and sweeps clip/cut artifacts

Verified E2E (scripts/e2e-motion-cut.ts, steps plan + tool, ALL PASS): plan 24 checks; tool 15 checks including the BLENDER_LOCAL clip (h264, exact 2.00s), 3/3 clips, the muxed cut (video+audio, 5.40s vs 5.6s sum, canvas fit, manifest, history event), render_shot clip language, bridge status, Ep7 untouched
- Regression: e2e-state-arc (plan + tool), e2e-arc-templates (plan + tool), e2e-template-registry (tool), e2e-ensemble-arcs (tool), e2e-ab-chain-usage (tool) ALL PASS
- Verified E2E (agent-browser): switched to the fixture production - Engine driver card reads LIVE BLENDER "Headless Blender 4.3.2 at /home/z/.venv/bin/blender - one 3D sequence worker at a time, overflow renders fall to the MOTION engine", three job cards show MOTION/MOTION/BLENDER chips with inline video players, in-page play() runs the clip with a real duration (1.58s STATIC shot); Episode cut export through the UI produced e2e-browser-motion-ep01-*.mp4 (h264+aac, 5.4s, 960x540) whose manifest counts 3 cues with realTakes 1 (the real TTS take renders into the mix, max_volume -3.6dB), meta row shows "960x540 @ 24fps · 5.4s · 3 shots · 3 stem cues · manifest" (iter27-driver-clips.png, iter27-cut-result.png, iter27-queue-clips.png); 0 page errors, console clean; fixtures removed (project + 5 artifacts), renders dir clean, Ep7 voice baseline holds (1 cue, unrendered)
- Note: Blender now lives at /home/z/blender-4.3.2-linux-x64 (symlinked into ~/.venv/bin/blender); the driver chain auto-detects it - ANIMEOS_BLENDER_BIN can repoint it, and env-host attach still works for workstation Blenders

Stage Summary:
- GitHub main pushed with this iteration
- Renders are no longer stills: every job finishes as a playable sequenced clip that follows the shot's camera grammar, rendered either by a real headless Blender 4.3.2 worker (installed on this box, one Cycles render at a time) or by the built-in ffmpeg MOTION engine over the shot's key art - both land an mp4 on the job card with per-frame progress
- Export is a deliverable: one POST (or one button) turns an episode into a single muxed mp4 - clips concatenated in story order, stems synthesized server-side with real TTS speech mixed in, manifest sidecar with the full shot/cue timeline
- The bridge add-on learned to animate (v3 worker pool): camera rig per movement grammar, deterministic lightning strobes, energy-driven emission, fog-colored worlds - and it can no longer deadlock its own HTTP server
- Next horizons: character motion inside the frame (img2vid / skeletal stand-ins), art-aware continuity (VLM vs anchor), cross-turn DSH plans, voice scale/cloning, anchor-consistency scoring, multi-user studio, distribution integrations

---
Task ID: 28
Agent: Super Z (main agent)
Task: Character motion inside the frame - shot pose vocabulary + Blender skeletal stand-in blocking + img2vid provider slot (user: "proceed with the next" - the next roadmap item after iteration 27's real animated shots)

Work Log:
- src/lib/animation/poses.ts (NEW): the 13-pose shot vocabulary (STANCE, WALK, LUNGE, SLASH, CAST, DRAW, BLOCK, LEAP, CROUCH, FALL, RISE, BOW, POINT) with per-pose 12-channel joint tables (root x/y, spine, head, shoulders/elbows, hips/knees), forgiving aliases (ATTACK->LUNGE, idle->STANCE, stand-up->RISE), eased cubic joint interpolation with STANCE fallback so a bad chip can never break a render, poseChip/describePosePair/hasPoseProgram wording helpers
- prisma Shot model: poseStart/poseEnd columns (db push + client regen + dev restart); POST/PATCH /api/shots validate them - unknown poses 400 with the valid list, aliases normalize, empty strings clear (fixed a pre-existing latent bug along the way: the single-shot PATCH built its field data OUTSIDE the try, so validation errors surfaced as 500 HTML instead of 400 JSON)
- bridges/blender/animeos_bridge.py v3.1: the worker now builds a SKELETAL STAND-IN FIGURE when the shot carries poses - primitives parented under joint empties (pelvis/spine/head/shoulders/elbows/hands/hips/knees) with the emissive blade parented to the right hand; per frame the joints interpolate between poseStart and poseEnd with eased timing, WALK endpoints add a procedural stride (hip/shoulder swing, counter-swinging arms, root bob), and the figure renders at prop scale (0.45x, matching the existing plinth/blade scene sizing) so every shot-type framing keeps working; the camera rig gained pose awareness - it lowers its target onto the body and backs off by the pose's root travel so a LUNGE toward the lens stays framed. Legacy plinth + floating blade scene is untouched for pose-less shots. Along the way fixed a REAL pre-existing defect: the startup Cube/Light/Camera were never purged, so every v3 render had a 2m default cube sitting on the origin swallowing the set (verified frame-by-frame)
- src/lib/bridge/img2vid.ts (NEW): the interpolation-model provider slot from the roadmap - ANIMEOS_IMG2VID_HOST points at any service speaking the tiny jobs protocol (POST /jobs with imageUrl + pose pair + timing, GET /jobs/{id} until done + videoUrl); the client downloads the finished clip into public/renders/{jobId}.mp4 (same output contract as the Blender bridge); env read lazily so operators/tests can set it after import; unset (default) the slot is invisible
- src/lib/engine/render.ts: driver chain is now BLENDER > BLENDER_LOCAL > IMG2VID (only when configured AND the shot carries a pose program; hero shots route to the provider first) > MOTION (pose pairs play as a blocking approximation) > SIMULATOR; IMG2VID jobs tick via the provider poll with MOTION take-over if the provider vanishes mid-job; both bridge payloads (env host + local worker) carry poseStart/poseEnd
- src/lib/bridge/motion.ts: CameraProgram carries poseChipText; pose pairs add a deterministic impact pulse at the end pose and describeProgram says "poses A -> B (blocking)"; renderShotClip passes poses through
- DSH: create_shot accepts poseStart/poseEnd (unknown poses ERROR with the valid list), NEW set_shot_poses tool lands/clears a shot's pair with the performance gloss in the result, render_shot names the engine per pose state ("headless Blender sequence worker (Cycles) with the skeletal stand-in performing STANCE -> LUNGE", "img2vid interpolation provider", "MOTION engine (poses play as a blocking approximation)"); prompts doctrine updated (renders are CLIPS with camera moves AND a pose beat)
- UI: panel inspector gained a Motion poses section (start/end selects over the vocabulary with a live pose chip, saved through patchShot - save button no longer requires a roster), pose chips (teal, next to the movement chip) in Comic Mode panel cards (both manhua/manga and webtoon variants) and Story & Scenes shot rows, render queue job cards carry an IMG2VID driver chip (sky) alongside BLENDER/MOTION with updated tooltips, the Engine driver card mentions the pose program and shows the ANIMEOS_IMG2VID_HOST line when a provider is attached; api-client: ShotRow.poseStart/poseEnd, BridgeStatusInfo.img2vid
- scripts/e2e-character-motion.ts (NEW): plan (20 pure checks - vocabulary/aliases, eased interpolation endpoints + monotonicity, chips/wording, MOTION pose program + impact pulse, img2vid slot default-off) + tool (mock img2vid provider on 127.0.0.1:8291 exercising the real client protocol: submit accepts the STANCE->LUNGE program, rejects a bogus one, poll reaches done, the clip downloads into public/renders and ffprobes as h264; live fixture: shots API validation (400 unknown / alias normalization / clearing), DSH create_shot + set_shot_poses + render_shot pose language, a STANCE->LUNGE shot renders BLENDER_LOCAL with a 2.00s h264 clip, the MOTION engine renders CAST->BOW with blocking wording, bridge status carries the img2vid slot) + scripts/browser-fixtures-iter28.ts (seed|clean: 3-shot fixture with STANCE->LUNGE, PAN camera-only and a CAST hold, rendered before returning)
- Debugging journey (worth remembering): the first browser-visible renders showed a giant hexagonal column - three red herrings peeled back (figure scale, plinth placement, camera distance) before the real cause: the worker overwrites its job file with progress state, so re-running a worker against a used job file feeds it an EMPTY payload (legacy branch, default MEDIUM framing) - and separately, Blender's startup Cube had never been purged from the v3 scene so a 2m default box sat on the origin swallowing every set; both fixed and verified frame-by-frame (STANCE stands, LUNGE performs toward the lens and stays framed, WALK strides)
- README: render pipeline feature row + Render engines section (stand-in figure wording, MOTION blocking approximation, new img2vid provider slot bullet), try-the-loop step 1 mentions the pose program, roadmap item 1 graduated from "Character motion inside the frame" to "Performance-grade character motion" (learned interpolation, faces/hands, physics) with the next steps spelled out

Verified E2E (scripts/e2e-character-motion.ts, steps plan + tool, ALL PASS): plan 20 checks; tool 22 checks including the mock-provider round trip (submit/poll/download, h264 clip on disk), shots API validation, DSH pose language, the BLENDER_LOCAL stand-in clip (h264, exact 2.00s), the MOTION blocking clip (CAST -> BOW wording), bridge slot reporting, Ep7 baseline untouched
- Regression: e2e-motion-cut (tool) ALL PASS - the iteration 27 cut export pipeline is unaffected (3 clips, muxed 5.40s cut, manifest, Ep7 voice baseline)
- Verified E2E (agent-browser): switched to the fixture production - Render Queue shows LIVE BLENDER + 3 inline video players (1 BLENDER chip + 2 MOTION chips), in-page play() runs the pose clip with a real 2.00s duration (iter28-queue-clips.png); Comic Mode shows teal pose chips STANCE -> LUNGE and CAST beside the PAN movement chip (iter28-pose-chips.png); the panel inspector's Motion poses section opens with STANCE/LUNGE selected (14 options each), changing the end pose to SLASH and saving updated the panel chip live, then restored to LUNGE (iter28-inspector-poses.png); Story & Scenes shot rows show the same chips; 0 page errors, console clean; fixtures removed (project + 4 artifacts), renders dir clean
- Note: the img2vid slot ships CLIENT-side only in this environment (no provider is configured); the E2E mock provider proves the protocol end to end, and any real service can attach via ANIMEOS_IMG2VID_HOST without further code

Stage Summary:
- GitHub main pushed with this iteration
- Characters now move INSIDE the frame: a shot can carry a start and end pose from a 13-pose vocabulary and every driver performs it - the headless Blender worker articulates a jointed stand-in (with the emissive blade in hand) through eased lunges, casts and walk cycles while the rig follows the subject, an img2vid provider slot is ready for real interpolation models, and the MOTION engine plays an honest blocking approximation
- Poses are first-class production data: validated through the shots API, authorable from the panel inspector and by DSH (create_shot / set_shot_poses), spoken in render_shot results, and visible as chips wherever the shot grammar shows
- The render pipeline debug also fixed a latent scene defect: Blender's startup cube no longer swallows the stand-in set, and worker job files are single-use (state overwrite) which the pipeline's one-worker-per-job design already respects
- Next horizons: wire a real img2vid provider into ANIMEOS_IMG2VID_HOST, per-state pose presets (a character's development state changes HOW a pose is performed), performance-grade motion (faces, hands, secondary motion), art-aware continuity (VLM vs anchor), cross-turn DSH plans, voice scale/cloning, anchor-consistency scoring, multi-user studio, distribution integrations

---
Task ID: 29
Agent: Super Z (main agent)
Task: Iteration 29 - wire a real img2vid provider, per-state pose presets, and art-aware continuity (user: "Proceed with this: wire a real img2vid provider, per-state pose presets, and art-aware continuity")

Work Log:
- Schema: RenderJob.providerTaskId (async video-model task id), CharacterState.poseStart/poseEnd (the state's pose preset), Shot.artGeneratedAt (art-staleness anchor), Character.modelSheetAt (anchor-staleness anchor); db push + client regen (one dev-server restart required - the stale client rejected the new columns mid-E2E)
- REAL img2vid provider (src/lib/bridge/img2vid.ts): a second provider behind the existing driver contract - the built-in z.ai async video-generation model. img2vidProvider() resolves host > zai > off (ANIMEOS_IMG2VID_HOST wins when set; zai is the default; ANIMEOS_IMG2VID=off disables); buildImg2VidPrompt compiles the pose beat (POSE_GLOSS wording for the start/end pair, held-pose and ambient fallbacks), the camera program (movement/shotType gloss) and an identity lock on the key art's character; resolveImg2VidImageUrl inlines local key art as a data URL (no public host needed, 8MB cap, absolute URLs pass through); submit returns the task id (stored on RenderJob.providerTaskId), poll queries the async result and downloads the finished clip into public/renders/{jobId}.mp4 - same contract as the host protocol and the Blender bridge; img2vidStatus carries the provider, the bridge API and the Engine driver card name it (zai line + ANIMEOS_IMG2VID=off hint)
- Engine wiring (render.ts): the img2vid branch now tries the attached host first, then the z.ai model (both only for shots with a pose program and only when the Blender chain could not take the job); tickRenderJob polls by provider (taskId present = zai) with progress creep while the model works; provider lost mid-job still degrades to MOTION/simulator exactly as before
- Per-state pose presets (src/lib/animation/state-poses.ts): a curated 24-entry library mapping state-label keywords to pose pairs (furious STANCE->LUNGE, grieving STANCE->CROUCH, battle-damaged STANCE->FALL, determined CROUCH->RISE, triumphant CROUCH->LEAP, ...); resolveActiveState picks the episode-effective state (same-episode ties break on createdAt - a state recorded later supersedes); statePosePair resolves explicit pair > library auto
- DSH: create_character_state auto-lands the library preset from the label and reports the body language; NEW set_state_poses tool (34 tools) sets/clears a state's pair with performance gloss + the applyStatePoses handoff in the result; get_production_context lists each state's poses chip; doctrine updated (state changes HOW a pose is performed, img2vid provider is built-in now)
- Shots API: PATCH applyStatePoses action resolves the detected cast (description match), takes each character's episode-active state, and lands its pair (explicit preset first, library fallback, explicit pose fields in the same request win) returning source + previous/now chips or a precise reason
- Art-aware continuity (src/lib/continuity-art.ts + /api/continuity-art): deterministic scan (stale-state: panel art predates the episode-active state; stale-anchor: panel predates the current model-sheet anchor; anchor-missing: featured cast without a sheet) over every shot; VLM deep check compares a shot's panel art against the featured character's canonical model sheet via createVision (strict JSON verdict) and lands ART_DRIFT / ART_VERIFIED continuity events (prior verdicts per shot replaced)
- art.ts: panel generation stamps artGeneratedAt and adds two continuity lines to the prompt - the featured character's start-of-move pose (the frame the motion grows from) and a previous-panel carry-over line (previous shot description + lighting + match-palette instruction, crossing scene boundaries within the episode); model sheet generation stamps modelSheetAt; prompt excerpts returned to clients widened to 1200 chars
- UI: characters view gained a per-state Pose preset row (start/end selects over the vocabulary, a Library button that re-resolves the label preset, and a body-language chip); the panel inspector's Motion poses section gained a Use state poses button that fills both selects from the episode-active state with the source line (client mirror of applyStatePoses); the Continuity view opened an Art-aware continuity panel (scan button, counts as chips, flagged shots with per-item chips and one-click VLM checks, verdict banner) and ART_* events render in the main event list with their own colors; api-client types extended (state poseStart/poseEnd, patchCharacterState poses+auto, BridgeStatusInfo.img2vid.provider)
- Seed: Lin Yue's four states now carry explicit presets (S01 STANCE->WALK, S02 LUNGE->SLASH, S03 STANCE->CAST, Battle-damaged STANCE->FALL) and the live DB was backfilled to match
- Blender headless reinstalled on this box (portable 4.3.2.2 build extracted to /home/z/blender-4.3.2-linux-x64, `blender -b --version` verified) after it went missing between sessions; E2E hid/restored the binary around provider-routing checks (and the re-extract was needed once after the fixture race corrupted the first extraction)
- scripts/e2e-iter29.ts (NEW): plan (26 pure checks - preset library, active-state resolution incl. the tie-break, provider selection host/zai/off, prompt wording, duration clamp, key-art data URLs) + tool (35 live checks - DSH auto-preset + set_state_poses steering/clearing/rejection, applyStatePoses explicit/library/no-cast paths, panel art artGeneratedAt + previous-panel continuity line, scan counts stale-state/stale-anchor/anchor-missing, a REAL vision verdict persisted as ART_DRIFT with drift aspects named by the model, and the REAL z.ai video render end to end: submit -> tick path -> REVIEW -> h264 clip on disk ffprobed at 5.19s; provider-off falls through to MOTION; Ep-cut regression ALL PASS)
- Debugging journey: two assertion misses (pose gloss substrings), one fixture design miss (shot 3 named Chen Hao so the no-cast path needed an empty-terrace shot), one real resolution subtlety (same-episode states tied on episodeNumber so the stale-state scan never saw the newer state - fixed with the createdAt tie-break in both resolvers), one client crash caught by the browser pass (API JSON carries createdAt as a string; resolveActiveState now coerces), and one transient 429 on the video endpoint (submit retries across the window; a prior run had already proven the full z.ai round trip)

Verified E2E (scripts/e2e-iter29.ts, steps plan + tool, ALL PASS): plan 26 checks; tool 35 checks including the real z.ai img2vid round trip (REVIEW, /renders/*.mp4, h264, 5.19s), the vision ART_DRIFT verdict with model-named drift aspects, the stale-art scan, the preset application paths, and the MOTION fallback regression; e2e-motion-cut (iteration 27 cut export) ALL PASS
- Verified E2E (agent-browser): Render Queue - Engine driver card carries the built-in z.ai provider line, 3 inline video players with MOTION chips, in-page play() verified (1.58s clip); Characters - Lin Yue's states show the Pose preset row (Furious = STANCE -> LUNGE selected, body-language chip), Library resolved Grieving (aftermath) to STANCE -> CROUCH and reverting cleared it (iter29-state-poses.png); panel inspector - Use state poses filled STANCE -> CROUCH from "Grieving (after the duel)" with the source line naming character, state and preset note (iter29-use-state-poses.png); Continuity - art scan reported 3 shots checked / 1 stale vs state / 1 stale vs anchor / 1 missing anchors with per-shot chips, and the live VLM check returned ART_VERIFIED ("faithful representation... matching in hair, face, wardrobe, and color palette") which landed as a green event card in the list (iter29-continuity-art.png); Comic Mode pose chips still render on panels (iter29-comic-panels.png); 0 page errors, console clean; fixture removed (project + 5 artifact files), renders/panels/sheets clean
- README: feature rows (34 tools, real provider, state presets, art-aware continuity row), Render engines img2vid bullet rewritten (built-in provider + chain + kill switch), try-the-loop step 1 mentions Use state poses, roadmap items 1 and 2 graduated with new next steps

Stage Summary:
- GitHub main pushed with this iteration
- The img2vid slot now ships with a REAL provider inside: pose-carrying hero shots become real AI video out of the box (key art + pose/camera program in, interpolated clip back into the same queue), with the host protocol, a kill switch, and transparent MOTION degradation still in the chain
- Development states carry body language: the state library lands a preset at creation, DSH and the casting board steer it, and any shot inherits it in one click - a state changes HOW a pose is performed, not just how a line sounds
- The continuity engine now sees the art layer: stale art vs states and anchors, missing anchors, and a vision-model comparison against the canonical sheet, all persisted as continuity events the director can call for itself
- Next horizons: stand-in rig upgrade (faces, hands, secondary motion), universe-facts vision checks with confidence scores feeding the re-render queue, cross-turn DSH plans, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 30
Agent: Super Z (main agent)
Task: Iteration 30 - stand-in rig upgrade (faces/hands) and universe-facts vision checks (user: "awesome, proceed with the stand-in rig upgrade (faces/hands) and universe-facts vision checks")

Work Log:
- bridges/blender/animeos_bridge.py v3.2 (stand-in rig upgrade): a second per-pose channel table POSE_FACE (7 channels: brow, eye, mouth, gripR/L, pointR/L) covers all 13 poses, eased between start/end with the same clock as the joints (lerp_face); the head grew a FACE - emissive eyes under squashable empties (squint with openness, deterministic blink every ~2.6s via blink_openness), brows that mirror their tilt (negative knits angry, positive lifts surprised) plus a slow sinus drift on holds, a mouth slab that opens with effort; both arms end in HANDS - palm + four fingers + thumb under curl pivots at the wrist (hand() parented under RHand/LHand at elbow-local z -0.26; the blade now sits BETWEEN palm and fingers as a gripped prop), finger_curl maps grip 0..1 to 0..78 degrees, the index straightens as point rises (POINT keeps it extended), the thumb half-curls with grip; apply_pose drives all of it per frame plus blade FOLLOW-THROUGH (secondary motion: tilt proportional to the eased swing rate of the right shoulder, clamped) - defensive helpers (normalize_face_row, face_row) mean a bad table entry or unknown pose falls back to STANCE and can never break a render; the worker reports a rig block in its final state (v3.2, face/hands true, 10 fingers, 7 channels) so the pipeline can assert the upgrade shipped
- Pose-aware camera fix found during verification: tight shot types now aim at the HEAD (target z 0.84 on the prop-scaled figure) so CLOSEUP/EXTREME_CLOSEUP actually frame the face and hands; wider framings keep the chest target (the old 0.42x height target cut the head out of every closeup)
- Schema: UniverseFact model (projectId, text, category WORLD|CHARACTER|PROP|LOCATION|RULE, source USER|DSH|BIBLE, active flag - inactive facts are skipped by checks); db push + client regen + dev restart
- src/lib/universe-facts.ts (NEW): checkShotUniverseFacts runs a REAL vision model (createVision) over the shot's panel art against every active fact - strict JSON verdicts (holds/confidence 0..1/note per fact, hallucinated ids dropped), verdicts persist as FACT_HELD / FACT_BROKEN continuity events tagged [universe <shotId>] (prior verdicts for the shot replaced, severity WARNING only when broken AND confidence >= UNIVERSE_QUEUE_THRESHOLD 0.6); universeReRenderQueue groups confident violations per shot and ranks worst-confidence-first with the violated fact texts; universePanelData bundles facts + queue + shot picker feed
- API: /api/universe-facts (GET panel data, POST author fact), /api/universe-facts/[id] (PATCH steer text/category/active, DELETE), /api/universe-facts/check (POST {shotId} vision check)
- DSH grew to 36 tools: add_universe_fact registers canon, check_universe_facts audits a shot's art and reports HELD/BROKEN (conf x.xx) lines with the re-render offer; get_production_context lists universeFacts; doctrine rule 3 teaches the full loop; render_shot now names "the articulated stand-in (face + hands) performing ..."
- UI: Continuity view gained the Universe-facts vision checks panel - facts list (category chips, source, active toggle, delete), add-fact input with category select, a shot picker over art-carrying shots with Run fact check (real VLM), and the RE-RENDER QUEUE (worst confidence first) with per-row fact/confidence chips and a one-click Re-render + re-check button that regenerates the panel then re-runs the check; FACT_HELD / FACT_BROKEN kind colors in the main event list
- Seed: Immortal Path's four canon facts (cyan blade glow, two moons, the half-mask, golden particles) seed idempotently via seedStudioTeam (runs from ensureSeed on boot); live DB backfilled with scripts/backfill-universe-facts.ts
- scripts/e2e-iter30.ts (NEW): plan (16 pure checks - the real python module's face/hand suite, worker construction markers, 36-tool registry, doctrine, threshold, routes, panel mounting) + tool (37 live checks - facts CRUD + DSH authoring + context, real panel art, the REAL vision verdict (BROKEN 95% two moons, per-fact confidences persisted as FACT_* events), DSH wording, manufactured-verdict queue ranking (0.71 < 0.83, low-confidence excluded), the re-render + re-check flow replacing prior verdicts, the in-Blender rig probe (all 13 poses drive the rig, POINT extends the index to 0 curl while others curl 7.8, brows mirror, blade lags -79.2 at mid-swing vs -72 rest), a REAL BLENDER_LOCAL CLOSEUP render of STANCE -> POINT (h264 2.00s clip, rig report in the worker state, frames extracted))
- Visual verification: extracted clip frames show the face (emissive eyes, brows, mouth) framing correctly after the camera fix; a dedicated hand macro render (SLASH pose, camera aimed at the hand's world position) shows the palm with FOUR finger segments curling across the emissive blade and the thumb opposing - a real grip
- scripts/browser-fixtures-iter30.ts (NEW, seed|clean): 3 facts + manufactured confident violation + real panel art + 3 rendered clips (CLOSEUP STANCE->POINT among them)

Verified E2E (scripts/e2e-iter30.ts, steps plan + tool, ALL PASS): plan 16 checks; tool 37 checks including the live vision verdict with per-fact confidence, the worst-first re-render queue, the full re-render + re-check clearing flow, the in-Blender rig probe and the real h264 face/hand clip
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): Continuity view - universe panel renders facts with category chips, the queue row shows "worst 78%" with the violated-fact chip, the LIVE Run fact check returned a real vision verdict (HELD 30% blade / BROKEN 95% two moons / BROKEN 98% golden particles) as a banner, and the Re-render + re-check button regenerated the panel and re-checked to all-HELD, emptying the queue (iter30-universe-panel.png); Render Queue - 3 inline video players play in-page (two 2.00s pose clips + 1.58s PAN), LIVE BLENDER badge and the built-in z.ai provider line intact (iter30-queue.png); 0 page errors, console clean; fixture removed (project + 5 artifacts), renders/panels/sheets clean
- README: DSH row (36 tools + universe-facts loop), Continuity engine row, Render engines v3.2 rig bullet, try-the-loop unchanged, roadmap items 1 and 2 graduated with new next steps (lip-sync pass; supervised auto re-paint queue runner)

Stage Summary:
- GitHub main pushed with this iteration
- The stand-in PERFORMS now: every pose carries a face and a pair of hands - eyes blink and squint, brows knit or lift, mouths open with effort, fingers grip the blade or spread to channel or extend to point, and the blade carries follow-through - with tight framings re-aimed at the head so closeups show the performance
- The universe's canon is now machine-checkable art QA: facts register in seconds (UI or DSH), a vision model judges every panel against them with a confidence per fact, and confident violations flow into a worst-first re-render queue whose one-click re-render + re-check closes the loop
- Next horizons: supervised auto re-paint (queue-runner without the click), fact-aware art prompts, lip-sync on speaking closeups, cross-turn DSH plans, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 31
Agent: Super Z (main agent)
Task: Iteration 31 - supervised auto re-paint runner, fact-aware art prompts, and lip-sync on speaking closeups (user: "awesome, now proceed with a supervised auto re-paint runner, fact-aware art prompts, and lip-sync on speaking closeups")

Work Log:
- Schema: RepaintRun model (projectId, status RUNNING|PAUSED|DONE|ABORTED, cap, index, steps JSON array of {shotId, ref, factTexts, beforeWorst, afterSummary, afterBroken, outcome, error, at}); db push
- Supervised auto re-paint runner (src/lib/universe-repaint.ts NEW): startRepaintRun walks the universe-facts re-render queue WORST-FIRST (capped 1-8, default 3), refusing when a run is already RUNNING/PAUSED or the queue is empty; each step = fact-aware re-paint (generateShotPanelArt with emphasisFacts) + real vision re-check (checkShotUniverseFacts) + outcome record (FIXED clears the row, STILL_BROKEN stops auto-retry and waits for the director, ERROR records what broke); the loop is fired-not-awaited with a DB status gate per step (pause finishes the in-flight step and parks, resume re-reads the queue fresh and continues skipping done shots, abort wins over natural completion - the DONE update only lands on a still-RUNNING run); run state lives in the DB so refreshes and DSH see the same progress; REPAINT_RUN continuity events mark start/abort
- API /api/universe-facts/repaint: GET latest run, POST start (maxItems), PATCH pause|resume|abort
- DSH grew to 37 tools: run_repaint_queue starts a pass and reports the supervision contract; check_universe_facts now offers the runner alongside the manual re-render; render_shot names the lip-sync per driver (viseme program on the stand-in / speech direction to the model / blocking speech beat); doctrine rules 1+3 teach lip-sync on speaking closeups, fact-aware art, and the supervised runner
- Fact-aware art prompts (src/lib/ai/art.ts): factCanonLines compiles active universe facts into MUST-hold directives (capped 6, category-tagged); factCanonSection adds the flagged-fact corrections ("the previous check flagged this panel for: ...") for re-paints; generateShotPanelArt injects the section into every panel prompt and loads project.universeFacts (active only) - the art is fact-aware BEFORE the vision check judges it
- Lip-sync (src/lib/animation/lipsync.ts NEW): isSpeakingCloseup detects SPEECH dialogue + CLOSEUP/EXTREME_CLOSEUP (THOUGHT/SFX never move lips); buildSpeechProgram lays the lines over the clip timeline riding real VOICE-take windows when they exist (cue startMs + voiceDurationMs) or spreading length-weighted with breathing gaps; buildVisemes maps per-character mouth shapes (vowel table with wide "ee" and round "oo" axes, m/b/p press shut, uppercase shouts open wider, vowels ring 1.35x, stops 0.7x) as monotonic non-overlapping ms segments capped at 600; sampleSpeech returns the covering shape or a 90ms decay-out so the mouth never snaps
- Engine wiring (render.ts): createRenderJob builds the speech program from the shot's dialogue + VOICE cues, stamps the job stage with "+ lip-sync on <speakers> (N lines, M visemes)", names it in the production event, and passes the payload per driver; the img2vid provider-lost takeover rebuilds the speech program so the MOTION fallback keeps the lip-sync
- Drivers: bridges/blender/animeos_bridge.py v3.3 - parse_speech defensively extracts the viseme table (bad shapes never break a render), speech_open_at mirrors the TS sampler per frame, apply_pose drives the mouth rig from the viseme (openness beats the pose floor, X-scale spreads wide/purses round) and the worker state reports the program; img2vid.ts carries speechLines into the prompt ("SPEAKS out loud ... lips and jaw articulate the words") for host + zai payloads; motion.ts plans a blocking speech beat per line (alpha-0.08 pulse over each span window) and names it in the program note
- UI: the Continuity view's universe panel gained the Supervised auto re-paint section - run-cap select, Start run, Pause/Resume/Abort, a 4s poll streaming the run while live, outcome-colored step rows (FIXED emerald / STILL BROKEN amber / ERROR rose) with ref, before% -> result and the flagged-fact detail, status chip DONE n/n; header copy names fact-aware generation
- README: v3.3 rig bullet (the face speaks), MOTION speech beat, img2vid speech direction, Status row 37 tools + lip-sync, roadmap items 1-2 graduated (lip-sync delivered; fact-aware prompts + supervised runner delivered) with new next steps (audio-driven visemes over real TTS takes; identity-similarity scoring + canon-health readout)
- Fixed three REAL bugs the previous session's unfinished E2E exposed: runLoop's index restarted from 0 on resume (now tracks the accumulated step count); a run aborted during its FINAL step was marked DONE (the natural-completion update now only lands on a still-RUNNING run); the img2vid takeover dropped the speech program (now rebuilt)
- E2E harness finished and hardened: scripts/e2e-iter31.ts (plan 32 pure checks; tool 18 live checks: empty-queue refusal, real fact-aware panel art, manufactured worst-first queue, start -> mid-step pause -> park PAUSED -> resume -> DONE index=2 with both outcomes recorded and the flagged fact text riding the step, manufactured verdicts replaced by real re-checks, the still-broken row staying queued BY DESIGN with a fresh run starting over it, DSH refusing while a run is live, abort parking ABORTED with the in-flight step recording and the run STAYING ABORTED; toollip 14 checks: python viseme suite, a REAL BLENDER_LOCAL render of the speaking closeup (REVIEW, h264 2.00s, worker state {"lines":1,"visemes":41}), in-Blender mouth probes (neutral x=1, ee opens AND widens z=1.56 x=1.32, oo purses x=0.55, the pose shout stays the floor) and extracted frames showing the mouth open mid-speech vs late); manufactured events now faithfully mirror the real event writer (entityName = registered fact text); guard rails branch on the real post-run queue state
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): Continuity view on a seeded fixture - the Supervised auto re-paint section renders status chip DONE 2/2 with the FIXED + STILL BROKEN step rows, Start run went LIVE (RUNNING 0/1 with Pause/Abort and the working indicator, REPAINT_RUN event in the list), the real step completed in-browser (DONE 1/1, STILL BROKEN row with the REAL 98% two-moons re-check verdict), queue row + Re-render + re-check intact (iter31-runner.png, iter31-runner-live.png); Render Queue - LIVE_BLENDER badge, BLENDER/MOTION chips, in-page play() verified (t=1.27s advancing); DB: shot 1's event carries "lip-sync on Lin Yue (1 line, 41 visemes)" with a BLENDER_LOCAL job; 0 page errors, console clean; fixtures removed (browser fixture project + 5 artifacts + orphan sweep clean)
- scripts/browser-fixtures-iter31.ts (NEW, seed|clean): 2 facts + real closeup panel art + a manufactured 0.78 violation + a finished 2-step run + 3 real clips (the closeup routes BLENDER_LOCAL with the lip-sync viseme program); scripts/cleanup-iter31-fixtures.ts now sweeps panels/renders/sheets by DB-existence instead of a time window and untracks e2e frame artifacts (.gitignore)

Stage Summary:
- GitHub main pushed with this iteration (the previous session's unpushed auto-commit was verified, fixed, message-amended and pushed)
- The re-render queue closes itself now: one click (or one DSH call) starts a supervised pass that re-paints flagged panels with the canon and the flagged facts riding the prompt, re-checks with the vision model, and stops for the director exactly where judgment is needed - pause/resume/abort hold across refreshes because the run lives in the DB
- The art generator is fact-aware BEFORE the audit: every panel prompt carries the production's active canon, and re-paints get the exact violations restated as corrections
- Speaking closeups perform their lines: the stand-in's mouth articulates a deterministic viseme program aligned to the voice-take windows, the img2vid model receives the lines as speech direction, and the MOTION engine lands a blocking speech beat - with the program visible on the job stage, the production event and the DSH result
- Next horizons: audio-driven visemes over real TTS takes, identity-similarity scoring per panel, cross-turn DSH plans, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 32
Agent: Super Z (main agent)
Task: Iteration 32 - DSH plans that survive the turn (cross-turn resumable plans + plan review UI) (user: "push first and proceed with the next iteration" - the leading README roadmap horizon)

Work Log:
- Pushed first: the previous session's iteration 31 work (found committed under an auto-generated message, never pushed, worklog entry missing) was verified (src tsc gate clean, eslint clean), its unfinished E2E was finished and driven to ALL PASS, the worklog entry was written, the commit was amended to the proper Iteration 31 message and pushed (58199eb..957cc11) before this iteration started
- Schema: DshPlan model (projectId, title, goal, status PROPOSED|ACTIVE|PAUSED|DONE|ABORTED, source DSH|CREATOR, steps JSON [{tool, args, why, status PENDING|DONE|ERROR, result?, at?}], cursor = next step to run); db push + client regen + dev-server restart
- src/lib/dsh/plans.ts (NEW): validatePlanSteps (every tool must exist in the registry, plan meta-tools refused - no recursion, args must be objects, why required, cap 12, accepts array or JSON string); createPlan lands PROPOSED; setPlanStatus steering guards (approve only PROPOSED/PAUSED -> ACTIVE, pause only ACTIVE, abort anywhere live, DONE/ABORTED frozen); runPlanSteps executes the next 1-3 steps per call through the SAME executeTool path as a live turn, records each result + timestamp on the plan, lands a production event per step ("Plan '<title>' step k/N <tool> -> ...") and a completion event; a FAILED step records ERROR, parks the cursor AT that step and stops the run (fix the cause, run again to retry) while the step after it never runs; cursor reaching the end marks DONE; report lines name every outcome and what remains
- DSH grew to 40 tools: create_plan (lands a cross-turn plan, PROPOSED, names the review gate), run_plan (executes the next steps of the latest ACTIVE plan or an explicit planId, reports per-step outcomes), steer_plan (approve/pause/resume/abort with the same default-pick rules); get_production_context (buildCompactContext) now lists the project's plans with status + progress + goal so a LATER conversation picks them up exactly where they stopped; doctrine intro teaches the resumable-plan loop and NEW rule 19 (PLANS THAT OUTLIVE THE TURN: land a plan for what does not fit the turn, the creator approves in the review panel, steer_plan approve only on an explicit go, run_plan continues across conversations, failed steps park for retry)
- API /api/dsh-plans: GET lists plans, POST creates a creator-authored plan (source CREATOR), PATCH approve|pause|resume|abort|run (maxSteps 1-3)
- UI: the DSH view gained the "Plans that outlive the turn" review panel (dsh-console.tsx) - a collapsible bar with counts ("N awaiting review" / "N in flight"), plan cards with status chip (PROPOSED violet / ACTIVE emerald / PAUSED amber / DONE sky / ABORTED neutral), done/total (+failed) progress, goal, per-step rows (status dot, index, tool, why or first result line, failed tag), and the supervision buttons per state (Approve; Run next step / Run 3 / Pause; Resume; Abort); 5s polling only while a plan is ACTIVE; the panel invalidates plans after every DSH turn (create_plan may have landed)
- scripts/e2e-iter32.ts (NEW): plan (14 pure checks - validation matrix incl. unknown/meta/over-cap/empty steps, 40-tool registry, doctrine rule 19 + intro, route + panel on disk) + tool (33 live checks - DSH lands a 3-step plan, PROPOSED refuses to run, approve opens the runner, one step per call advances the cursor with the term REALLY in the DB and a production event naming the plan, run 3 finishes DONE with every step DONE and the dialogue/fact steps really executed, DONE plans refuse run + steering, a poisoned plan parks ACTIVE at the failed step with the next step PENDING and a retry report, pause/resume/abort steering with the refusals, the production context lists both plans with progress, GET/POST/PATCH API surface incl. run-before-approval refusal, DSH default-pick for run_plan/steer_plan and the no-ACTIVE-plan error); E2E fetches pinned to 127.0.0.1 (localhost resolved to an unreachable IPv6 in this environment)
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): fixture project with a PROPOSED plan (3 steps) + an ACTIVE plan mid-progress - the panel renders both states with counts ("1 awaiting review / 1 in flight"), pressing Run next step REALLY executed step 2 (cursor 1 -> 2, the universe fact REALLY created, production event "Plan 'Canon notes continuation' step 2/3 add_universe_fact -> ..."), pressing Approve REALLY flipped the proposal to ACTIVE (chip flipped to "2 in flight") with Run/Pause buttons appearing (iter32-plans.png, iter32-plans-live.png); fixture removed (project + plans cascade)
- scripts/browser-fixtures-iter32.ts (NEW, seed|clean): 1 PROPOSED + 1 mid-progress ACTIVE plan over cheap tools so the browser exercises real executions
- README: Status row (40 tools, cross-turn plans clause), roadmap item 3 graduated (plans + review UI delivered) with the new next step (a scheduler that runs approved plans on a cadence)

Stage Summary:
- GitHub main pushed with this iteration
- DSH now remembers across conversations: work that will not fit a turn lands as an ordered, explained tool-call plan; the creator holds the review gate; execution is real, resumable, and supervised (failed steps park, steering holds, events narrate)
- The plan state rides the production context, so "continue yesterday's plan" is a normal DSH instruction with no re-explaining
- Next horizons: a scheduler for approved plans (nightly breakdowns, render-queue supervision), audio-driven visemes over real TTS takes, identity-similarity scoring per panel, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 33
Agent: Super Z (main agent)
Task: Iteration 33 - cadence scheduler for approved plans (nightly breakdowns, render-queue supervision) and audio-driven visemes over real TTS takes (user: "Proceed with the roadmap's next horizon ... a scheduler that runs approved plans on a cadence ... and tackle audio-driven visemes over real TTS takes")

Work Log:
- Push state verified first: main was in sync with origin/main (iteration 32 pushed as b982d02), nothing pending
- Schema: StudioSchedule model (projectId, name, kind PLAN_RUN|REPAINT_QUEUE, planId nullable for latest-ACTIVE pick, cadence HOURLY|DAILY|WEEKLY with intervalHours/hourUtc/weekday, maxSteps 1-3, enabled, lastRunAt, nextRunAt, lastStatus OK|SKIPPED|ERROR, lastReport, runCount); RenderJob.lipNote column so the speaking-closeup note survives the completion path; db push + client regen
- Audio-driven visemes (src/lib/animation/viseme-audio.ts NEW): parseWavMono (PCM16 WAV parser), frameAudio (10ms hop RMS + zero-crossing rate), analyzeTakeVisemes (95th-percentile loudness gate, voiced frames open with the energy envelope, high-ZCR quiet frames land wide fricative shapes, 3-frame median smoothing, short runs snap to neighbors, 1:1 audio-to-clip time mapping clipped to the span, monotonic non-overlapping segments capped 600) - audio-drivenSpeechProgram builds the text-path spans then swaps each paired take's visemes for audio-derived ones (misalignment guard: partial take sets keep the text performance); describeAudioSpeechProgram notes "audio-driven lip-sync on X (N visemes from M real takes)"
- Engine wiring (render.ts): shotSpeechProgram reads each VOICE cue's take from public/voices/{cueId}.wav, builds the program, and names the note; used at BOTH call sites (createRenderJob + the img2vid provider-lost takeover); the viseme payload shape is unchanged so the v3.3 Blender worker consumes audio-driven programs as-is; the note persists on RenderJob.lipNote and is kept through the BLENDER_LOCAL / BLENDER / IMG2VID completion stages (the poll path used to overwrite it - found by the E2E and fixed for real)
- Cadence scheduler (src/lib/scheduler.ts NEW): computeNextRun (hourly interval, daily at hourUtc with today/tomorrow rollover, weekly weekday math), describeCadence ("nightly at 02:00 UTC"), listSchedules with pinned plan titles, createSchedule (validated, nextRunAt computed, SCHEDULE event), fireSchedule (PLAN_RUN: pinned plan or latest ACTIVE, runPlanSteps maxSteps, approval stays the gate - PROPOSED/DONE/ABORTED plans SKIP with the reason; REPAINT_QUEUE: tickProjectJobs advances active render jobs then the universe queue is supervised - a live run or a clean queue SKIPS, a dirty queue starts a real startRepaintRun), fireDueSchedules (claims the slot FIRST - lastRunAt + nextRunAt advance before the work runs - so slow fires and concurrent manual ticks cannot double-run, then records lastStatus/lastReport/runCount + a SCHEDULE event), fireScheduleNow for Run-now/DSH, startSchedulerLoop (60s interval, globalThis HMR guard, 15s warmup)
- src/instrumentation.ts NEW: boots the loop on the nodejs runtime (verified live in dev.log: "[scheduler] cadence loop started")
- API /api/schedules: GET list, POST create + {action:"tick"} fire-all-due, PATCH enable|disable|run, DELETE
- DSH grew to 42 tools: create_schedule (planTitle pin optional, defaults to latest ACTIVE) and steer_schedule (run/enable/disable/delete by name fragment, default latest); buildCompactContext lists the schedules line (ON/OFF, kind, cadence, next fire, last status + report); doctrine intro names ON A CADENCE + AUDIO-DRIVEN visemes and NEW rule 20 teaches the nightly-breakdown pattern, the supervision contract and that approval stays the gate
- UI: the DSH view gained the "Cadence scheduler" panel (dsh-console.tsx) under the plans panel - armed counts, create form (name, kind, cadence, hour, plan picker over ACTIVE plans, steps/fire), schedule rows (ON/OFF, kind chip, last-status chip OK emerald / SKIPPED amber / ERROR rose, cadence line, fire count, next-fire countdown, last report) with Run now / Disarm-Arm / Delete; 8s poll while any schedule is armed
- Environment repair: the sandbox reaps background processes between tool calls, so the dev server is now driven per-call via scripts/dev-up.sh (start-or-reuse, turbopack cache keeps it ~1s); Blender 4.3.2 portable reinstalled at the bridge's probe path (the box had been reset)
- E2E: scripts/e2e-iter33.ts (plan 29 pure checks: synthetic-envelope WAV analysis matrix, audio-driven builder incl. text fallback + partial-take guard, cadence math incl. rollover cases, 42-tool registry, doctrine, instrumentation + route + panel on disk; tool 33 live checks: REAL TTS take through renderVoiceTake (provider stayed 429 rate-limited after 3x45s backoff, so the harness synthesized a speech-like PCM take on the SAME disk path - the provider loop itself was proven in iters 27/31), real BLENDER_LOCAL speaking-closeup render with the audio-driven program (worker state visemes=5), stage + event carrying "audio-driven lip-sync ... 1 real take" through COMPLETION, scheduler matrix: API create/list/fire, step REALLY executed (terminology row in DB) + row ledger + cadence advance, DONE-plan SKIP, default-pick latest-ACTIVE fire, clean-queue SKIP + already-live SKIP + due-batch fire, disabled refusal, DSH create/steer incl. refusals, context schedules line, fire ledger, full fixture cleanup)
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected (LIVE_BLENDER intact)
- Verified E2E (agent-browser): fixture project on the DSH view - the panel renders "2 of 2 armed" with both kinds, Run now on the nightly plan run REALLY executed step 2 (universe fact "the terrace lanterns burn blue at night" REALLY in the DB, last OK "2/2 steps done - plan complete"), Run now on the watch fired last SKIPPED with the clean-queue report (fires=4), the create form landed a third schedule (3 of 3 armed) and Delete removed it (iter33-scheduler.png / iter33-scheduler-live.png / iter33-scheduler-final.png); 0 page errors, console clean; fixture removed (project + plans + schedules cascade)
- README: DSH row (42 tools + cadence scheduler clause), render-engine v3.3 bullet (audio-driven over the real take), Status row (42 tools + cadence thesis), roadmap items 1 and 3 graduated (audio-driven visemes delivered; scheduler delivered) with new next steps (per-provider latency/cost readout + neural viseme pass; per-episode plan templates)

---
Task ID: 34
Agent: Super Z (main agent)
Task: Iteration 34 - per-provider latency/cost readout on queue cards, per-episode plan templates, and identity-similarity scoring for panels (user: "Proceed with per-provider latency/cost readout on queue cards, per-episode plan templates, and identity-similarity scoring for panels.")

Work Log:
- Push state verified first: main in sync with origin/main (iteration 33 pushed as d14042a)
- Schema: RenderJob.telemetry JSON column (spans per provider that worked the job, takeover trail, totalMs, credits); IdentityScore model (projectId, shotId unique, scores JSON [{characterName, similarity 0..1, aspects {face,hair,wardrobe,weapon,palette,style}, note}], worst, castSize, note, scoredAt - replaced on re-score); db push + client regen (the dev server initially served a STALE Prisma client - found live by the E2E's first API call, fixed by regenerating + restarting)
- Per-provider telemetry (src/lib/engine/telemetry.ts NEW, pure): parseTelemetry (defensive), closeSpan (records/extends a provider's latency span from startedAt), takeoverSpan (closes the OLD provider's span + appends the takeover trail in one call - the driver-switch paths reset startedAt so each span measures only its own work), estimateCredits (documented rate table: hosted IMG2VID bills 2 credits per clip-second; MOTION/BLENDER/BLENDER_LOCAL/SIMULATOR bill zero - local compute), describeTelemetry ("MOTION 12.4s (built-in ffmpeg) -> IMG2VID 48.2s - ~34 credits"), formatSeconds, providerLedger (per-provider jobs/total/avg latency with credits attributed to the job's FINAL provider)
- Engine wiring: every completion path in render.ts records telemetry - MOTION (success/failed/error catch, via a prev-row re-read), BLENDER_LOCAL (all 4 terminal paths), IMG2VID (3 terminal paths + the MOTION and SIMULATOR takeovers), BLENDER (terminal paths + the bridge-lost SIMULATOR takeover), SIMULATOR (wall-clock completion); telemetry persists through DSH inspection and stage rewrites (verified live: the fixture job went NEEDS_REVISION with telemetry intact)
- UI (render-view.tsx): every finished queue card carries the readout (chained provider chips with per-span latency + notes, a ~credits chip when hosted work billed, a takeover flag with the trail tooltip); a Provider ledger panel above the queue aggregates per-engine jobs, avg/total latency and credits; a ~total credits chip joins the counts row
- Per-episode plan templates (src/lib/dsh/plan-templates.ts NEW): four templates per episode (beat-breakdown: context + a fresh beat scene + a 3-shot ESTABLISHING/MEDIUM/CLOSEUP cinematography breakdown with pose programs + check_capabilities; panel-pass: 3 fact-aware panel generations + art-continuity scan; render-pass: 3 PREVIEW renders + a voice-direction diff; canon-audit: universe-facts vision verdict + art scan + capabilities), all args resolved against the REAL episode row at instantiation (episode/scene numbers), steps after create_scene deliberately omit sceneNumber (the tools' latest-scene default makes run-time ordering deterministic), every template validated against the live registry; instantiateEpisodePlan lands PROPOSED (source CREATOR) with a duplicate-live-plan refusal; episodeTemplateCatalog serves the UI
- API /api/plan-templates: GET catalog, POST instantiate; DSH grew to 44 tools: land_episode_plan (episodeNumber defaults to the latest episode, unknown templates refused with the catalog)
- UI (dsh-console.tsx): the plans panel gained the Per-episode templates section (episode picker with counts, 4 template cards with step counts + cadence hints, Land as plan buttons, landing confirmation banner); the panel now ALWAYS renders (the browser pass caught the zero-plans early-return hiding the templates - real UI bug fixed)
- Identity-similarity scoring (src/lib/identity.ts NEW): ONE vision call per panel judges the art against EVERY sheeted featured character's sheet (prompt manifest built per cast; strict-JSON verdict parsed by parseIdentityVerdict - clamps 0..1, filters aspects to the six axes, cast members the model skipped score 0 with an explicit note, worst = min); scoreShotIdentity splits into loadIdentityShot/prepareIdentityContext/buildIdentityPrompt + the REAL vision call + persistIdentityVerdict (upsert per shot + replace-prior IDENTITY_VERIFIED/IDENTITY_DRIFT continuity events tagged [identity E1 Sc1 S###]); scoreShotIdentityFromRaw drives the SAME pipeline from a pre-retrieved verdict (the E2E seam while the vision provider is 429); scoreProjectIdentity batch (capped 1-8, scored-worst-first then never-scored); identityPanelData (worst-first rows, below-bar queue, picker, average); IDENTITY_REPAINT_THRESHOLD = 0.6
- API /api/identity: GET panel feed, POST score one, PATCH batch; DSH score_panel_identity (targeted by scene/shot, default-worst via the IdentityScore table, batch via limit, art-less refusals, the re-paint offer named in the result); doctrine rule 21 (PER-EPISODE TEMPLATES AND IDENTITY DRIFT) + intro teaching templates and the identity score; buildCompactContext gained the identity lines (worst-first with DRIFT flags) and a planTemplates line
- UI (continuity-view.tsx): Identity similarity panel - stats chips (scored count, avg worst, identity bar, below-bar count), the re-paint queue with one-click Re-paint + re-score (regenerates the panel with the canon riding the prompt then re-scores), worst-first rows with per-character score chips (aspect tooltips), Score now per row, Score worst 3 batch; IDENTITY_VERIFIED/IDENTITY_DRIFT colors in the event list
- E2E: scripts/e2e-iter34.ts (plan 31 pure checks - telemetry matrix incl. span merge, takeover chain, hosted-only credit math (17s clip x 2 = 34 credits), ledger attribution, template validation + number resolution, identity parse matrix, 44-tool registry, doctrine rule 21, routes + UI on disk; tool 39 live checks - the image provider attempted first then synth-fallback REAL PNGs on the same disk paths (429 discipline, per iter 33), identity scoring through the REAL pipeline with the vision SDK module mocked in-process (bun mock.module verified retroactive via probe; the live vision loop was proven in iters 29/30), verified/drift threshold sides (0.83 VERIFIED INFO vs 0.41 DRIFT WARNING), re-score upsert, worst-first panel data + queue + average, GET/POST API incl. the art-less refusal over HTTP, worst-first batch, DSH targeted + default-worst + batch + refusal, template catalog/instantiate/duplicate/DSH land/bad-template refusal, the beat-breakdown plan approved + REALLY run to DONE (scene 2 + 3 posed shots + capabilities in the DB), a REAL MOTION render completing with telemetry (spans, zero credits, describe, ledger, API round trip), context identity + templates lines)
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): fixture project - Render Queue shows the PROVIDER LEDGER ("MOTION - 1 job, avg 833ms - total 833ms - 0 credits") and the finished card's readout chip ("MOTION 833ms - built-in ffmpeg") from the REAL completed render; Continuity identity panel renders both scored rows + the re-paint queue and the LIVE Score worst 3 click returned the real banner ("0 panel(s) scored, 2 skipped" - the 429s honestly reported by the UI round trip); DSH plans panel caught + fixed the zero-plans bug, then the Per-episode templates section rendered all 4 cards and a LIVE Land as plan click landed "E01 - Episode beat breakdown" PROPOSED ("1 awaiting review", 6 concrete steps verified in the DB); 0 page errors, console clean; fixture removed (project + cascade + orphan artifact sweep - the sweep's directory-crash on renders/cuts found and fixed)
- scripts/browser-fixtures-iter34.ts (NEW, seed|clean): 2 sheeted characters + panels on 2 of 3 shots + identity rows via the REAL persistence path + a real MOTION render with telemetry; scripts/verify-iter34-db.ts for post-browser DB checks
- README: DSH row (44 tools + per-episode templates + identity scoring), Continuity engine row (identity-similarity scoring clause), try-the-loop items 4/11/12, Status row (44 tools + ledger + templates thesis), roadmap items 2/3/5 updated (identity scoring, per-episode templates and the provider ledger delivered; new next steps: canon-health readout, schedule-health digest, neural viseme pass, embedding-based identity distance)

Stage Summary:
- GitHub main pushed with this iteration
- The queue is now a ledger: every job reports which providers worked it, how long each took and what the hosted work cost, with takeover chains preserved and a per-engine aggregate above the queue
- The nightly-breakdown pattern is one click: four per-episode plan templates resolve an episode's real numbers into a reviewable plan, and approval opens the runner the scheduler already knows how to walk
- Identity drift is a number now: one vision call scores each panel against every featured character's canonical sheet per aspect, the worst below the 60% bar queues the panel, and the re-paint + re-score loop closes it - with the score riding the production context and DSH surface
- Next horizons: canon-health readout over fact verdict history, schedule-health digest, neural viseme pass, embedding-based identity distance, cross-episode template variations, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 35
Agent: Super Z (main agent)
Task: Iteration 35 - canon-health readout over fact verdict history, schedule-health digest, embedding-based identity distance + the studio_pulse health readout (user: "proceed with the next iteration")

Work Log:
- Push state verified first: main in sync with origin/main (iteration 34 pushed as 9ef5f91)
- Schema: PanelEmbedding model (projectId, shotId unique, hashHex 64-bit dHash, rows JSON [{characterName, palette, structure, combined, note}], worst = min combined affinity, method note); db push + client regen + dev restart
- Canon health (src/lib/canon-health.ts NEW): factHealthFromEvents rolls the FACT_HELD/FACT_BROKEN event history into per-fact rows (matching by the entityName text prefix the pipeline writes; HELD/VIOLATED/UNVERIFIED status, held/broken counts across audited panels, worst confident violation, newest note/confidence), canonScoreFromRows scores the production 0.4*coverage + 0.6*holdRate over ACTIVE facts only (an unchecked canon is not a healthy canon; inactive facts' history never drags the score), canonBand splits HEALTHY/WATCH/DRIFTING at 0.85/0.55, canonHealthData adds a 14-day trend and a worst-facts ranking (violated first, then unverified) plus a headline
- Schedule health (src/lib/schedule-health.ts NEW): scheduleHealthFromEvents aggregates the SCHEDULE production events per schedule over a 14-day window (registration events without a fire status excluded - found by the live digest showing 6 fires instead of 3), per-schedule OK/skipped/error counts, consecutive-error streaks (any non-error breaks it), OVERDUE when an armed schedule's nextRunAt slipped >1h past, ok-rate and a headline; scheduleHealthData over the live rows + events
- Embedding (src/lib/embedding.ts NEW): pure math (l2Normalize, paletteHistogramFromPixels 4x4x4 RGB bins, structureHashFromGray 9x8 dHash, cosineSimilarity, structureSimilarity hamming, affinityBetween mean, hex roundtrip) + sharp IO (embedImageFile decodes via 32x32 raw resize, cell-averaged 9x8 luminance for the dHash; BigInt literal-free for the pre-ES2020 target); embedPublicImage resolves the same public paths the art pipeline writes
- Affinity pass (identity.ts): scoreShotEmbedding (panel vs every sheeted featured character's sheet, PanelEmbedding upsert, honest notes: tripwire only, the vision score stays the authority), scoreProjectEmbeddings batch, AFFINITY_WATCH_THRESHOLD 0.5, parseAffinityRows/describeAffinity; identityPanelData now returns an embeddings map keyed by shotId
- DSH at 45 tools: studio_pulse (src/lib/studio-pulse.ts NEW: one instant provider-free readout with canon score + worst facts, identity avg-worst/below-bar counts + affinity low, schedule digest with needs-attention names, queue pressure; doctrine rule 22 THE STUDIO REPORTS ITS OWN HEALTH + intro sentence), score_panel_identity now computes the free affinity in the SAME result (rider line, WATCH flag below the tripwire line, tripwire phrasing in both branches); buildCompactContext gained canonHealth + scheduleHealth headlines and affinity lines (panelEmbeddings include)
- API: GET /api/canon-health (digest + rows); GET /api/schedules now returns {schedules, digest}; /api/identity POST/PATCH accept mode:"affinity" (the local pass; default remains vision)
- UI: Continuity view gained the Canon health panel (score chip in band color, audited/hold-rate/14d chips, worst-facts rows with status chips and never-audited labels) and the identity panel gained the Affinity pass button + per-row "aff <name> <pct>" chips (amber under the watch line, tooltip names the tripwire); the DSH scheduler panel renders the digest chips (armed counts, 14d outcomes, overdue, needs-attention) plus an erroring-schedules line
- E2E: scripts/e2e-iter35.ts (plan 41 pure checks: canon rollup incl. the newest-verdict-wins and inactive-fact-exclusion semantics, score math, bands, schedule window/streak/overdue/aggregate matrix, embedding math incl. REAL sharp decode of synthesized gradients (deterministic, same=1.0, different=0.454), 45-tool registry, doctrine rule 22, routes/UI on disk; tool 40 live checks: REAL fact verdict events through checkShotUniverseFacts (vision SDK mocked in-process as in iter 34), canon score 0.8 WATCH over the real history, REAL schedule fires (SKIPPED x2 + ERROR via the deleted pinned plan) with the digest over real events, the REAL provider-free affinity pass through sharp (mandelbrot panel 0.297 far vs crimson-panel 0.593 close, upsert, art-less refusal, batch), POST /api/identity mode=affinity over HTTP with zero providers, the vision score + affinity rider in one DSH result, studio_pulse lines, context canon/schedule/affinity lines)
- The pure pass caught two test-design flaws and one real lib decision: the hold rate counts ACTIVE facts only, the dHash gradient direction, and the newest-verdict semantics; the live pass caught the registration-events-as-fires bug and the rider's missing tripwire phrase
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): fixture project - Continuity "Read canon health" returned the real chips (canon 80% WATCH, 3/4 facts audited, hold rate 83%, 14d: 5 held / 1 broken) with the VIOLATED mask fact ranked first; identity rows show the live affinity chips (aff Xiao Chen 48%, aff Lin Yue 24%) and the LIVE Affinity pass returned "2 panel(s) embedded"; the DSH cadence panel renders the digest chips (3 armed of 3, 14d: 1 OK / 1 skipped / 1 error - the watch honestly started a supervised re-paint pass when it found the queue dirty) + "last fire errored: Stuck exporter"; 0 page errors, console clean; fixture removed (project + cascade + orphan sweep, .gitkeep files restored)
- scripts/browser-fixtures-iter35.ts (NEW, seed|clean): gradient sheets/panels + REAL affinity rows via scoreShotEmbedding + facts/events in the exact pipeline format + REAL schedule fires
- README: try-the-loop 4/12 (studio_pulse, canon health, affinity pass), Status row (45 tools + the health thesis), roadmap items 2/3/5 graduated (canon health, schedule digest, embedding distance delivered; new next steps: auto-retire suggestions, creator-authored template variations, per-character drift curves)

Stage Summary:
- GitHub main pushed with this iteration
- The studio reports its own health now: the canon score turns fact verdict history into a 0..1 readout where an unchecked canon is honestly unhealthy; the cadence digest turns 14 days of fire events into per-schedule streaks, overdue flags and needs-attention names; and the provider-free embedding tripwire (dHash + palette histogram) keeps identity drift measurable even when the vision provider is down - all aggregated by the instant studio_pulse readout DSH can cite any time
- Next horizons: per-fact auto-retire suggestions, per-character identity drift curves over episode order, creator-authored per-episode template variations, a daily digest message, neural viseme pass, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 36
Agent: Super Z (main agent)
Task: Iteration 36 - img2vid demotion (default-off + previz-only labeling), per-fact auto-retire suggestions, per-character identity drift curves, creator-authored plan template variations, and a daily digest (user: "proceed with the next iteration with the img2vid demotion (default-off + previz-only labeling) plus your four queued features")

Work Log:
- Push state verified first: main in sync with origin/main (iteration 35 pushed as 41276ca)
- Context: before this iteration the user asked why img2vid exists at all given the studio's design-first thesis; the answer (previz/benchmark scaffolding only) became this iteration's demotion work, and the four queued README-roadmap features (auto-retire, drift curves, template variations, daily digest) shipped alongside it
- img2vid demotion: img2vidProvider() now requires EXPLICIT opt-in for the built-in model (ANIMEOS_IMG2VID in on/zai/true/1/previz; unset and off both close the slot; an attached host still wins) - the module header documents the previz-only doctrine; render.ts comment + stage text say previz; render-view shows an off-by-default note when the slot is closed, PREVIZ chips on IMG2VID-driver job cards with a "previz pass, not a final render" tooltip; panel-inspector copy, set_shot_poses + render_shot DSH texts, and the doctrine intro all teach "designed, not generated, never the final render"
- Per-fact auto-retire (canon-health.ts): RetireSuggestion + retireSuggestionsFromRows (active facts with >= RETIRE_MIN_PANELS 3 audits and hold rate < RETIRE_HOLD_RATE 0.34, sorted worst hold-rate first, reason names the counts + the reword/retire fork), canonHealthData returns suggestions + digest.retireSuggestions + a headline clause; CanonHealthPanel renders the suggestions with live Reword (prompt + PATCH text) and Retire (PATCH active:false) buttons; studio_pulse gained a retire-suggestions line + headline part; DSH context gained canonRetire; doctrine rule 22 teaches "a writing problem, not an art problem"
- Identity drift curves (identity.ts): DriftPoint/CharacterDrift/identityDriftFromRows (per-character curves from IdentityScore.scores arrays, story-ordered by episode/scene/shot, delta = last-first, trend bands at DRIFT_TREND_THRESHOLD 0.05 into DECLINING/IMPROVING/STABLE/FLAT, worstAspect = lowest mean aspect, curves sorted steepest decline first) + identityDriftData (loader + headline); identityPanelData now carries drift; the identity panel draws per-character sparklines (pure SVG polyline) with trend chips, latest score, panel counts and the weak aspect; studio_pulse + context identityDrift lines flag declining characters; doctrine rule 21 teaches reacting to a DECLINING curve with a fresh sheet anchor
- Creator-authored template variations (plan-templates.ts + PlanTemplate model): resolveTemplateValue deep-resolves {episode}/{latestScene}/{title} tokens; validateTemplateSteps validates against the LIVE TOOL_DEFS registry (1..12 steps, unknown tools refused at save AND at landing); savePlanTemplate/listPlanTemplates/deletePlanTemplate/builtInStepsFor (PROJECT or STUDIO scope mirroring ArcTemplate); instantiateEpisodePlan now lands built-ins OR saved variations by id/exact name (duplicate-live refusal, usageCount/lastUsedAt bump on landing); /api/plan-templates gained PUT (save) + DELETE + a GET preview mode (built-in steps for the authoring dialog); PlansPanel gained Your variations cards (scope tag, base, usage count, Land + Delete) and an Author a variation editor (base select + load-base steps, per-step tool/args-JSON/why rows, add/remove, scope select, client-side JSON validation); land_episode_plan DSH text/handler cover variations; context planTemplates line lists them
- Daily digest (digest.ts NEW): buildDigestFromEvents (pure: 24h window filter, renders/plans/schedules buckets with ERROR counting from fire summaries, canon + drift headlines, queue-now line, honest empties on quiet days), postDailyDigest (builds + lands a DIGEST production event with the full text in payload), listDigests (panel feed); scheduler gained the DAILY_DIGEST kind (fire = postDailyDigest, registration copy, kind validation); /api/digest GET/POST; DigestPanel on the DSH view (latest + history with expandable lines, Post digest now, Arm nightly digest shortcut, armed-chip honesty); scheduler panel kind select + DAILY DIGEST chips; DSH grew to 46 tools with post_digest; doctrine rule 23 THE STUDIO WRITES TO THE CREATOR + rule 20 amended
- E2E: scripts/e2e-iter36.ts (plan 50 pure checks: opt-in gate matrix incl. host-wins + unset default-off, retire rollup matrix, drift rollup matrix incl. ordering/trends/worst-aspect/sort, variation validation + deep token resolution + built-in loader, digest window/buckets/error naming/quiet-day honesty, 46-tool registry, doctrine rules 20-23, previz labeling + routes + UI + schema on disk; tool 40 live checks: REAL sheets/panels, the closed previz slot proven in the REAL driver chain (pose-carrying shot lands MOTION), drift curves through the REAL persistence path (Lin Yue DECLINING -20% over 2 panels) + the re-paint queue, retire suggestions through REAL BROKEN verdicts + the live reword/retire API loop, a variation saved/landed by id/duplicate-refused/tokens resolved to E01/REALLY run to a created scene/studio-scope saved+listed+deleted, DAILY_DIGEST fired via fireScheduleNow + API + post_digest, pulse + context lines)
- The pure pass caught two test-design flaws (IdentityScore.scores is a bare entries array; the digest picks the newest event it is handed); the live pass caught two more (0.62 is honestly above the 0.6 identity bar; string-token resolution), all fixed on the test side
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): fixture project "E2E Browser Iter36" (scripts/browser-fixtures-iter36.ts seed|clean: 3 REAL-scored panels building the declining curve + below-bar Xiao Chen, a 3-BROKEN fact, a saved variation, a posted digest) - Render view shows "img2vid previz slot: off (default)"; Continuity renders the drift-curves section (DECLINING -20% sparkline row) and the canon-health AUTO-RETIRE SUGGESTIONS with the live Retire click landing the "Fact retired (deactivated)" banner; DSH view renders the STUDIO DIGEST panel (latest chip, Post digest now REALLY landing a second digest, Arm nightly digest REALLY creating the DAILY_DIGEST schedule) and the plans panel's YOUR VARIATIONS with the author loop (Load base steps REALLY resolving the built-in, Save variation REALLY saving, Delete REALLY removing); the browser pass caught and fixed a REAL runtime bug (DigestPanel's schedules query key collided with SchedulerPanel's differently-shaped query -> .some is not a function; fixed with a unique key + unconditional enable + correct invalidations); 0 page errors, console clean; fixture removed (project + cascade + 10 orphan artifacts swept)
- scripts/verify-iter36 check: DB clean after cleanup (1 project, 0 plans/templates/digests)
- README: DSH row (46 tools + variations + digest clauses), Render pipeline row (previz demotion), render-engine bullet (opt-in default-off previz), try-the-loop 1/4, Status row (46 tools + health + digest thesis), roadmap items 1/2/3/5 graduated (previz demotion, auto-retire, drift curves, variations, digest delivered; new next steps: fact-level drift curves, digest delivery integrations, composition-tolerant embeddings)

Stage Summary:
- GitHub main pushed with this iteration
- The studio's output stance is now explicit: the render path is designed (Blender + MOTION), the interpolation model is an opt-in previz/benchmark slot that is off by default and labeled PREVIZ everywhere, never the deliverable
- The canon panel suggests its own fixes now: facts that keep failing earn reword-or-retire suggestions with a live two-button loop, and identity scores grew from points into per-character curves over episode order with declining-character watch lines riding the pulse and the context
- The creator can author their own per-episode plan variations (cloned from built-ins, token-resolved, registry-validated, studio-shareable) and the studio writes to them: a scheduled or on-demand daily digest lands the last window of activity as one readable message
- Next horizons: fact-level drift curves, composition-tolerant embeddings, digest delivery beyond the studio, neural viseme pass, voice cloning slot, multi-user studio, distribution integrations

---
Task ID: 37
Agent: Super Z (main agent)
Task: Iteration 37 - fact-level drift curves, digest delivery beyond the studio (webhook/email), and the neural viseme pass (user: "proceed with fact-level drift curves, digest delivery beyond the studio (webhook/email), and the neural viseme pass")

Work Log:
- Push state verified first: main in sync with origin/main (iteration 36 pushed as fa0602f)
- Schema: StudioSchedule gained webhookUrl + digestEmail columns (DAILY_DIGEST delivery targets, both nullable = studio-only); db push + client regen + dev restart; nodemailer 7.0.13 installed (dynamic import, guarded) for the SMTP email slot
- Fact-level drift curves (canon-health.ts): FactDriftPoint/FactDrift/factDriftFromEvents (per-fact confidence curves over episode order from the FACT_HELD/FACT_BROKEN event history, matched by the entityName text prefix like the health rollup; points ordered by episode then check time; events without a parseable confidence or episode position skipped - a curve point needs both; delta = last-first, trend bands at FACT_DRIFT_TREND_THRESHOLD 0.05 into DECLINING/IMPROVING/STABLE/FLAT, holdRate across the curve, sorted steepest decline first) + factDriftData (loader + watch subset + headline); canonHealthData returns drift; the canon-health panel renders Fact drift curves with inline sparklines (the identity DriftSparkline generalized to {score} points), trend chips, latest confidence, panel counts and hold rate; studio_pulse gained a Fact drift line + headline part; the DSH context gained factDrift; doctrine rule 22 teaches "a fact losing verdict confidence across the show is headed for a violation streak - propose the reword before the queue floods"
- Digest delivery beyond the studio (digest.ts + scheduler.ts): DeliveryTarget/DeliveryOutcome + deliverDigest (webhook: POST the digest JSON {project, headline, lines, windowHours, events, postedAt} with an 8s timeout and a redacted target in outcomes; email: dynamic-import nodemailer, requires ANIMEOS_SMTP_URL, honest "no SMTP transport configured" outcome when unset, invalid addresses refused before transport); postDailyDigest gained targets + returns deliveries, lands them in the digest event payload and appends a "delivered: ..." clause to the summary; listDigests parses deliveries back for the panel; scheduler DAILY_DIGEST fires pass the row's targets and name the outcomes in lastReport; createSchedule validates webhook (http(s)) + email (RFC-ish) and stores them on the row; ScheduleView exposes them; PATCH /api/schedules gained action set_delivery (digest-kind-only guard, clearable); DigestPanel gained the Delivery beyond the studio editor (webhook + email inputs, Save delivery over PATCH, armed-targets line) and digest cards render delivery outcome chips (OK emerald / FAILED rose with the detail tooltip); DSH create_schedule accepts webhookUrl/digestEmail and reports the delivery note; doctrine rules 20/23 teach delivery beyond the studio
- Neural viseme pass (src/lib/animation/viseme-neural.ts NEW): a 15-entry NEURAL_VISEME_SHAPES vocabulary (AA/EY/IY/OW/UW/AH + M_B_P/F_V/TH/S_SH/N_L/K_G/R/W_Y/SIL, same 0..1 o/w/r axes); parseNeuralPlan (strict-JSON parser: per-line unit lists, weights clamped 0.2..3, unknown visemes fall back to AH, garbage refuses to null); neuralPhonemePlan (ONE LLM call per program over the z-ai SDK with a 14s timeout, cached per line set (LRU 64), null on any failure); planVisemesFromPlan (units spread across the span window weighted like the text path, monotonic clipped); conformVisemesToPlan (AUDIO CONFORM: openness stays audio-owned, wide/round identity adopted from the plan segment covering each audio segment's midpoint); neuralSpeechProgram (spans with a decodable take get audio-timed plan-shaped visemes, spans without audio get the plan's own performance, a failed plan degrades to audio-only then the per-character text table - the render never blocks on the model); describeNeuralSpeechProgram notes the shaping source; render.ts shotSpeechProgram awaits the neural pass inside try/catch with the audio-only fallback; the doctrine intro teaches the pass
- E2E: scripts/e2e-iter37.ts (plan 34 pure checks: parser clamps/refusals, plan->visemes monotonic spread + shape table, conform openness/identity split, fact-drift rollup matrix incl. skip rules/trends/hold rate/steepest-first, delivery refusals, registry args, doctrine rules 20/22/23 + intro, files on disk incl. schema columns + set_delivery route; tool 29 live checks: the neural plan through the REAL LLM seam with a REAL synthesized WAV take (plan-conformed identity from the shape table + audio-owned openness + the honest lipNote on a REAL render job + the no-take plan performance), fact verdicts through the REAL pipeline across TWO episodes building a DECLINING -35% mask curve + the canon API/pulse/context lines, a REAL local webhook receiver (Bun.serve) the DAILY_DIGEST fire REALLY POSTs the digest JSON to with the delivery outcomes in the report and the event payload, the honest no-SMTP email FAILED outcome, set_delivery over HTTP (save + clear + re-save) and the cleared-target fire skipping the webhook)
- The pure pass caught two test-math bugs of mine (an extra E4 point changed delta to -0.13 and hold rate to 3/4); the live pass caught two REAL fixture design errors: the audited shots lacked panel art (the vision check honestly refuses art-less shots) and check_universe_facts re-audits replace per PANEL (same shot = one verdict set), so the curve needs different shots in different episodes (fixed with a scene-2 episode-2 fixture and both-facts verdict bodies); one eslint error (non-null-asserted optional chain) fixed
- Regression: e2e-motion-cut (tool) ALL PASS - the cut export pipeline is unaffected
- Verified E2E (agent-browser): fixture project "E2E Browser Iter37" (scripts/browser-fixtures-iter37.ts seed|clean: mask curve DECLINING 0.9->0.55 + stable moons across two episodes, a posted digest with a deliberately unreachable webhook so the FAILED outcome renders) - the canon-health panel renders FACT DRIFT CURVES with DECLINING -35% and STABLE -2% sparkline rows; the DSH digest panel renders the webhook FAILED chips on the digest cards, the Arm nightly digest click REALLY created the schedule, the Delivery beyond the studio editor appeared, and Save delivery REALLY persisted both targets ("armed targets: webhook + email"); 0 page errors, console clean; fixture removed (project + cascade)
- DB verify after cleanup: 1 project, 0 digests, 0 schedules
- README: Status row (fact curves + delivery + neural visemes), DSH row (delivery targets clause), Render pipeline row (neural viseme pass), Blender bullet v3.4 clause, roadmap items 1/2/3 graduated (neural viseme pass, fact-level curves, digest delivery delivered; new next steps: an acoustic-model slot that re-times the plan from the real audio, a reworded-fact re-audit helper, platform-publishing integrations on the delivery spine)

Stage Summary:
- GitHub main pushed with this iteration
- The canon now curves like the cast does: per-fact confidence over episode order joins the per-character identity curves, so a fact sliding toward a violation streak is visible (and rewordable) before the queue floods
- The digest leaves the building: webhook POST + SMTP email with validated targets, steerable from the digest panel, and every outcome - OK or honestly FAILED - recorded on the digest itself
- The mouth got a language brain: an LLM-planned phoneme score supplies the wide/round/closed identity the audio envelope could never recover, conformed onto the real take's timing, with honest degradation at every step
- Next horizons: an acoustic-model slot that re-times the viseme plan from the WAV, a reworded-fact re-audit helper, platform-publishing integrations on the delivery spine, voice cloning slot, multi-user studio
